// ---------------------------------------------------------------------------
// skills_core_adapter — sdd_skills' client hop into sdd_core.
//
// Identity re-exports of the core portals: the packs whose skills extend the
// built-ins, and the configuration naming the enabled targets.
// ---------------------------------------------------------------------------
export { loadProjectExtensions, loadProjectConfig } from '../index.js';

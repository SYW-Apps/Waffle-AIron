# Phased Design — Waffle-AIron

## Base system (Stages 1–6)
- [x] Stage 1–5: L0–L5 designed for sdd_core, sdd_validator, sdd_cli, sdd_mcp, sdd_skills, sdd_git, sdd_producers, sdd_host (base)
- [x] Stage 6: implemented; tree validates; hosted server shipped

## Professional-use expansion (sdd_host draft layer, committed 2026-07-09)
Design doc: `docs/design/hosted-professional-use.md`. Phases run: design-finalize → validate → human lock → implement.

### Phase 1 — Identity + Audit (ACTIVE)
- [x] L3 contract fixes on iidentity_orchestrator (credential-based auth pattern; add listUsers/queryAuditEvents/pruneAuditEvents; defer resolveOrCreateUser + recordAuthenticatedAction)
- [x] L4/L5 batch 1: audit_store/registry/index/repository, user_store/registry/index/repository
- [x] L4/L5 batch 2: identity_orchestrator (full), identity_portal (calls-only)
- [x] Integration deltas on complete components (user-approved): auth_specialist grants/subject resolution (+authenticateCredential); host_request_orchestrator audit append
- [x] Status promotion to complete (identity_provider_adapter stays draft until Phase 5/SSO)
- [x] User-provisioning gap closed: admin upsertUser + setUserStatus on identity portal/orchestrator; allows narrowed to 4 (findByExternalSubject ×2 → Phase 5 SSO, audit count ×2 → Phase 5 pagination)
- [x] sdd_validate_tree zero errors; validate --ci exit 0; as-complete gate zero errors
- [x] Human ran `wairon lock` (2026-07-09; blanket draft→complete freeze on Phase 2–5 planes reverted — product gap: lock needs phase awareness)
- [x] Implementation: auth foundation (grants/expiry/authenticateCredential), audit plane (audit.ts), user plane (users.ts), identity API (identity.ts, /identity/* on admin listener), data-plane mcp.tool.call audit append (request.ts). 293 tests green; live e2e verified (mint → scoped MCP call → audit provenance).

Phase 1 COMPLETE. Vocabulary follow-ups for the Phase 2 spec pass: canonicalize the 'user:admin' permission string; decide whether mintToken should resolve ownerSubject from the user repository (currently synthesized from ownerUserId).

### Phase 2 — Approvals + Self-service (ACTIVE)
Approved design: credential-based iself_service contract (house pattern); policy + identity deps deferred (Phase 3+); decision surface = admin_portal → self_service directly (avoids the admin_orchestrator cycle); admin plane gains pre-authorized executeApproved* entry points (approval IS the authorization — master-gated methods can't serve execution); host_request dispatches the four sdd_host_* MCP tools; lazy expiry wires expirePending; self-approval invariant on decideRequest; new grant vocab: approval:decide.
- [x] L3 rework (credential pattern) + member interfaces (iapproval_store/registry/index) + narratives (approval triad intent, facade forwarding, 7 full self-service methods) + deltas (host_request four-tool switch dispatch; admin_portal /admin/approvals endpoints; admin_orchestrator executeApproved* entries) + promotion; index/facade filter param aligned (requestedByUserId)
- [x] Validation green: sdd_validate_tree + validate --ci exit 0, zero warnings on promoted specs; as-complete gate zero errors
- [x] Human ran `wairon lock` (draft-freeze on Phase 3–5 planes reverted again, status-only)
- [x] Implementation: approvals storage (approvals.ts, 23 tests) → self-service orchestrator + pre-authorized admin entries (selfservice.ts/admin.ts, 22 tests) → MCP dispatch (request.ts, 6 tests) ∥ admin endpoints (http.ts, 11 tests). 355 tests green; live e2e verified: agent requested init over MCP → admin listed/approved/executed → project provisioned; audit chain complete (token.mint → approval.request.created → mcp.tool.call → approval.decided → approval.executed).

Phase 2 COMPLETE.

### Phase 3 — Project policy / profiles (ACTIVE)
Approved design: IdP methods on ipolicy_repository deferred to Phase 5; policy-aware approval execution via pre-authorized executeApprovedInit wrapping admin executeApprovedCreate (keeps it wired; self_service → policy → admin acyclic); request-time evaluateInitRequest in requestProjectInitialization (warn attaches findings, enforce rejects); direct portal creation gated by project:create; new vocab: policy:manage. Policy store = single active InstancePackPolicy at pack-policy.json.
- [x] Spec finalization; all gates green; human locked (draft-freeze reverted)
- [x] Implementation: policy plane (policy.ts, 17 tests; packs.ts pre-authorized entries) → self-service retarget (5 tests). 377 tests green; live e2e: block-mode policy rejected a non-compliant agent request at request time with actionable findings; compliant request flowed through approval; project provisioned with profileSelection recorded.

Phase 3 COMPLETE. Tech debt: ProjectConfigSchema doesn't model profileSelection (raw-YAML workaround in policy.ts) — fold into the core-model pass. UX watch-item (Robbe): warn/block surfacing needs an end-user pass with the Phase 5 UI.

### Phase 4 — Landscape / organization (ACTIVE)
Approved design: credential-based discovery methods (uniform with Phases 1–3); directional relations-only reachability (placements = visual grouping, no access); upsertRelation validates targetPublicInterface against the target's snapshot (refresh required first); refreshPublicSurface reads L0 publicInterfaces leniently via raw YAML (core SystemSpecSchema gap stays in the validation-extensions batch, like profileSelection); private-by-default (no L0 surface = empty snapshot); vocab += landscape:manage, landscape:read. Source layout: organization.ts / relations.ts / surfaces.ts (triads), landscape.ts (orchestrator+portal+specialist).
- [x] Spec finalization (9 member interfaces, removeRelation + getUnit wirings, host_core_adapter spec-read forwarders, organization_state type); all gates green; human locked (draft-freeze reverted)
- [x] Implementation: 3 parallel triads (organization/relations/surfaces, 39 tests) → landscape.ts (18 tests; adapters.ts spec-read forwarders) → request.ts discovery dispatch (6 tests). 440 tests green; live e2e: beta published an L0 surface → redacted snapshot → snapshot-validated relation alpha→beta → alpha's agent discovered exactly [beta] + its redacted interfaces; gamma denied with no existence leak.

Phase 4 COMPLETE. Type-spec fix pending: organization_unit_record.parentId required-vs-"when set" mismatch (typed optional in code).

### Security-hardening pass (post-prod-review, ACTIVE) — decisions from Robbe 2026-07-10
Prod-readiness review found blockers; docker VERIFIED production-ready (built/booted/health/placeholder-refusal/admin-isolation all pass). Fixing now:
- **B1 user lifecycle**: collapse to ONE active|inactive flag (drop suspend/deactivate distinction — overkill). inactive = revoke all keys (credential_registry sweep by ownerSubject) + authenticate() blocks inactive + completeSsoLogin blocks inactive re-login. Record KEPT for audit provenance (no hard-delete by default; optional hard-delete after deactivation). Contract changes: auth_specialist status check, credential_registry new revokeAllForOwner, identity_orchestrator setUserStatus triggers revocation.
- **B2 projectPath chaining**: KEEP as full feature hosted+local (Robbe corrected me — the "NOT chaining" spec line is about the isolation unit, not disabling chaining within a project; bigger hosted systems need it MORE). Fix = CONTAINMENT only: loader (specs.ts ~585/654) + setter must resolve projectPath WITHIN the bound project root, reject absolute + ../ escape. Zero feature loss. Cross-PROJECT stays the landscape plane's job.
- **S2-minimal**: instance-permissions (audit:read, landscape:read/manage, operations:read, user:admin, approval:decide, policy:manage, project:create) require an INSTANCE-WIDE grant; reject/ignore project-scoped forms so they can't confer instance-wide reach. (Full org-unit-scoped admin = deferred Phase 6.)
- **S5**: mintToken must verify the caller holds each delegated permission for that project (not just key:manage). **S3**: max-body cap on readBody before auth on public /mcp.
Deferred: S4 (HostExposurePolicy requireTls/allowedOrigins/allowedNetworks unenforced), JWKS verification, N-notes.

### Phase 6 — Tenancy & scoped administration (ACTIVE) — decisions from Robbe 2026-07-10
Make Phase-4 org units ACCESS-bearing. Approved: FULL delegated admin (scope-filtered), BUNDLE 6b (granular data-plane perms), RECURSIVE subtree.
- **Grant scope**: ProjectGrant gains optional `orgUnitId` — a grant scoped to a unit covers every project placed in that unit AND all descendant units (recursive subtree via units+placements). Third scope kind alongside projectId / '*'.
- **User membership**: HostedUserRecord gains optional `unitId` (home org unit) — user-list scoping keys on it.
- **scope_specialist** (NEW, pure, sdd_host): given a principal's grants + the org unit tree + placements, computes the in-scope project-id set and unit-id set for a permission (or ALL for a '*' super-admin grant). Centralizes scope logic (also retires the per-file carriesInstancePermission smell). Orchestrators gather org data (organization_repository) and call it.
- **6a scoped control-plane**: identity (audit/users/grants/token-mint), landscape (units/placements/relations/graph), operations (health/usage), self_service (approval:decide/list), admin (project create into unit) — each authz becomes "super-admin ('*') OR a unit-scoped grant carrying the permission," and results FILTER to the in-scope set. Several complete orchestrators gain organization_repository + scope_specialist deps. super-admin='*'; tenant owner=root-unit admin; team lead=sub-unit admin.
- **6b granular data-plane**: host_request_orchestrator enforces mcp:read vs mcp:write per tool — write sdd_* tools (add/update/delete/write/define/set/initialize/externalize/internalize/move) require mcp:write in the grant covering the bound project; read tools (get/validate/status) require mcp:read. Makes read-only project tokens real. Needs a tool read/write classification.
Loop: spec-finalize (one comprehensive pass) → review scope-resolution + data-plane-perm narratives → human lock → implement (waves) → verify + re-review (adversarial, given it's authz).
- [x] Spec finalized + locked; god-component resolved via configurable threshold=10 (zero new allows).
- [x] Implemented: scope.ts engine (10 tests) → identity/landscape/operations/selfservice/admin scope-filtering + admin data-plane 6b (3 parallel agents). http.ts admin catch maps ForbiddenError→403. 597 tests. Live e2e: tenant owner (acme) saw both projects, team lead (web) saw only p-web, out-of-scope unit write 403; read-only token refused sdd_initialize_system with "permission mcp:write required" while sdd_get_status passed and a read/write token wrote successfully.
- [x] Adversarial re-review DONE (2026-07-12): authz core is sound (cross-tenant isolation, recursive org-subtree scoping, deactivation revocation, mintToken delegation all verified). ONE HIGH found + fixed: data-plane JSON-RPC batch bypassed the mcp:read/write gate (gate saw only msg[0], transport dispatched all) → multi-request bodies now refused (400). Grant normalization (projectId:'*'+orgUnitId ≠ super-admin) applied everywhere. Commit 558ee37.

### Phase 5 — split per approved scope decision
**5a — Headless SSO onboarding (ACTIVE)**: /identity/sso/start + /identity/sso/callback on the identity portal (OIDC code exchange → resolve-or-create user inlined into completeSsoLogin → mint user-bound token shown once); stateless HMAC SSO state signed by auth_specialist; IdP config CRUD restored on ipolicy_repository (instance-admin gated, surfaced on /identity/providers); audit count endpoint. Clears all 4 lint allows + every Phase-5 deferral marker. resolveOrCreateUser resolves as inlined narrative steps (a public method would be uncallable → honest inline instead).
**5b — Slim operations + exposure enforcement**: health/usage/advisory-quota read-only surface (diagnostics + quota specialists); HostExposurePolicy actually gates the mounted control planes; backup_* + backup_archive_adapter + restore specs DELETED (platform snapshots are the documented backup path).
**Deferred as drafts**: admin_ui_* (+ web_session) pending real-usage/UX evidence; local_control_portal pending exposure-policy-driven need.
**5c — skills resources** (sdd_mcp/sdd_skills): after 5a/5b.
- [x] 5a COMPLETE: spec'd, locked, implemented (idp.ts OIDC adapter; auth.ts SSO state; policy.ts IdP storage; identity.ts six flows + routes; errors.ts extracted breaking the identity↔policy cycle). 476+ tests; live e2e: stub provider → start → callback → first-login user (empty grants) → 403 on data plane → admin grants → re-login → data-plane success; zero lint allows remain anywhere. Bonus fix found by e2e: URL-decode path segments in all four routers (SSO ids carry colons).
- [x] 5b spec finalization (backup plane deleted end-to-end; operations slimmed to read-only health/usage/advisory-quota; HostExposurePolicy enforcement at host_server; subsystem surface de-staled); locked
- [x] 5b implementation COMPLETE: operations.ts (pure specialists + gated orchestrator + 3 GET endpoints, 22 tests) + exposure gating (compatibility default; <dataDir>/exposure-policy.json source; 8 tests incl. dual-listener). 506 tests; live e2e: policy-disabled identity/landscape 404 while admin/operations/data-plane serve.
- [x] 5c COMPLETE: skills as MCP resources (spec'd via the forced Adapter→Portal boundary chain; implemented in src/core/skills.ts + src/mcp/server.ts; stdio + hosted scoped servers publish identically via the shared factory; 10 tests; live-verified over the hosted data plane incl. resources/read).
- [x] Extension-image pack layering COMPLETE (spec'd, locked, implemented, live-verified: baked pack visible tier-tagged → instance install shadows it → drift in listing AND pack-shadowing health warn naming the pack; Dockerfile bakes /opt/wairon/packs + env; docs carry the sample extension-image Dockerfile). Original design intent: **baked images are the PRIMARY pack channel for hosted instances** — `FROM ghcr.io/syw-apps/wairon:X` + `COPY packs/ /opt/wairon/packs/` → build → redeploy keeping the volume; custom pack combinations = custom Dockerfiles. CLI/exec pack install is repositioned as LOCAL-user tooling (getting pack files onto an isolated cloud VM via scp/download+exec is the awkwardness being designed away). Two-tier pack source: immutable image-layer dir (/opt/wairon/packs) merged with the mutable volume dir (/data/packs); rationale: named volumes seed from image content only on FIRST creation, so baking into /data/packs breaks extended-image upgrades. Open decision to present: name-collision precedence (recommend volume-wins + a diagnostics check making image-drift visible; keeps the exec escape hatch meaningful while images stay authoritative in practice). Includes: merged discovery (WAIRON_IMAGE_PACKS_DIR), Dockerfile prep, docs repositioning (extension-image pattern as the recommended hosted path, sample Dockerfile), and diagnostics checks for shadowed packs + projects referencing missing packs/profiles. Spec delta → approval → lock → implement.

### Phase 7 — Public Surface Exchange (Stage 1 of surface/visibility roadmap)
Contract-grade, portable public-surface snapshots — one artifact, three origins (generated for chained children at generate/lock; exchanged between wairon projects/tenants; authored/imported for external 3rd-party systems, OpenAPI as the first codec). The project's L0 `publicInterfaces` becomes the formalized gateway surface (each entry backed by a subsystem-published Portal, carrying an `audience` ceiling: project < department < instance < partner < external); the surface projector filters by ceiling and embeds full L3 contracts + dispatch tables + the transitive type closure + StateId provenance. Validation resolves cross-tree/remote adapter refs against stored snapshots (SURFACE_STALE on StateId mismatch; CROSS_TREE_REF_UNRESOLVED only without coverage). Stage 2 (org-unit visibility grants: unit tree already nests via parentId; add visibility posture + exposeTo) and Stage 3 (landscape completion, Swagger serving) layer on top without rework.
- [x] Spec draft: sdd_surfaces subsystem (surface_portal → surface_orchestrator → repository[store ram-projection/registry/index/fs adapter] + projector + openapi_codec + core/transfer adapters), lifecycle init hydrate flow, consumer adapters (validator_surfaces_adapter, cli_surfaces_client_adapter + `wairon surface` command), types (SurfaceSnapshot/SurfaceContractEntry/SurfaceTypeDef), audience ordering formalized on SystemPublicInterface, SystemSpec.publicInterfaces documented. Validates clean (new binary, zero errors).
- [x] Human review + lock of the Stage 1 draft
- [x] Implementation COMPLETE: models (SystemSpec.publicInterfaces + surface schemas), src/core/surfaces.ts (projection, audience ceilings, type closure, storage, child generation, freshness) + openapi.ts codec, validator resolution (SURFACE_REF_NOT_EXPOSED / SURFACE_STALE / snapshot-resolved cross-tree refs incl. dependsOn + dispatch capabilities, Adapter shape enforced across project boundaries), `wairon surface` CLI. 668 tests; live: export/list/validate on own tree.
**Stage 2 — Unit visibility semantics (DRAFTED, awaiting lock)**: OrganizationUnitRecord gains `visibility: inherit|open|closed` + `exposeTo: [unitId]` (no deny-lists — closed groups cover restriction; tenant roots always closed cross-tenant, exposeTo is the only bridge). NEW pure `visibility_specialist` (sdd_host): resolveVisibility/isVisible/audienceDistance over units+placements → VisibilityResolution (distances department|instance|partner intersect with entry audience ceilings). Enforcement: upsertRelation requires target-surface visibility (not just snapshot existence); listReachableProjectInterfacesForMcp audience-filters summaries by distance; NEW listVisibleSurfaces discovery catalog (orchestrator + portal GET /landscape/projects/{id}/visible-surfaces → VisibleSurfaceEntry[]). Dogfood: wairon's own L0 now declares hosted-mcp-data-plane (external) + hosted-admin-api (instance) gateway entries.
- [x] Human review + lock of the Stage 2 draft
- [x] Implementation COMPLETE: src/server/visibility.ts pure engine (7 tests: closed groups, exposeTo grants, cross-tenant fail-closed, distances, audienceCovers) + landscape enforcement (upsertRelation visibility gate with a no-org-units legacy escape, spec'd; listReachableProjectInterfacesForMcp audience filter; listVisibleSurfaces + GET /landscape/projects/{id}/visible-surfaces).
- [x] Surface-artifact DOWNLOAD (user-directed scope add): GET /landscape/projects/{id}/surface?format=native|openapi&audience= — generate-on-request attachment, the diagram pattern; host_surfaces_adapter → surface_portal spec'd + implemented. Swagger UI SERVING deliberately deferred OUT of this roadmap (separate scope). 675 tests; self-validate clean.
**Stage 3 — Consumer exchange + relation health (IMPLEMENTED, awaiting lock)**: data-plane MCP tools `sdd_landscape_list_visible_surfaces` (visibility-resolved discovery catalog for the bound project) and `sdd_landscape_get_project_surface` (CONSUMER path: visibility-gated — legacy relations-only fallback on unit-less instances — contract-grade snapshot at the observer's audience-distance ceiling, origin `exchanged`, ready for `wairon surface import`); host_request_orchestrator dispatch extended to 4 landscape tools; `relation-health` diagnostics check (active relations targeting missing snapshots or drifted interfaces → warn) via extended runChecks + operations gather. Swagger UI serving remains OUT (separate scope). 678 tests; self-validate clean.

### Phase 7 — Unified web UI (ACTIVE) — decisions from Robbe 2026-07-11
NOT an "admin UI": ONE role-based web app for everyone (developers author specs visually; admins additionally get control-plane pages), capabilities gated by the Phase 6 grant/role model. Decisions: full level-of-detail now; unify local+hosted on the live model (wairon diagram --serve + hosted; static single-file becomes an export); reframe admin_ui_* → web_ui.
Architecture: THIN web server layer that REUSES the whole scoped API + data plane. Bridge = a browser SESSION is resolvable to a Principal like a bearer token (auth_specialist.authenticateSession + web_session_repository), so the browser authors via the existing /mcp data plane (Phase-6b granular perms) and admin pages call the existing identity/landscape/operations/policy portals (Phase-6 scoped). New server surface: session storage + auth bridge + live JSON+LOD graph endpoint + web portal/orchestrator shell serving the client app. Slices 2 (authoring) + 3 (control pages) are mostly CLIENT (L4 served by web_portal), no new server components.
Sections: §1 session foundation (web_session type + repository triad + auth bridge); §2 live scoped graph keystone (sdd_core diagram_specialist.buildGraphModel JSON+LOD → core_portal → host_core_adapter.buildProjectGraph; web_graph_model type; web_orchestrator.getGraph+getCurrentContext; web_portal routes); §3 reframe admin_ui→web_portal/web_orchestrator + SSO session lifecycle. local_control_portal left as separate deferred draft. Styling reuses canvas --syw-* theme. SSO browser session = new security surface → own adversarial review at impl. NOTE: sdd_surfaces (OpenAPI/public-surface exchange, 80%, in-flight parallel) is separate — covers the Swagger-hosting idea; leave alone.
- [x] Spec authoring COMPLETE (web_session triad + auth bridge; buildGraphModel JSON+LOD; web_graph_orchestrator split out at 5 deps / web_orchestrator 7; web_portal; admin_ui_* deleted). Keystone auth-bridge fix: authenticateCredential resolves session ids (not just a separate authenticateSession) so reused endpoints authenticate a session. Zero errors, zero warnings on web_ui specs. OPEN: single-active-session vs multi-device decision; --ci red from PRE-EXISTING sdd_surfaces warnings (landscape 11 deps, untyped seam — theirs).
- [ ] Human lock → implement (waves) → verify + adversarial re-review (SSO session security surface)
  - [x] SSO session surface adversarial review DONE (2026-07-12): plumbing sound (cookie attrs, session entropy, fixation, sign-out completeness, dev-bypass gating all verified). ONE HIGH found + fixed: SSO login CSRF — the state nonce was signed but never browser-bound → now a short-lived HttpOnly nonce cookie must match the signed state on callback (commit 558ee37). Redirect_uri allowlist (MEDIUM) NOW IMPLEMENTED: IdentityProviderConfig.allowedRedirectUris + idp.assertAllowedRedirectUri enforced in startSignIn.
  - [x] Slices 2 (authoring) + 3 (control pages) IMPLEMENTED 2026-07-12 (web UI is now feature-complete + shippable, still opt-in via exposure.webUiEnabled): client SPA rebuilt with Canvas/Specs/Admin tabs. Specs = component index (from /web/graph) + structured inspector + description edit via sdd_update_spec + Validate via sdd_validate_tree over the existing /mcp data plane (write affordances disabled for read-only sessions). Admin = new session-scoped /web/admin/{users,landscape,health,usage,approvals,approvals/decide} routes in handleWebRequest reusing the Phase-6 scoped functions (listUsers/generateLandscape/getHealthReport/getUsage/listPendingRequests/decideRequest) — each authenticates the ws_ session credential + filters to grants; viewer → 403. Spec sync: web_orchestrator dependsOn += operations_orchestrator + self_service_orchestrator (conformance-required), identity_provider_config += allowedRedirectUris; admin routes are L4 dispatch detail of web_portal (no new contract methods, per the blessed "slices 2/3 = client L4, no new server components" design). 796 tests (+9: idp-redirect 6, admin routes 3), typecheck + build + validate --ci all green; conformance 0 findings; live-smoke: shell serves with 3 tabs, /web + /web/admin fail-closed 401 without auth. OPEN operator decision: default webUiEnabled on vs off (kept OFF — enabling a browser SSO surface by default is a posture call for the operator).

## Code↔Spec Conformance pillar (ACTIVE) — decisions from Robbe 2026-07-11
Staged: Level 1 (structural) IMPLEMENT; Level 2 (dependency) DESIGN; Level 3 (call-graph↔L5) SKETCH only.
Approved decisions:
- **D1 Architecture**: extend sdd_validator (no new subsystem). NEW `source_analysis_adapter` (Adapter — the only source-code I/O + parser tech binding): buildCodeModel(implementations, projectRoot) → pure CodeModel (per-file SourceFileFacts: declarations incl. destructuring/object-literal keys/nested scopes, export bindings with export-* barrel chasing, import bindings+specifiers, string-literal anchors, analysis grade). validateSddTree builds+injects it into RuleContext beside surfaceSnapshots (sync); conformance rules are ordinary registry modules (src/core/rules/conformance.ts).
- **D2 Realization semantics (N:1 native)**: model keyed by resolved sourcePath; each impl checked independently; one anchor satisfies every component declaring that method name. "Realized" = anchor at/above dial tier. Tiers: `declared` (named declarations any depth, destructuring, object-literal keys, export specifiers incl. resolved barrels, import bindings) | `anchored` (+ exact string literals — tool/route registrations) | `off` (generated/vendored; file existence still checked). NEW L4 fields: spec-level `conformance` dial + per-method override + per-method `symbol:` (intent-language renames, e.g. isurface_store.put → saveSnapshot). Stereotype defaults: Portal → anchored, else declared.
- **D3 Parsing (LEAN, any-language — Robbe custom)**: NO mandatory runtime dep. Tiered analyzers: (a) exact — TS/JS via dynamically resolved `typescript` from the analyzed project's node_modules, else wairon's own install, never bundled; (b) declarative per-language pattern tables (declaration keywords, import patterns, comment styles) extending the language-pack table concept — packs add languages declaratively or register exact analyzers programmatically; (c) generic word-boundary scan as universal fallback. CodeModel records analysis grade (exact|pattern|generic); finding messages carry it. Hosted repos without node_modules degrade to pattern grade, never silently pass.
- **D4 Delivery**: rule family inside validateSddTree (roundtrip precedent), all codes completeness-classed (draft-waived in --ci): MISSING_SOURCE_FILE (error), SOURCE_PATH_ESCAPES_ROOT (error, mirrors B2 containment), UNREALIZED_METHOD (warning), MISSING_SOURCE_PATH (warning), CONFORMANCE_ANALYSIS_SKIPPED (warning, rare — unreadable/binary only given the generic floor). UNDECLARED_EXPORT deferred to Level 2 (needs import graph to avoid false-flagging closure-realized components).
- **Level 2 design (implement if time)**: same adapter's imports → resolve relative specifiers → file→file edges → lift to components via sourcePath map; edge justified iff some declared dependsOn/owns pair (or same file/component) covers it; `import type` allowed. Codes: UNDECLARED_DEPENDENCY, UNREALIZED_DEPENDENCY (same-file/DI skipped), UNDECLARED_EXPORT. Union semantics under N:1 = under-report, no false positives.
- **Level 3 sketch (separate effort)**: L5 call/dispatch steps vs realizing function's call graph; needs ts.Program+checker, DI/closure symbol resolution, capability-string matching; per-method opt-in dial.
- Dogfood forecast: ~15 impls lack sourcePath (sdd_surfaces ×11, visibility_specialist, host/cli surfaces client adapters — code exists: surfaces.ts/openapi.ts/visibility.ts/adapters.ts); surface_store needs symbol mappings; portals pass via anchored. Fixing these IS the acceptance test.
- [x] Spec authoring (after 3rd /mcp reconnect finally landed fresh — earlier reconnect raced a 19:47 rebuild): source_analysis_adapter component+interface+L4/L5 (technologies: [typescript-compiler], leaf Adapter), spec_validator dependsOn + buildCodeModel narrative inserts (validateSddTree step 9, validateAsComplete step 9) + rules-enumeration step extended, ispec_validator description extended, types code_model + source_file_facts, implementation_spec type gains conformance field + methods symbol/conformance doc. Portal untouched (1:1 forward to spec_validator). YAML-on-disk verified: technologies, structured params, array fields all landed.
- [x] Validate clean (zero errors; single DRAFT_COMPONENT_WARNING with draftContext=true — --ci waived) → drafts presented → awaiting human lock
- [x] Implement Level 1 COMPLETE: src/core/source-analysis.ts (tiered analyzers: dynamic-TS exact AST w/ export-* barrel chase + property-access weak anchors; declarative pattern tables ts/js/py/rust/go/c#/java/c/cpp/ruby/php/kotlin/swift; generic word-boundary floor; binary→unreadable; N:1 dedupe) + rules/conformance.ts (5 codes, completeness-classed) + RuleContext.codeModel injection + L4 schema fields (conformance dial + per-method symbol) + MCP write_narrative schema. Dogfood: 131 findings → 0 honestly — core portal barrel actually republishes provision/diagram/lockfile/statehash (real gap fixed in code); admin_portal re-pointed to src/server/http.ts (routeAdmin IS the portal), cli_core_adapter → src/commands/subsystem.ts; ~100 symbol mappings (intent-language → prefixed code names, incl. users.ts triad functions, surfaces flat functions, runHost* commands, sdd_* tool strings); mcp_core_adapter/mcp_orchestrator dialed anchored (property-access/string-registration realization); 11 per-method offs on split-file facade methods (core_orchestrator provisioning/lock/hash/diagram + validator_core_adapter.dryRunSerializeSpecs) with reasons in descriptions. Spec-only wrapper demo uses lint.allow MISSING_SOURCE_PATH (the sanctioned mechanism, demonstrated). 768 tests (18 new) + typecheck + build green; validate --ci exit 0; `rules list` shows the family. Level 2 (dependency conformance) designed above; imports already collected in SourceFileFacts.
- [x] Level 2 IMPLEMENTED (same session, post re-lock): rules/dependency-conformance.ts — UNDECLARED_DEPENDENCY + UNREALIZED_DEPENDENCY (warnings, completeness-classed). Pure string-wise import resolution against the closed mapped-file set (no I/O in rules); exact-grade files only; type-only imports excluded at collection; export-from specifiers split into SourceFileFacts.reexports (republication ≠ collaboration, but realizes a declared forwarding edge). Justification: direct dependsOn/owns pair | shared component | facade hop (member of a depended-on pattern) | same-subsystem REVERSE edge (mutual portal↔server mounting collapsed to one file direction) | cross-subsystem declared edge to the target subsystem's PUBLISHED surface (in-process imports may land on concrete modules; the barrel is cosmetic). Dogfood 51→0: real code fixes (httpio.ts extracts sendJson/bearerToken out of host_request_orchestrator's file; error-class imports repointed to errors.ts incl. AdminAuthError move; WEB_SESSION_PREFIX → server/types.ts; cli/index.ts imports command modules DIRECTLY not via barrel) + 7 true spec gaps declared (auth_specialist+identity_provider_adapter→secret_registry, scope_specialist→organization_repository [code truth: resolveScopeFor gathers org data itself], self_service+pack orchestrators→project_registry, web_orchestrator→identity_orchestrator, cli_validator_adapter→core_portal) + 9 documented allows (admin statehash bypass, diagram→validator overlay, git/miro/notion shared-secrets placement debt, core_orchestrator split-file facade, web slices 2–3 design-ahead, operations/self_service org-gathering delegated to scope). 778 tests (10 new) + typecheck + build green; validate FULLY clean (zero warnings); --ci exit 0. NOTE: these post-lock deltas re-dirty the statehash — re-run `wairon lock`. Level 3 (call graph ↔ L5 narrative steps) remains sketch-only: needs ts.Program+checker, DI/closure symbol resolution, capability-string matching; separate research effort.

## ZIP extension packs + @wairon/sdk (ACTIVE) — decisions from Robbe 2026-07-21
Portable, versioned `.wpack` archive as THE pack unit (replaces loose-YAML install across CLI + webapp + hosted API), plus a companion `@wairon/sdk` authoring library. SDD flow: spec-first, per-layer approval, implement via generated implementers; no source until specs validate + human lock.
Approved shape (L0/L1 gate): (a) pack commands live in the always-present `@wairon/cli` (`wairon pack init|build|add|list|remove`, `packs` deprecated alias); `@wairon/sdk` = programmatic library scaffolded packs depend on (`import { defineRule } from '@wairon/sdk'`) — robust under global install (no plugin resolution across global-bin/local node_modules). NEW subsystem `sdd_sdk` (= the `@wairon/sdk` package boundary, standalone-publishable, depends on nothing): sdk_portal (Portal) + sdk_orchestrator + pack_codec (Specialist: envelope schema + safety policy) + pack_scaffold_specialist + pack_archive_adapter (Adapter, zip-lib tech). Hosted install: ZIP primary + raw-YAML-text accept kept for one deprecation window.
Format insight: a `.wpack` = a directory pack + root `wairon-pack.yaml` envelope (formatVersion/name/version/kind:declarative|code/entry/min-wairon/integrity). Extraction happens at INSTALL time → `.wai/packs/<name>/` (a directory pack the loader ALREADY reads) → runtime load path unchanged; a raw `.wpack` is an installable artifact, not a loadable ref. Declarative-only hosted install stays (code ZIPs are filesystem/image-only). Safety (SDK extractor): zip-slip normalization, reject abs/`..`/backslash/symlink/non-regular; caps on entries/total-uncompressed/per-entry/ratio(zip-bomb)/depth (hosted stricter + max-body cap). Open for L2/L3: rule-authoring types home (move SddRule/RuleContext/Finding contract into SDK vs SDK-published stable facade core conforms to); web upload transport (raw application/zip + ?name vs base64-json vs multipart); whether to add an L0 system publicInterface for the SDK library API.
- [x] SHIPPED to dev 2026-07-21/22 (commits 3ce5ae0 feature, 5a937fc CI fix, 3316b6a release wiring). CI BREAK FOUND + FIXED: workflows built the root before the sdk/ workspace → esbuild "Could not resolve @wairon/sdk" → added root `prebuild` script (`npm --prefix sdk run build`, makes `npm run build` self-sufficient in CI/publish/release/docker/local) + reordered ci.yml (Build before Typecheck/Test) + moved @wairon/sdk to devDependencies (it's BUNDLED via tsup noExternal, so published @wairon/cli must not carry a `*` runtime dep). RELEASE WIRING: new reusable .github/workflows/publish-sdk.yml (provenance publish of sdk/, version set BEFORE build so baked SDK_VERSION = published version) + publish-sdk job in release.yml → a version tag now publishes BOTH @wairon/cli AND @wairon/sdk in lockstep. CADENCE: every dev push auto-tags -dev.N (auto-tag-dev.yml, updates WAIRON_VERSION in src/config/defaults.ts) → release.yml publishes to the `dev` dist-tag. dev.3 release failed (pre-fix); dev.4 = cli-only (pre-sdk-wiring); dev.5 (from 3316b6a) = FIRST dual publish. CAVEAT: the FIRST @wairon/sdk publish needs an NPM_TOKEN that can CREATE @wairon/sdk (Automation or granular @wairon/* create scope) — a @wairon/cli-scoped token can't create it.
- [x] LIVE HOSTED E2E 2026-07-21 (docker-compose.local.yml on localhost:8080): login → `POST /web/admin/packs/upload` (raw application/zip + X-Wairon-Pack-Name) installed a declarative .wpack (HTTP 200, descriptor tier:instance) → listed in GET /web/admin/packs → a CODE .wpack was refused HTTP 400 ("only declarative packs install over this surface") BEFORE extraction. The test CAUGHT + FIXED a real distribution bug: tsup externalized `@wairon/sdk` (unpublished workspace) so the built CLI `require('@wairon/sdk')`'d at runtime and the container crash-looped `MODULE_NOT_FOUND`; the Dockerfile also never copied sdk/. FIX (build-only, no spec impact): tsup.config.ts `noExternal:['@wairon/sdk','fflate']` (bundle both into dist; js-yaml stays external = a real runtime dep) + Dockerfile copies sdk/package.json before `npm ci` and `sdk/` source, and runs `npm --prefix sdk run build` before the root build. Verified dist/cli/index.js has no @wairon/sdk/fflate require; container boots + serves. GOTCHA for future workspace siblings: an unpublished workspace pkg MUST be tsup-noExternal'd (and its non-root deps bundle transitively) or the shipped image can't resolve it.
- [x] IMPLEMENTED + verified 2026-07-21 (post-lock, 4 implementer agents + web done directly): `@wairon/sdk` workspace package (sdk/src/{index,orchestrator,codec,scaffold,archive,types,authoring,version}.ts; fflate + js-yaml + node crypto; whole-buffer not streams; central-dir symlink scan supplements fflate; 32 tests) + sdd_host (adapters.ts host_sdk_adapter, packs.ts registry+orchestrator archive installs w/ HOSTED_STRICT 8MiB total/4MiB entry, projectops.ts forwarders, web.ts /web/{admin,projects}/packs/upload routes + http.ts readRawBody reusing the 8MiB MAX_BODY cap + CSRF mutation-gating; 13 new + 82 regression) + sdd_cli (commands/packs.ts init/build/ZIP-aware add via staging-dir→rename, cli/index.ts `wairon pack` group + `packs` deprecated-alias w/ warn; 4 new + 18 regression; live CLI e2e init→build→add→list) + React web file-picker (api.ts postBinary; Instance/ProjectOps → /upload w/ optional X-Wairon-Pack-Name). Spec RECONCILED to code (integrity digests exclude the envelope file; inspectArchive does NOT verify identity per its L5 — identity is a build-time check; planExtraction skips dir entries) + 2 documented `lint.allow UNREALIZED_DEPENDENCY` on cli_packs_adapter_impl/host_sdk_adapter_impl (cross-package `@wairon/sdk` import — the Level-2 analyzer resolves only relative specifiers, so a workspace-package import reads as unrealized). GOTCHA: `@wairon/sdk` package name ≠ any sourcePath, so cross-package edges always need this allow. FULL VERIFY GREEN: root typecheck + 1212 tests + build; sdk build + 32 tests; web typecheck + rebuilt/re-embedded dist/webapp.html; `validate --ci` = all checks passed. NEXT: post-lock spec edits re-dirty the statehash → human re-runs `wairon lock`; then wire `@wairon/sdk` publish to the release (currently version 0.0.0-dev, workspace dep `"*"`).
- [x] DESIGN COMPLETE — validated recursively with ZERO errors (2026-07-21). L1 sdd_sdk (5 components) + L3 interfaces (+ types) + L4/L5 narratives all authored and approved layer-by-layer. Consumer deltas authored: sdd_cli (cli_packs_adapter +initPack/buildPack + ZIP-aware add + sdk_portal edge; cli_runner +runPack, packs=deprecated alias), sdd_host (NEW host_sdk_adapter→sdk_portal; pack_registry/pack_orchestrator/project_ops +installGlobalPackArchive/installProjectPackArchive additive; web_portal +adminInstallGlobalPackArchive/projectPackInstallArchive on POST /web/{admin,projects}/packs/upload, raw application/zip + X-Wairon-Pack-Name header, name-from-envelope). sdd_core loader UNCHANGED (extraction is install-time → .wai/packs/<name>/ = a directory pack the loader already reads). Legacy YAML-text install kept, deprecated. Integrity enforced pre-write in extractPack (verifyIntegrity). Resolved decisions: bytes (not Uint8Array) is the builtin binary token; sdk_portal endpoints are Custom (@wairon/sdk#method); admin API/host-CLI left untouched (possible future obsolete per Robbe). Remaining warnings = draft-status + MISSING_SOURCE_FILE/UNREALIZED_METHOD (code not written yet, implementer's todo). NEXT: human `wairon lock` → spawn generated implementer agents (sdk/src/{index,orchestrator,codec,scaffold,archive}.ts is the new @wairon/sdk package; packs.ts/projectops.ts/web.ts/packs command deltas). Open for impl: pick the zip lib (proposed fflate), wire @wairon/sdk package build + publish, React file-picker replacing the YAML textarea.

## Hosted pack/policy/profile/spec-UI gaps (ACTIVE) — decisions from Robbe 2026-07-22
Release found hosted (`sdd_host`) gaps. Staged: **A** packs-apply fix (ship first) → **B** structured instance-policy UI + pack-contents introspection → **C** project pack adoption from catalog → **D** profile-apply-INTO-spec → **E** first-class subprojects + scoped tokens (design-first, parked) → **F** full project spec value-editor (values-only, selects-over-freetext) → **G** canvas interactivity + floating-header overflow bug → **H** external links (L4, companion to sourcePath, pairs conformance:off). Decisions: reconcile **option A** (explicit reconcile applies resolvable required/default packs regardless of enforcementMode); profiles applied INTO the spec; subprojects = first-class eventual but remodel parent/sibling-as-external-reachable-interface first (parent-as-unit; scope-aware list-external-interfaces surfaces parent+siblings; warn when scoped into a subproject that parent/sibling relations can't be verified).

### Stage A — packs actually apply + honest reconcile (SPEC DONE, validates; awaiting human lock)
Root cause (4-agent diagnosis, all `src/server/policy.ts`): policy applier `readGlobalPackContent` read ONLY the instance tier (image-tier baked packs never resolved = packs never applied), rejected directory-form packs (`.wpack` extracts), matched file-stem vs manifest-name (the `appender`/`appender-make` case → reconcile never converged, re-installed forever), silently skipped unresolved (`installPacks` `if(content)` no-else — baked into the L5 narratives), reconcile gated to `auto_reconcile` only (default warn → no-op, no feedback).
- [x] Spec revision authored + validated (sdd_validate_tree sdd_host = **0 errors**; 5 warnings = expected code-not-written: UNREALIZED_METHOD ×1 + CALL_STEP_UNREALIZED ×4): new value-objects `resolved_global_pack` + `pack_resolution`; `+unresolvedPacks` on `policy_evaluation_result`; `enforcementMode` documented closed-set (set-time validation); `ipack_registry` `+executeApprovedResolveGlobalPacks` (pre-authorized batch resolver — both tiers, file+dir form, stem-OR-manifest match, canonical name + serialized content, returns {resolved,unresolved}); `pack_registry_impl` intent method; `iproject_policy_orchestrator.reconcileProjectPolicy` desc → option A; L5 rewrites route pack resolution through the registry seam (executeApprovedInit/initializeProjectWithProfile/evaluateProjectPolicy/reconcileProjectPolicy), reconcile drops the auto_reconcile gate + surfaces unresolvedPacks, `setPackPolicy` gains an enforcementMode guard.
- [x] Human locked; **IMPLEMENTED + verified** (this session): `packs.ts` `executeApprovedResolveGlobalPacks` (indexes both tiers — instance wins collision — file+directory-form, stem/basename/manifest match → canonical name + serialized content + `{resolved, unresolved}`, dedup by canonical); `policy.ts` retires `readGlobalPackContent`/`installPacks`, routes init/evaluate/reconcile through the registry seam, compliance compares by CANONICAL name (fixes appender↔appender-make convergence), explicit reconcile applies regardless of mode (option A), surfaces `unresolvedPacks`, `setPackPolicy` validates `enforcementMode`; `types.ts` `+unresolvedPacks` + `ResolvedGlobalPack`/`PackResolution`; dropped the now-unused `project_policy_orchestrator → host_core_adapter` dependsOn (resolution moved to pack_registry). Verify: typecheck + **1216 tests** (5 new/rewritten: image-tier resolution, honest unresolved reporting, stem≠manifest no-reinstall-loop, reconcile-regardless-of-mode, mode guard) + build green; `sdd_validate_tree sdd_host` **0 errors / 0 warnings**. Checkpoints: aa07b66 (canvas header fix), 127c9c7 (specs), + impl commit. SHIPPED — merged to dev as v5.0.2-dev.1.

### Stage B+C — structured pack/policy UI + pack-contents introspection + adopt-from-catalog (SPEC DONE, validates; branch feat/hosted-pack-mgmt-ui)
Instance-policy config is free-text CSV and the per-project packs page only offers ZIP upload, because the backend exposes only counts and no adopt-by-name / profile catalog. Grouped B+C on one branch (shared catalog/introspection backend).
- [x] Spec authored + validated (sdd_validate_tree sdd_host = **0 errors**; 11 warnings = expected code-not-written UNREALIZED_METHOD): types — `pack_descriptor` +profileIds/languageIds/ruleIds (probe already loads these; enrichment is free), new `available_profile` {id, source (builtin|packName), family?}; `ihost_core_adapter` +`builtinProfileIds()`; `ipack_registry` +`storeListAvailableProfiles()` (builtin via adapter ∪ pack profiles, source-tagged, deduped); `ipack_orchestrator` +`listAvailableProfiles` (auth+forward), +`listAdoptableProjectPacks` (project:read → server-global catalog), +`adoptProjectPack` (project:admin → resolve via Stage-A seam + vendor by name, reject unknown); `iproject_ops_orchestrator` +3 forwarders; `iweb_portal` +3 routes (GET /web/admin/profiles, GET /web/projects/packs/adoptable, POST /web/projects/packs/adopt) w/ endpoints + calls-only symbols (opsListAvailableProfiles/opsListAdoptableProjectPacks/opsAdoptProjectPack). NO new component dependsOn edges (aggregation kept in pack_registry which already deps host_core_adapter).
- [x] IMPLEMENTED + verified (2 parallel agents; commits 5e7d285 spec + dca31e3 impl) — root+web typecheck, 1227 tests (4 new), build+embed, sdd_validate 0/0, and a LIVE HTTP smoke on a real booted server: 5/5 PASS (profiles source-tagged builtin+pack, pack profileIds/languageIds enrichment, adoptable catalog, adopt-by-name + negative-CSRF 403 gate + appears-installed, policy profile-id round-trip). Merged to dev. **Backend:** adapters.ts builtinProfileIds; packs.ts probe→profileIds/languageIds/ruleIds + storeListAvailableProfiles + listAvailableProfiles/listAdoptableProjectPacks/adoptProjectPack; projectops.ts 3 forwarders; web.ts opsListAvailableProfiles/opsListAdoptableProjectPacks/opsAdoptProjectPack handlers + dispatch; http.ts route+CSRF registration. **Frontend (web/):** types.ts mirrors (PackDescriptor +ids, AvailableProfile); api.ts; Instance.tsx PolicyForm → multiselects (packs from listGlobalPacks, profiles from /web/admin/profiles grouped by source) + reframed sections + GlobalPacksTab contents; ProjectOps.tsx PacksTab → "Add from available" dropdown (adoptable minus installed), ZIP demoted. **Tests:** profile aggregation, adopt-by-name (+reject unknown), adoptable listing, descriptor enrichment.

## Multi-spec share / artifact / CLI — portal-auth epic close-out (2026-07-24)
**Regression introduced by the portal-auth epic itself.** Before it, `exportSurface` ALWAYS populated `rendered` (one combined OpenAPI doc); after per-portal specs, `rendered` is set only when exactly ONE portal exists. Three consumers still read `result.rendered` and silently degrade: share-snapshot capture (`?? '{}'` → a multi-portal shared link renders **Swagger UI over an empty spec** — the user-visible bug), landscape artifact download (`?? '{}'`), and CLI export (prints nothing; `--out` writes only the FIRST spec). The web UI was already converted in Phase 1-2 (`/web/openapi?spec=`); these three were the deferred fast-follow.
Decisions (Robbe): generate multiple specs and serve them **separately, never merged**; ONE share link serves each of its APIs via `?spec=<portalId>` with an index page when several. Share scope stays **full-project** — `buildCanvasDataModel()` takes no scope and the OpenAPI is captured at the `project` ceiling precisely so the APIs match the shared canvas; share viewers are anonymous token holders, so there is no per-viewer filtering (the decision is made at link creation + the `allowDownloadOpenapi` toggle).
- [x] SPEC AUTHORED + validates **0 errors / 0 warnings**: type `share_snapshot` **+openapiSet** (`NamedOpenApiSpec[]`), `openapi` RETAINED (single-portal captures; pre-existing share links keep serving unchanged). Optional trailing `portalId` (structured `optional: true`) with uniform three-way semantics — *select one / index when several / that one when single, never merged* — on `ishare_portal.getSharedOpenApi` (the Swagger UI page) + `.downloadSharedArtifact`, `ishare_access_orchestrator.downloadArtifact`, `ilandscape_portal` + `isurface_exchange_orchestrator.exportProjectSurface`, `isurface_orchestrator` + `icli_surfaces_client_adapter.exportSurface`, `icli_runner.runSurface` (+`--portal <id>`). L5 rewritten: capture the WHOLE set (never `'{}'`); selection/index serving; multi-portal `--out` writes **ONE FILE PER PORTAL** (out stem suffixed with each portalId); a `portalId` naming no portal is refused, not substituted.
- [x] Human `wairon lock` → implemented + verified → SHIPPED to dev (merge 04165da feat/multi-spec-share-cli + spec-debt reconcile merge 93f2018; dev green: dogfood validate --ci 0/0, ~1286 tests)

## Track G — canvas org-overview deep-expand + interactivity close-out (ACTIVE) — decisions from Robbe 2026-07-24
Scope pinned with Robbe: **G = unit deep-expand + relation-edge retarget**; the floating-header overflow bug is ALREADY FIXED (aa07b66, 2026-07-22 — collapse loop measured the spacer's pinned right edge, so every width read as overflow) → verify-only; WS realtime believed fixed → verify-only (specifically: a received data-plane message must actually update the mounted canvas, not just arrive); inline spec drill-inside REJECTED (never asked for — the unit→project boundary stays a hard navigation). Branch: feat/canvas-unit-deep-expand.
**Deep-expand decision**: internals expansion recurses FOR UNITS ONLY — expanding a unit walks all nested unit layers down to the FIRST projects reached; units render as boundary boxes, projects as collapsed leaf tiles, cross-project relations as direct leaf-to-leaf edges across boundaries. Works at instance root (full org overview) and any drilled unit scope. Frontend-only mechanics: the environment canvas already fetches the ENTIRE permission-filtered landscape once (`/web/graph?tier=landscape&level=1`); "one layer" was purely a client rendering rule (`innerLayout` emits direct children, never recurses). Design: opt-in `deepInternals: true` on subsystem model records (Environment.tsx stamps unit frames; per-project canvas never sets it ⇒ untouched), recursive innerLayout w/ variable tile sizes, ports only at the outermost container, depth cap 6. Engine edits in src/core/canvas.ts (SOURCE OF TRUTH) + `npm run gen:canvas` (drift test enforces).
**Data gap found + fixed**: relations targeting a PUBLISHED INTERFACE vanished at level 1 — the reshape dropped interface nodes (level 2) and every edge touching them, so precisely the well-maintained relations (snapshot present) were missing; only snapshot-less fallback project→project edges survived.
- [x] Spec: L5 honesty edit on `web_graph_orchestrator_impl.getGraph` step 4 (relation edges whose publishedInterface endpoint is dropped by the detail cutoff RETARGET to the owning project node — one edge per relation, keeping edgeKind/relationId/label; other edges to dropped nodes still drop). sdd_validate_tree **0 errors / 0 warnings**. Re-dirties statehash → re-run `wairon lock` when convenient.
- [x] Backend: `reshapeLandscapeGraph` (src/server/web.ts) retargets per the narrative + regression test (2-project relation w/ snapshot: level 2 ends on iface node, level 1 becomes project→project with relationId; publishes edges stay dropped). web.test.ts 69/69 green.
- [x] Engine deep-expand implemented (df3f9e4): recursive compounds w/ variable sizes, direct cross-boundary relation edges (aggregate suppression), port lift at outermost box, depth cap 6, no-flag path unchanged; Environment.tsx flags unit frames; canvas.test.ts gained an EXECUTED-ENGINE harness (runs the real engine body under stubbed DOM+cytoscape). **Robbe eyeballed 2026-07-24: deep-expand + header OK.**
- [x] WS realtime live-check FAILED (Robbe): remount threw `attachShadow` NotSupportedError — attachShadow is once-per-element + destroy() cleared host light DOM (empty in shadow mode) ⇒ live updates were broken on BOTH canvases since the realtime wiring shipped. FIXED (b79e044): mountCanvas reuses host.shadowRoot + clears rootEl (idempotent); destroy clears rootEl; source-level lock test pins the contract (generator template in scripts/gen-canvas-engine.mjs).
- [x] Header compaction — Robbe's follow-up request, shipped (1356400): overflow system generalized into ordered reusable STAGES (trailing folds → View folds → search→🔍+floating panel w/ query badge + focus preservation → mode tabs→current-mode dropdown → ancestor crumbs→ordered "…" dropdown as a renderCrumbs RENDER MODE); crumb renders nudge reflow (edge-anchored #hdr never resizes from content); oscillation-free (restore-all reverse + strictly-forward walk, inReflow suppression); full width unchanged. 1296 tests, validate --ci green.
- [x] Final round: Robbe verified (WS live update + selection survival via 45fb0ba, compaction, deep-expand) → **MERGED to dev e1b5746 2026-07-24**; `wairon lock` re-run + `/mcp reconnect` done. Round-2 additions during verification: attachShadow remount fix (b79e044 — realtime was truly broken), staged header compaction (1356400 — Robbe's follow-up), selection restore (45fb0ba).

## Track E — subproject external model + scoped tokens (ACTIVE) — decisions from Robbe 2026-07-24
Stage-plan audit: **F found ALREADY IMPLEMENTED** (web/src/views/SpecsEditor.tsx = the values-only selects-over-freetext editor); **D still open** (only init-time profileSelection recording exists — no apply-INTO-spec verb; queued after E). **E decisions (design session over a verified current-state map):**
1. **Exchange model = snapshot-primary hybrid**: the parent generates BOTH its family surface (exists: generateChildSnapshots) AND each SIBLING's published surface into every direct child (origin 'generated', parent stateId provenance); NEW scope-aware list-external-interfaces (MCP tool + CLI) enumerating vendored surfaces w/ origin + freshness; `mcp serve` announces the chaining context at bind (today: nothing does — findChainingParent surfaces only via validate); freshness checked LIVE against the parent root when reachable (checkChildSurfaceFreshness machinery), vendored snapshots stay the source of truth (portable standalone CI).
2. **Strictness = precise warn, reference family only**: cross-tree refs COVERED by a vendored surface validate fully (real errors enforceable — contract mismatch, non-Adapter caller); UNCOVERED refs emit one precise ci-waivable code (UNVERIFIED_EXTERNAL_REF) REPLACING the reference-family blanket downgrades (validation.ts SUBPROJECT_LENIENT_CODES reference half); the code-conformance leniency family (sourcePaths etc.) stays unchanged.
3. **Scope = remodel + hosted scoped tokens in ONE epic**: hosted tokens can scope INTO a subproject of a project; data plane binds the child root (containment holds — child lives inside the project root, so findChainingParent finds the parent INSIDE dataDir and the warn model applies naturally); design refinement to settle at the sdd_host layer: token-claim NARROWING (subproject-qualified entries in ApiKeyRecord.projects, resolver untouched) vs a new ScopeKind level.
Branch: feat/subproject-external-model. Affected subsystems: sdd_surfaces (sibling projection + discovery), sdd_core (lock-time snapshot regeneration), sdd_validator (UNVERIFIED_EXTERNAL_REF remodel), sdd_mcp (tool + bind context), sdd_cli (surface externals cmd), sdd_host (subproject-scoped tokens + bind). SDD flow: layer-by-layer approval → validate 0/0 → human lock → implement via subagents (fitting models).
- [x] DESIGN COMPLETE (2026-07-24, all 5 gates Robbe-approved, tree validates **0 errors**; warnings = code-not-written implementer todo): **G1 sdd_surfaces** — ExternalSurfaceEntry/ChainingParentRef types, projectSubsystemSurface (sibling published-Portal snapshots, '<system>::<subsystemId>' keying), generateChildSnapshots delivers family+siblings per child, listExternalInterfaces discovery w/ freshness (vendored stateId vs parent's CURRENT hash). **G2 sdd_core** — findChainingParent (spec store walk, full narrative), resolveChainingParent + computeStateIdAt (read-only cross-root hash) on orchestrator+portal; ChainingParentRef ownership moved to sdd_core. **G2b sdd_cli** — the previously UNMODELED local lock flow: NEW cli_lock_adapter (lockTree→promoteAllComplete+writeLockRecord, symbol runLock), validateAsComplete on validator adapter, icli_runner.runLock (as-complete gate → freeze → regenerate child+sibling snapshots when chained children exist), LockOptions typed seam, deliberate-hub lint.allow (GOD_COMPONENT/EXCESSIVE_DEPENDENCIES) on the runner. **G3 sdd_validator** — validateSddTree step 19: snapshot-covered cross-tree refs validate FULL strength; uncovered → one ci-waivable UNVERIFIED_EXTERNAL_REF (reference family only; conformance family keeps crossTreeContext downgrade); notice counts unverified refs. **G4 sdd_mcp+cli** — NEW mcp_surfaces_adapter, sdd_list_external_interfaces tool (portal→orchestrator→adapter), createMcpServer bind-time chaining announcement, `wairon surface externals`. **G5 sdd_host** — token-claim narrowing w/ subproject-qualified entries 'projectId::subsystemId' (nested compose; NEVER a grant refinement): api_key_record/principal fields, resolveRoot binds the mounted child root (containment-checked, unknown/non-chained mount rejected), mint-time validation (mintToken + mintSelfToken), handleRequest binds the child root (UNVERIFIED_EXTERNAL_REF model applies naturally — findChainingParent finds the parent inside dataDir) + audit stamps top project id + bound qualifier. Design fixes en route: UNTYPED_SEAM (LockOptions), UNNARRATED_COMPLEXITY (findChainingParent full narrative).
- [x] Human locked; **IMPLEMENTED (3 subagent batches) + verified 2026-07-24** (commits 79bc19e impl + 009bb9e reconcile): typecheck clean, full suite **1341/1341**, dogfood `validate --ci` exit 0 (all 15 UNREALIZED cleared), sdd_validate_tree 0/0. Reconcile items: `resolveBinding` modeled as the data-plane seam (ProjectBinding type, project_index full narrative per the complexity lint, symbol resolveProjectBinding), `ihost_core_adapter.resolveContainedProjectPath` (projects.ts routed off core modules), handleRequest step-6 retarget; git-backed lock test got a per-test timeout (full-suite contention flake). Deviations logged: sibling snapshot filenames sanitized `::`→`--` (Windows), audit qualifier in metadata (event-shape convention), generateChildSnapshots family projection hoisted (existing allow). **OPEN design flags for Robbe:** (a) projectSubsystemSurface projects published PORTALS only — a published Gateway would be excluded; (b) execute-primary lifecycle tools (e.g. lock) on a subproject-narrowed token still act on the TOP project (specs don't restrict; possible follow-up tightening). Statehash re-dirtied by the reconcile → re-run `wairon lock`.
- [x] Robbe verified (Tokens UI qualifier mint + CLI/MCP checks on the redeployed stack) → **MERGED to dev 0cc5315 2026-07-25**; `wairon lock` re-run.
- [x] **Both open E flags DECIDED by Robbe 2026-07-25** (own follow-up branch, after D): **(a) sibling projection broadens** from Portal-only to ANY legally published component — Portal | Gateway | Observer, exactly what the boundary rules already sanction as a published surface (`stereotype-deps.ts:134` allows Portal|Gateway for cross-subsystem deps; `public-surface.ts` allows Observer for MessageBus) — plus a NOTICE when an entry is still skipped (unbound/missing), so a child is never silently short a sibling surface. Today `projectSubsystemSurface` (`src/core/surfaces.ts:217`) hard-skips non-Portals, so a Gateway-published subsystem is legitimately published in-project yet permanently invisible to chained children (its refs stay UNVERIFIED_EXTERNAL_REF). **(b) execute-primary lifecycle SPLITS** on a subproject-narrowed token: TREE operations (`sdd_host_lock_project`, `sdd_host_promote_project`) act on the BOUND CHILD root (promote + statehash + lock.json inside the subproject — the loop a narrowed agent exists for), while RECORD-level tools (`sdd_host_initialize_project`, approval status/await, and the project-ops pack/policy/produce/commit set) are REFUSED on a qualified token with a clear message. Today all of them take the TOP project id (`src/server/request.ts:151`), so a token narrowed to `p::sub` can lock/commit the whole parent while being unable to lock its own subtree.

## Track D — profile-apply-INTO-spec (ACTIVE) — decisions from Robbe 2026-07-25
Robbe's framing: D largely shipped WITH the project spec-editor feature; what remains is **"ensure the selected profile's rules actually apply to the project, even if the profile comes from a pack, and that it applies to the entire project."** So this track was scoped from a verified current-state trace, not a fresh design.
**Works today** (shipped with the spec editor / Stage F): `SpecsEditor.tsx` `ProjectConfigPanel` offers a grouped profile picker fed by `/web/admin/profiles`, POSTs `/web/projects/config` → `setProjectType` → `project.yaml#projectType`; validation passes `projectType` + `rules` in (`mcp/server.ts:994`, `adapters.ts:70`) and `getComponentProfile` (`rules/index.ts:377`) resolves every component to it. So **built-in** profiles genuinely govern.
**Four verified gaps, all on the from-a-pack path:** (1) `storeListAvailableProfiles` (`packs.ts:203`) scans only the SERVER-GLOBAL tiers and `setProjectType` writes the id blindly → picking a pack profile whose pack isn't installed in that project leaves `ctx.ext.profiles[id]` undefined ⇒ UNKNOWN_PROFILE, `family:'neutral'`, and the profile's whole `rules` block (designDepth, severities, naming, complexity) + stereotype doctrine silently DON'T apply; conversely a pack uploaded straight into a project has profiles the dropdown never shows. (2) `performInit` (`policy.ts:589`) records `profileSelection` but never writes `projectType` → a policy-REQUIRED profile doesn't govern until a human hand-picks it. (3) `ProjectConfigSchema` doesn't model `profileSelection` while `storeInstallProjectPack` round-trips through `saveProjectConfig(parsed)` (`packs.ts:360`) → any later pack install/adopt/remove SILENTLY DELETES the recorded selection (the Phase-3 tech debt, now load-bearing). (4) `reconcileProjectPolicy` applies packs but never the profile → drift invisible and unrepairable.
**Approved decisions**: auto-adopt (a chosen profile's providing pack is vendored into the project, then the profile is written — one click, and the pack is committed with the project so CI/clones enforce it too); multi-id selection applies the FIRST resolvable id and REPORTS the remainder (projectType takes exactly one profile; the rest belong on subsystems — never silently dropped, never distributed by guesswork).
- [x] **DESIGN COMPLETE 2026-07-25 — validates recursively 0 errors** (10 warnings = UNREALIZED_METHOD ×5 + CALL_STEP_UNREALIZED ×5, the implementer's todo). No new components, no new dependency edges (`project_policy_orchestrator` already deps `pack_registry` + `host_core_adapter`). **Types**: `available_profile` +`installed?`; NEW `profile_application` {profileId, source, adoptedPackName?}; `project_config` +`projectType`; `project_config_view` +`profileSource?`/`profileResolvable`/`adoptedPackName?`/`unappliedProfileIds?`/`overridingSubsystemIds?`; `policy_evaluation_result` +`governingProfileId?`/`unappliedProfileIds`. **Contracts**: `ipack_registry` +`executeApprovedListProjectProfiles` (PROJECT-scoped catalog: built-ins ∪ the project's OWN registered packs ∪ server-global, `installed`-tagged, project tier winning) +`executeApprovedEnsureProfileInstalled` (pass-through | vendor the providing pack | THROW on an unresolvable id — a dead profile is never written; idempotent); `ipack_orchestrator` +`listProjectProfiles` (project:read); `iproject_ops_orchestrator` +forwarder; `iweb_portal` +GET `/web/projects/profiles` (symbol `opsListProjectProfiles`). **L5 rewrites on `project_policy_orchestrator_impl`** (no new interface methods): `setProjectType` = ensure-then-write + fold the applied id to the FRONT of `profileSelection` (stamped from the caller) + enriched view; `getProjectConfig` = classify source/resolvability against the project catalog + report overriding subsystems, never repair; `initializeProjectWithProfile` + `executeApprovedInit` = apply the recorded selection as `projectType` (catalog → choose first qualifying → ensure → write; an unresolvable selection leaves the default and is audited, never fails creation); `evaluateProjectPolicy` = report `governingProfileId`/`unappliedProfileIds`; `reconcileProjectPolicy` = repair ONLY when policy requires a profile that isn't governing or the recorded `projectType` resolves to nothing — a deliberate, resolvable, compliant choice is reported and left alone. GOTCHA caught during authoring: inserted-branch `onFalseStep` values are relocated by LATER inserts in the same delta (and step 11's existing onFalseStep skipped the new repair region) — 4 jump fields had to be retargeted; the insert NOTICEs are what surfaced it.
- [ ] Human `wairon lock` → implement via subagents (packs.ts registry+orchestrator, policy.ts 6 narratives, projectops.ts forwarder, web.ts route, models/project.ts schema fix, SpecsEditor.tsx picker) → verify + live e2e (pack profile picked → pack auto-adopted → doctrine actually enforced)
- [x] **IMPLEMENTED + verified 2026-07-25** (3 subagent batches + 2 fixes by hand; commits 5cdd88b spec, 991a167 impl, 75b1364 acceptance test, a7c215e convergence fix): root+web typecheck clean, **1362 tests** (+21), `npm run build:web` → `npm run build` (ordered, SPA re-embedded), dogfood `validate --ci` exit 0, `sdd_validate_tree` **0 errors / 0 warnings** — every UNREALIZED_METHOD + CALL_STEP_UNREALIZED cleared, so the conformance gate itself confirms the code matches the narratives incl. call steps. **Code**: `packs.ts` (project-scoped catalog + ensure seam sharing ONE two-tier scan with the instance-wide catalog, whose shape is unchanged; `storeListProjectProfiles` sealed like its `storeListProjectPacks` sibling), `policy.ts` (6 methods; shared `classifyProfile`/`firstApplicableProfileId`/`unappliedIds`/`overridingSubsystemIds`/`foldAppliedProfile` helpers), `projectops.ts`+`web.ts` (`GET /web/projects/profiles`, envelope `{profiles:[…]}` matching `/web/admin/profiles`; `http.ts` correctly untouched — a GET never joins the CSRF mutation list), `models/project.ts` (profileSelection modeled), `core/rules/types.ts` (`PROJECT_KINDS` exported once) + `adapters.ts` `builtinProjectKinds` behind a NEW `ihost_core_adapter` method (the honest alternative to restating core's kind list inside the host, where it would drift), web `SpecsEditor` picker. **Deliberate calls kept**: the raw project.yaml read/merge/write helpers STAY (the narratives say raw read-merge-write; the schema fix addressed data loss, not the way one key is touched). **2 findings fixed beyond the spec pass**: (1) NON-CONVERGENCE — compliance reads the recorded selection while a repair wrote only projectType, so a policy that STARTS requiring a profile after a project exists left evaluate reporting "not selected" forever (same shape as the pack-name canonicalization loop); reconcile now folds the repair into the selection via the shared helper (+1 narrative step, regression test asserts one-pass convergence). (2) The suite proved every STEP of the chain but never that the profile GOVERNS — added `tests/server/profile-governs.test.ts`, which drives the hosted write path against a server-global-only pack whose profile forbids Actor and then reads the VALIDATOR's verdict through the hosted read path: before = no finding, after = PROFILE_FORBIDDEN_STEREOTYPE on the seeded component AND no UNKNOWN_PROFILE (enforced for the right reason). GOTCHAS recorded: inserted-branch `onFalseStep` values are relocated by LATER inserts in the same delta (4 jump fields needed retargeting — the insert NOTICEs are what surfaced it); `sdd_write_narrative` REPLACES a whole L4 spec and resets status to draft, so granular narrative edits must go through `sdd_update_spec`.
- [ ] Human re-run `wairon lock` (the reconcile narrative step re-dirtied the statehash) + eyeball the UI on the local stack → merge --no-ff to dev

## Track D2 — profiles for subprojects vs internal subsystems (NEXT) — decisions from Robbe 2026-07-25
Robbe's distinction: **a project type belongs to a PROJECT**, so it may vary per subsystem only when that subsystem IS a project — a chained SUBPROJECT with its own `.wai/project.yaml`. An internal subsystem lives in the same system/project and therefore has no project type of its own; its `profile` override is a profile, not a project type, and must resolve against THAT ONE project's packs.
Verified fact that makes this load-bearing: a parent-scoped validation runs `scanAllSpecs({recursive})` into ONE tree with a SINGLE `projectType`, and `loadProjectExtensions()` reads only the current project's config — so a chained child's own projectType and its own installed packs are consulted **only when the child is validated standalone**. In a parent run a child's components resolve `subsystem.profile ?? parent projectType`, and a child-pack profile named there cannot load (UNKNOWN_PROFILE).
Approved decisions: **(1) internal subsystems** — the subsystem profile picker adopts the contributing pack INTO THE CONTAINING PROJECT on save, composing two verbs that already exist (`POST /web/projects/packs/adopt` from Stage C, then the subsystem spec write): frontend-only, no new backend surface, no spec change. **(2) subprojects** — make the child's own project type editable against the CHILD root (Track E's contained child-root binding is the machinery) AND add a validator honesty lint when the parent's view of that subtree disagrees with the child's own projectType; single-projectType runs stay, the disagreement becomes visible instead of being silently resolved one way. Needs its own spec pass (a hosted verb variant + a new rule code) → own lock cycle. **(3) scope** — a follow-up track AFTER D merges, not folded into it.
Deliberately NOT taken (costed, available later): making the validator per-subtree config-aware (a parent run loading each subproject's own projectType + packs and applying them to that subtree). It is the doctrinally correct fix but `RuleContext` carries one `projectType` and one merged profile map, so it touches `getComponentProfile`, per-rule severity resolution, designDepth resolution and extension loading — a track of its own.
**D2 refinement (Robbe 2026-07-25, second pass) — INHERITANCE is the model, not per-subsystem project types:**
- **Internal subsystem**: needs NO project type — it inherits the project's automatically (today's `getComponentProfile` fallback). Adding a per-subsystem project-type control would be useless surface with failure modes, so D2 adds none. The existing optional `subsystem.profile` field STAYS as a deliberate doctrine override (approved): it is how a `fullstack` project fences its frontend subsystems as frontend-reactive while backend ones stay backend — the profiles rule's family split depends on it. Nothing is deprecated; the redundant concept simply is not introduced.
- **Subproject**: it IS a root/standalone project, so it HAS a projectType — but it must **keep inheriting until explicitly told otherwise**, so extracting a subsystem into a subproject never silently changes its doctrine. Mechanism (approved, BOTH forms): an explicit `projectType: inherit` value, AND an OMITTED projectType in a chained child also inherits. Resolution walks to the chaining parent (Track E's `findChainingParent`) and takes the parent's EFFECTIVE type, recursively, falling back to 'backend' at an unparented root — so it resolves in a standalone child run as well as in a parent-scoped one. Externalizing a subsystem writes `inherit`, making "still auto inherits until specified" true by construction.
- **ACCEPTED CONSEQUENCE of the omitted-means-inherit half**: `ProjectConfigSchema.projectType` is `z.string().default('backend')`, so an omitted field is INDISTINGUISHABLE from an explicit 'backend' after parsing, and existing chained children that omit it silently run 'backend' today. Making omitted mean inherit therefore (a) requires resolution to consult the RAW project.yaml for key presence (or the schema to go `.optional()` with an explicit resolver at every use — the wider blast radius) and (b) CHANGES BEHAVIOUR for existing chained children whose parent is not 'backend'. Robbe accepted this deliberately (it is the bug: a child quietly running 'backend' under, say, a fullstack parent) — implementation must carry a release note.
- Design consequence worth keeping: with `inherit` as the extraction default, a parent-scoped run is ALREADY correct for the common case, so the approved divergence lint only ever fires when a child DELIBERATELY sets a different project type — which keeps the costed-but-not-taken per-subtree validator work genuinely optional rather than routinely needed.
- [x] **Robbe verified the UI 2026-07-25** ("this seems to work") on the local dev stack; `wairon lock` re-run and CONFIRMED fresh (lock digest == current computeStateId). Two follow-on pieces shipped in the same branch after the verification pass:
  - **Manual test kit** (commits 160d914, 1518c78): `dev/packs/` two declarative demo packs mounted read-only into the container's IMMUTABLE image tier by docker-compose.local.yml (present on boot, adoptable to every project, not removable at runtime; the mutable instance tier stays free for API installs) + `dev/demo-specs/` seed tree + `dev/packs/README.md` walkthrough documenting the ACTUAL captured output (same tree: 1 warning under `backend`, 2 errors + 4 warnings under `demo-strict`, 2 warnings under `demo-frontend`) + `tests/core/demo-packs.test.ts` pinning the documented doctrine so schema drift breaks the build instead of a manual session. Walkthrough uses its own project id (`packdemo`) after Robbe hit the trap of testing on a long-lived volume's pre-existing `demo` project — WITHOUT the seeded Actor neither stereotype rule can fire, which reads as "the profile isn't applying" when it is; seeding is now stated REQUIRED and a troubleshooting section orders the checks by likelihood. NOTE: no fixture leakage — `package.json.files` is `["dist","README.md"]` and the Dockerfile never copies `dev/`.
  - **Validation rail scoping + navigation** (commit 9c4268e, Robbe's request): scoped to the focused spec by default (a component scopes to its whole unit — component + interface(s) + implementation(s) from the level=4 graph), always showing `N on this <noun> · M elsewhere` with a show-all toggle that resets on selection change; a clickable chip per finding naming the affected spec (opens it through the tree picker's own URL-tracked selection; an unresolvable id renders non-interactive rather than guessing a kind); and field highlighting via a curated CODE_FIELD_MAP (description, componentType, durability, profile, publicInterfaces) since `ValidationIssue` carries no field path. DELIBERATELY DROPPED from the map: dependsOn (EXCESSIVE_DEPENDENCIES/GOD_COMPONENT), patterns (UNKNOWN_PATTERN_REF), sourcePath (MISSING_SOURCE_PATH et al) — this values-only editor has no control for those fields, so mapping them would point at nothing. Those findings are navigable but not fixable in the UI; making them fixable is a scope decision about what the editor may write, not a bug.
- [x] CHANGELOG entry added under `Unreleased (major — from v4.3.0)`, including the known subsystem-profile gap. Release path (from the CHANGELOG's own instruction): merge dev → main with `[major]` in the merge commit → **v5.0.0** (auto-tag.yml computes the stable version, release.yml publishes `latest` + docker.yml the image; `dev` pushes auto-tag `-dev.N` to the `dev` dist-tag).

## Track E flag close-out — subproject confinement + Gateway sibling projection (ACTIVE) — branch feat/subproject-confinement
Robbe's call after the D merge: fix the gaps a USER can walk into before the stable release; defer D2's subproject-inheritance model (narrower blast radius, needs its own gates + a documented behaviour change).
- [x] **Reachable half of D2 item 1 SHIPPED with D** (commit 119e5a0, on dev): the subsystem Profile picker labels adoptable pack profiles, adopts the contributing pack before writing the spec, refuses the save with an actionable message when adopting 403s (project:admin vs write), and refetches the catalog so a just-adopted profile stops advertising itself.
- [x] **DESIGN AUTHORED + validates 0 errors / 0 warnings** (2026-07-25, both Robbe decisions applied): **flag (b) lifecycle confinement** — `iproject_lifecycle_orchestrator.lockProject/promoteProject` + `iadmin_orchestrator.executeApprovedLock/executeApprovedPromote` gain an optional `subproject` qualifier binding the CHAINED CHILD's tree through `project_registry`'s containment-guarded qualified resolution (permissions + audit stay anchored at the TOP project id); `handleRequest` gains a pre-dispatch guard refusing the WHOLE record-level set (initialize, approval status/await, six project-ops) on a qualified binding as an isError result, while lock/promote are confined by forwarding instead. GIT half needed NO conditional: `readGitConfig` resolves from the BOUND root, so a child tree carries no binding and sync/publish no-op naturally — Robbe's "freeze the child, skip the publish" decision falls out of the design rather than being bolted on (documented as a consequence in the narrative). **flag (a) sibling projection** — `projectSubsystemSurface` broadens Portal-only → Portal | Gateway | Observer (exactly what `stereotype-deps`/`public-surface` already sanction as a cross-boundary target), and an entry that still cannot be projected (a `Custom` entry over a Specialist/Orchestrator) is REPORTED as a `[surfaces] skipped …` diagnostic following the existing `variants.ts:70` precedent — no new channel, no contract/return-type ripple; unbound + missing-component entries deliberately unreported (the validator already errors on those). Verified our own tree publishes nothing unprojectable, so the dogfood lock stays quiet.
- **HONESTY NOTE on the gate**: this delta is entirely descriptions, optional params and non-`call` steps, so `sdd_validate_tree` stays 0/0 whether or not the code actually threads the qualifier — structural conformance CANNOT catch a confinement mistake here. The TESTS are the only proof; the confinement tests must assert the negatives (a subproject lock leaves the PARENT tree unlocked and performs no commit/push on a git-backed project; each refused tool's underlying action did not happen).
- **STANDING GOTCHA (bit me twice this session)**: in ONE `sdd_update_spec` delta, an inserted step's own jump fields are relocated by LATER inserts in the same delta. Insert first, then retarget the jump fields in a SECOND call, and ALWAYS re-dump the flow afterwards. Here the confinement guard's `onFalseStep` drifted 12→13, which would have sent every request straight into the tool switch, bypassing the directly-dispatched-tool branch.
- [x] **IMPLEMENTED + verified + MERGED to dev 2026-07-25** (commit dfc9c12, merge da3d12e; Robbe locked at 17:33Z, lock CONFIRMED fresh): typecheck clean, **1387 tests** (+22), ordered build w/ SPA re-embed, `validate --ci` exit 0, tree 0/0. **Confinement**: `PROJECT_RECORD_TOOLS` (the 3 record tools + the 6 project-ops, spread from the existing constant so they cannot drift) refused on a qualified binding BEFORE the lifecycle dispatch and BEFORE the permission gate, as an isError result naming tool + qualifier, still audited through the same path; lock/promote confined by forwarding into `boundLifecycleRoot` → `resolveSubprojectMounts` (unresolvable qualifier throws — the silent parent fallback WAS the bug); git half no-ops naturally (bound-root config), no conditional. **APPROVAL path confined too** (the residual the implementer found and correctly refused to close unilaterally): the qualifier rides on the ApprovalRequest via the EXISTING payload/payloadType mechanism (`SubprojectScope`, exactly as project:init carries its input — Robbe's steer: reuse what's there, don't add model surface or refuse), is named in the summary so an approver sees the real scope, and is forwarded by BOTH decideRequest's auto-execute and the executeApprovedRequest retry; a payload that does not parse REFUSES rather than widening. **Projection**: `CROSS_BOUNDARY_TARGETS = {Portal, Gateway, Observer}` + `[surfaces] skipped …` diagnostic (variants.ts precedent); our own tree trips none, so the dogfood lock stays quiet.
- **ANTI-VACUITY (the standard this delta needed, since the gate is blind to it)**: every guard was verified to FAIL when removed. Breaking them exposed the REAL bypasses — initialize CREATED a project, pack_install VENDORED into the parent, approval_status/await DISCLOSED the whole request, pack_list disclosed installed packs, produce reached the producer, commit reached admin.commitProject. I held my own approval-path fix to the same bar (drop the forwarding → that exact test fails).
- **Corrected my own spec text**: I had written "the parent's next lock or scoped commit carries the frozen child tree" — FALSE, a chained child lives outside the `.wai/` pathspec. Fixed in spec + the comment it was copied into. Real limitation to remember: a subproject freeze reaches a remote only via a commit whose scope covers the child's path.
- Two of the nine were not truly bypassing (`policy_evaluate`/`_reconcile` already honoured narrowing, failing with a misleading `Unknown project`); the guard replaces that with a clear refusal — an improvement, not a closed hole.
- **RELEASE STATE**: dev = da3d12e (CI cutting v5.0.2-dev.16). main = v5.0.1. Next stable = merge dev → main with `[minor]` → **v5.1.0** (see the CHANGELOG's corrected release note). Still open by decision: **D2** (subproject `projectType` inheritance: `inherit` + omitted-in-child, divergence lint, accepted behaviour change needing a release note) and the CHANGELOG housekeeping of splitting the accumulated Unreleased section into `## v5.0.0` / `## v5.0.1`.

## Dynamic agent briefs over MCP (ACTIVE) — decisions from Robbe 2026-08-08
Robbe's UX target: **no more session restarts after `wairon lock`**, and a delegation model that works identically for local stdio and the hosted data plane (where generated agent FILES can never reach the agent anyway). Approved model: **MCP serves live per-agent briefs; a delegation skill teaches any session (main or sub) to fetch a brief and spawn a scoped generic subagent from it.** Robbe explicitly does not value the CLI agent picker / native auto-delegation — a native subagent is just a preseeded prompt + fresh context window, and generic spawn-with-prompt provides the latter. Key insight: make the dynamic thing the DATA, not the tool list — briefs are composed per call from the current tree, so nothing goes stale and no listChanged machinery is required for correctness (hosted stateless stays correct-on-poll by construction).
Research ground truth (2-agent sweep): per-subsystem owners + architect are ALREADY the default granularity (`generateComponentImplementers` schema default false; this repo = 11 agents for 194 components); the topology is ALREADY runtime-resolved on every call (`loadRegistry` → `resolveAgentTopology`, no persisted registry); skills ALREADY flow over MCP as resources+prompts via the shared factory (hosted included); hosted serves NO agent briefs; NEITHER server emits listChanged (local even advertises `tools:{listChanged:true}` and never sends — fixed by this track's honesty clause); generated implementer files are ~1KB by-reference pointers (no enforcement briefs would lose — exporters emit no `tools:` frontmatter).
**SPEC AUTHORED + validates 0 errors** (9 warnings = draft notice + UNREALIZED_METHOD/CALL_STEP_UNREALIZED/UNREALIZED_DEPENDENCY on the new methods — the implementer's todo):
- **G1 sdd_core**: types `agent_brief` (rendered instructions + ownedPaths/readPaths/domainRoot/template scope fields; never persisted) + `agent_template`; NEW `template_source_adapter` (Adapter, src/core/templates.ts — the previously unmodeled 3-tier template I/O, shared by exporters and briefs so file/MCP instructions can't drift); `iagent_resolver.composeAgentBrief(agentId)` (+ full L5: resolve topology → refuse unknown ids → loadTemplate → render vars → return brief) + resolveAgentTopology description reframes component implementers as a legacy escape hatch; `icore_portal.composeAgentBrief` 1:1 forward.
- **G2 sdd_mcp**: `imcp_core_adapter.composeAgentBrief` → core portal; `imcp_orchestrator.getAgentBrief` (symbol sdd_get_agent_brief); `imcp_portal.sdd_get_agent_brief` tool; resources UNIFIED — `listResources` = skills + one `wairon-agent://<id>` descriptor per current-topology record (recomputed per request), `readResource` branches on URI scheme (wairon-skill:// → skills adapter, wairon-agent:// → composeAgentBrief); `createMcpServer` description now REQUIRES truthful capability declarations (resources/prompts advertise listChanged only because the long-lived stdio session actually emits it after topology/skill-altering spec writes; hosted stateless advertises none). Hosted needs NO sdd_host delta: shared factory serves the tool, `sdd_get_*` naming lands in the mcp:read class, resources pass ungated.
- **G3 sdd_skills**: subsystem description now names FIVE built-ins incl. **sdd-delegate** (the delegation skill: fetch live brief → spawn scoped subagent); `buildServerInstructions` (interface + L5 step 4) composes the delegation directive into the connect-time briefing.
- **G4 sdd_cli sweep**: `runInit` — generateComponentImplementers defaults OFF in the written config (fixes the init.ts:559 inconsistency vs schema/provision defaults); `runGenerate` — files reframed as the optional MATERIALIZED VIEW; pruning always resolves the FULL topology for its expected set so a domain-scoped run prunes safely (fixes the scoped-lock orphan bug, generate.ts:176); `runLock` — narrative gains the honest regenerate step (captureJumps insert) and post-lock guidance points at live briefs, NO restart instruction (cli/index.ts:199-202 + ai-guide.ts:59 to be rewritten).
- [x] Human review + `wairon lock` DONE (2026-08-08); spec commit 791d0c0 on feat/dynamic-agent-briefs (branched off the theme-builder HEAD — the two webapp-theme commits ride along; NOT based on dev).
- [x] **IMPLEMENTED + verified 2026-08-08** (commit ff59ff0; 4 subagent waves: core, skills, cli in parallel → mcp after core): typecheck clean, **1561 tests** (134 files), ordered build + SPA re-embed, `validate --ci` **exit 0 with ZERO findings** — the conformance gate itself confirms every new method AND call step realized (one post-wave fix by hand: the wairon-agent:// listing called the core hop through its import alias `coreResolveAgentTopology`, invisible to the call-step checker — replaced with a local `resolveAgentTopology` adapter-forwarder matching the file's own convention). Notes: brief var map duplicates exporters' buildVars verbatim (core must not import exporters — downward-only); `loadTemplate` widened to `Template & AgentTemplate` (one implementation serves exporters + briefs, per spec); notification emission via SDK `sendResourceListChanged`/`sendPromptListChanged` guarded pre-connect, never on hosted; hosted permission gate needed NO change (`sdd_get_` = read prefix). Residuals (pre-existing, deliberate): pruning stays `.md`-only so `.yaml` orphans in gemini/agy dirs are still never pruned; a `--target`-scoped generate now reconciles ALL configured target dirs (strictly more correct).
- [ ] Robbe: eyeball on the local stack (hosted data-plane brief fetch + a live re-lock mid-session confirming the next sdd_get_agent_brief reflects it without reconnect) → merge to dev. Mind the branch base: dev merge drags the two theme-builder commits unless rebased/merged separately.

### Stage 2 — zero files by default + user guidance seam (SPEC DONE, validates 0 errors; awaiting lock) — decisions from Robbe 2026-08-08
Robbe: prefer storing NO agent files at all — briefs inferred on request; boilerplate is the template, everything else derives from specs, so a generated file is redundant. ONE optional user seam: a per-agent readme generated once from the specs which the user may adjust. Ownership doctrine confirmed: an owner's scope runs UP TO its chained subprojects, never into them (already structurally true; now stated on composeAgentBrief).
Approved design: **(1) zero files default** — new `rules.materializeAgentFiles` (default OFF); `runGenerate` becomes a RECONCILER: ON → owner/architect files rendered through the SAME live brief composition + full-topology prune; OFF (default) → desired state is zero agent files, leftover wairon:managed files removed once, only guides + skills (re)injected. Pruning thereby disappears from the default UX entirely. **(2) guidance seam** — `.wai/agents/<agent-id>.md`, user-owned, scaffolded ONCE on request, NEVER regenerated/pruned, read LIVE on every brief composition and folded in under '## Project guidance' (composeAgentBrief step 6-7); lives in the project tree so it travels to hosted automatically (hosted briefs include it — .claude/agents never could). **(3) CLI** — new `wairon agent brief <id>` (print the live brief) + `wairon agent customize <id>` (scaffold, refuses when the file exists); new icli_core_adapter.composeAgentBrief forwarder.
Spec delta: template_source_adapter description + `loadAgentOverride` (+L5); agent_resolver_impl composeAgentBrief steps 6-7 insert + interface description (scope-up-to-not-into doctrine); icli_core_adapter/cli_core_adapter_impl composeAgentBrief; icli_runner runAgent (+9-step L5 switch) + runGenerate reconcile semantics + runInit zero-files note. Validates **0 errors**; 4 warnings = code-not-written.
- [x] Human `wairon lock` DONE (2026-08-08, spec commit 8ace761).
- [x] **IMPLEMENTED + verified 2026-08-08** (commit 7f95571; 2 parallel subagents): typecheck clean, **1571 tests** (135 files), build, `validate --ci` exit 0 (all 4 stage-2 conformance warnings cleared), live CLI smoke (`wairon agent brief sdd_skills-owner` prints the composed brief on this repo). Implementation notes: exporter agent-file BODY now comes from composeAgentBrief itself (buildVars+render usage deleted from exporters/generate.ts — one text across file/tool/resource, guidance folded into materialized files too); `pruneStaleAgents` gained an optional scanDirs param (an empty expected set alone scanned nothing — scan dirs were derived FROM the expected paths); provision.ts + init.ts literal RulesConfig constructions gained the new field (zod-default fields are required on the inferred type — the generateComponentImplementers precedent); runLock wording softened to "generated outputs reconciled". RELEASE NOTE REQUIRED: default OFF removes previously generated managed agent files on the next generate/lock run (deliberate — wairon-owned artifacts reconciled away; hand-authored files and .wai/agents/ never touched); this repo's own .claude/agents will disappear on its next lock unless materializeAgentFiles: true is set.

## Declared entrypoints — register steps + invokedBy (ACTIVE) — decisions from Robbe 2026-08-10, branch feat/declared-entrypoints
Root cause analysis (Robbe's UNUSED_* false-positive question): unused-detection is a pure spec-graph walk (rules/graph.ts reachabilityRule) — code inheritance/interfaces are irrelevant, and the spec model has no polymorphism seam (L3 = single owner, calls name component ids). The REAL false-positive families, per this repo's own six lint.allows: (1) composition-root/runtime callbacks (host_server start/stop, periodic timers), (2) cross-subsystem consumers through published code barrels (pack_store×5 ← sdd_cli, variant_registry ← sdd_mcp, openapi_codec ← sdd_host), (3) external/library entry points (loadExtensions). A lint.allow also does NOT seed the walk, so suppressions cascade downstream.
Approved design (two mechanisms, honesty-differentiated): **(1) `register` narrative step** — targetComponent/targetMethod resolved exactly like a call step; a deferred-invocation HANDOFF edge: reachability reaches the callback wherever the registering narrative is reached (derived — the callback returns to unused if its registrar does); exempt from CALL_STEP_UNREALIZED (a registration references, not invokes); NOT followed by the durability boot walk (non-flooded doctrine: registration ≠ boot-time invocation). **(2) `invokedBy` on L3 methods** — `{kind: runtime | external | sibling-subsystem, caller}` (types declared_invocation + method_signature; interface_spec.methods now MethodSignature[]); unused-detection seeds the method as an entrypoint (propagating, unlike an allow); caller prose mandatory (INVOKED_BY_UNDESCRIBED) and declarations on internally-reached methods flagged stale (INVOKED_BY_REDUNDANT). sibling-subsystem is the typed second-best to modeling the edge (core_portal passthrough stays the honest fix for pack_store).
- [x] Spec deltas authored + tree validates **0 errors / 0 warnings** (declared_invocation + method_signature types; interface_spec/implementation_spec vocabulary; ispec_validator + sdd_validator descriptions; spec_validator_impl validateSddTree step 18 reachability clause)
- [x] Implement (subagent, reviewed): models schema ('register' + invokedBy, caller lint-not-load-failure) + graph.ts (register traversal behind WalkOptions.followRegisterEdges, two-phase redundancy walk reusing phase 1 when no declarations, INVOKED_BY_* anchored to the INTERFACE spec, caller thinness = the 40-char intent floor) + boot-walk exclusion (semantic-edges) + contracts register-target parity + narrative-flow successor fix (register would have dead-ended → false UNREACHABLE_STEP) + antipatterns (terminal yes, cycle edge no) + call-conformance naturally exempt (regression test) + writer needed NO change (schema-parse spreads; ROUNDTRIP-proven) + diagram dashed async arrow / canvas ⟲ label + regenerated engine + skills templates + CHANGELOG. Typecheck clean, 1604 tests green (+13: 12 rules + 1 live MCP roundtrip). MCP schema additions (define_interface invokedBy, write_narrative 'register') land in src/mcp/server.ts — see seam note below.
- **CONCURRENT-WORKSTREAM NOTE**: a parallel session was refactoring src/mcp/server.ts (the carry-forward seam fixing the "stale server strips unexpressed fields" gotcha + tests/mcp/schema-field-coverage.test.ts) while this track implemented. server.ts + server-schema-roundtrip.test.ts hold BOTH workstreams' edits (coexist cleanly; combined suite green; their coverage test REQUIRES every MethodSignature field in the tool schema — invokedBy satisfies it). Those two files + the untracked coverage test are deliberately LEFT UNCOMMITTED by this track so the seam work isn't entangled/misattributed — whichever session commits the seam carries the three localized schema additions with it.
- [x] Dogfood COMPLETE (2026-08-11, post build + /mcp reconnect — the STALE SERVER guard earned its keep twice: two pre-reconnect writes warned AND silently dropped invokedBy, both verified on disk + reverted): all SEVEN UNUSED_METHOD lint.allows deleted (admin/extension/git_backing orchestrators, host_server, openapi_codec, pack_store_adapter, variant_registry_adapter); host_server_impl.start steps 6/7 retyped call→register (schedule-not-invoke, the timers stay reached through the now-seeded start); invokedBy on 15 methods total — the planned 9 (loadExtensions external; pack_store×5 + resolveVariantGuidance + toOpenApi sibling-subsystem; start/stop runtime, init stays lifecycle-seeded) PLUS 6 extension_orchestrator methods the blanket allow had been SILENTLY hiding (packEntryLabel/packEntryRef/globalPacksEnabled → cli+host pack planes, diagnoseProjectPacks/pinInstalledPacksAsSelections → doctor, defaultPackSelections → init — the cascade problem this feature exists to fix, demonstrated on our own tree). Validates **0 errors / 0 warnings** with ZERO UNUSED_* suppressions anywhere.
- [ ] Robbe: `wairon lock` (statehash re-dirtied by spec deltas) → merge to dev (branch is stacked on feat/dynamic-agent-briefs — merge that first or together)

## E2E agent-journey tier (ACTIVE) — decisions from Robbe 2026-08-11, branch feat/e2e-journeys
Professional-OSS assurance for the MCP server: black-box journeys against the BUILT artifact (the tier that catches the stale-server field-stripping + stale-binary incident classes unit tests structurally cannot see — both bit us on 2026-08-11).
- [x] tests/e2e/ suite (17 tests, ~3s): helpers (scratch project + StdioClientTransport spawn of `node dist/cli/index.js mcp serve`, dual-channel callTool, on-disk YAML asserts, fail-fast when dist missing) · authoring journey (init→subsystem→components→interfaces→narratives→validate 0 errors→status→agent brief, field-exact on disk) · declared-entrypoints regression (invokedBy VERBATIM on disk = the stale-server assertion; register step; INVOKED_BY_UNDESCRIBED on thin caller) · failure paths (invalid step type / unknown component / bad componentType → refused with the value named AND spec tree byte-identical) · CLI smoke (validate --ci exit 0, lock --yes writes stateId, agent brief non-empty). vitest.e2e.config.ts separate; unit suite excludes tests/e2e (1605 stay green).
- [x] CI gate: ci.yml runs test:e2e after Build (same job) — PR-blocking once branch protection marks CI required on dev/main (repo-settings step, Robbe).
- [x] Post-publish verify: publish-npm.yml exposes workflow_call output `version`; release.yml `verify-npm` job (clean node:20 container, NO checkout) npm-installs the just-published @wairon/cli with propagation retry, asserts `wairon --version` == tag, and completes a dependency-free JSON-RPC initialize handshake against `wairon mcp serve` — handshake replicated locally in a projectless dir (serverInfo name=wairon + protocolVersion returned).
- **PRODUCT FINDINGS from the journeys (reported, deliberately unfixed here)**: (1) `DRAFT_SUBSYSTEM_WARNING` is FATAL under validate --ci while DRAFT_COMPONENT_WARNING is waived — isCiDraftWaivable (src/commands/validate.ts:41-50) omits it though it carries the same draftContext:true; any fresh draft tree fails --ci on a pure status notice. Likely oversight; invisible on our locked dogfood tree. (2) A design-only pre-code tree cannot pass --ci without lint.allow (MISSING_SOURCE_PATH fatal + not draft-waivable, declaring sourcePath early = MISSING_SOURCE_FILE error) — maybe intended, worth a doctrine decision. (3) Pre-existing: git-subprocess unit tests flake at full parallelism on fast machines (5s testTimeout), pass with --max-workers 4.
- Deferred: generate/pack-workflow journeys, .wai/agents customize-seam journey, @wairon/sdk publish smoke, golden-fixture version-skew tier, nightly LLM evals.
- [ ] Robbe: merge to dev; enable branch protection (require CI) on dev + main after first green run.

## Rule-matrix invariant tier (ACTIVE) — decisions from Robbe 2026-08-11, branch feat/rule-matrix-tests
Robbe's ask: airtight, SCALABLE validation coverage — happy AND unhappy paths, all rules on all spec kinds/stereotypes, packs included; tests derive from documented intent, never tailored to implementation. Audit found 159 codes / 18 never referenced by any test.
- [x] Harness (1 infra agent): defineRuleFixture contract (scenario-realism enforced, no placeholder ids), loader-path runner (real temp .wai tree + setProjectRoot + validateSddTree ≈20ms/fixture — regressions the loader starves are exactly what this catches), auto-collecting families (import.meta.glob, zero conflict surface), meta.test.ts invariant vs knownIssueCodes WITH the fixture pack loaded, one-way ratchet.json (uncovered set only shrinks; new rule without fixtures = red CI), `validateFromSubdir` seam (chained-child root binding) + `anchoredTo: null` (tree-level findings), fixture packs ledger-platform + carrier-doctrine (namespaced assertion codes enforced same as builtins), coverage-ratchet thresholds in vitest.config (global ~36.6/31.2/41.8/68.5 excl. rules glob; src/core/rules/** 90/81.9/93.5/93.4).
- [x] Fixtures (4 parallel family agents + 2 follow-ups): **561 fixtures — 222 fire / 339 control — covering 158 of 160 codes fire+control**, incl. the full 12×12 stereotype dependsOn sweep (59 illegal fires / 85 legal controls, MediBook clinic naming) + view row, unused-detection/invokedBy/register realistic shapes, durability boot-graph non-flood proofs, conformance via real TS files at exact grade, packs/store/variants through real resolution (WAIRON_PACKS_DIR seam), chained-context honesty pass via the subdir seam. Suite: 1605 → **2175 tests** (matrix tier 570); testTimeout 5s→15s (matrix load tipped known-marginal git/skill/producer tests).
- **RATCHET RESIDUE (2, both documented)**: ROUNDTRIP_SERIALIZATION — by design no declarative fire state exists (loader/writer are exact inverses; tripwire for drift; historical lock-refusal shape pinned as control); NAMESPACE_SHADOWING — see finding 1.
- **PRODUCT FINDINGS**: (1) **NAMESPACE_SHADOWING structurally unreachable from disk**: qualifyId (src/core/specs.ts ~101) root-anchors a chained child's colliding bare id BEFORE the rule can see a qualified form — the child's subsystem silently merges into the root id space (observed: duplicate subsystem + ORPHANED_SUBSYSTEM, no shadowing warning); the exact hazard the rule documents, tripwire blind on the real path. Fire fixture preserved commented in references-namespace.fixtures.ts. FIX CANDIDATE. (2) **Specialist doctrine gap**: prose says Specialists must not own persistence/runtime concerns, but the enforced forbidden list omits Registry and Actor — Specialist→Registry and Specialist→Actor are quiet; matrix encodes the explicit list as legal controls pending a doctrine call. (3) CHAINED_SUBPROJECT_CONTEXT bypasses addIssue (issues.unshift in validation.ts ~498) — sddRuleSeverity/lint.allow don't apply to it; probably fine for a tree-level notice, noted.
- [ ] Robbe: merge to dev; doctrine calls on findings 1 (fix qualifyId collision handling) + 2 (Specialist forbidden list).

## Pre-release bug fixes + Specialist doctrine closure (ACTIVE) — Robbe 2026-08-12, branch feat/validator-bug-fixes (stacked on rule-matrix)
Robbe: fix the matrix-found bugs BEFORE the next release; push harder on subtle bugs.
- [x] **NAMESPACE_SHADOWING reachable from disk** (116d5f7): root cause = scanSpecsForProject ran DECLARED child ids through reference-resolution qualifyId, whose root-subsystem anchor silently merged a colliding child subsystem into the root id space. Fix = new qualifyDeclaredId at every declaration site (always mount-qualifies; sanctioned exception: child subsystem named as the mount's local name = flat-external realization, the relativizeId id===prefix inverse); qualifyId untouched at reference sites → roundtrip/fixpoint inverses hold; ORPHANED_SUBSYSTEM ghost symptom gone; faithful matrix fixture enabled as regression; .wai narration check clean (sdd_validator's "root-subsystem shadowing" claim finally TRUE). Adjacent deliberate change: depth-2+ flat-external realizations now collapse onto their mount at any depth. Same commit: **--ci draft-waiver** (DRAFT_SUBSYSTEM_WARNING joins DRAFT_COMPONENT_WARNING; family audited = only those two exist); e2e workaround allow retired.
- [x] **Specialist forbidden list closed** per Robbe's doctrine (wildcard = pure capability; ALL storage even in-memory via Store/Registry/Index/Repository): +Registry +Actor → ['Portal','Observer','Orchestrator','Store','Registry','Supervisor','Actor']; ripple check = inline per-stereotype lists, exactly 2 edges flipped (matrix 61 illegal/83 legal); docs/standards/architecture.md updated; dogfood impact ZERO (own tree: 12 Repository/9 Adapter/2 Specialist deps, no Registry/Actor). MIGRATION NUANCE: SPECIALIST_DEP is error-severity — lint.allow can't suppress; valve = rules.sddRuleSeverity retune or fix the edge (stated in CHANGELOG).
- [x] Gates: tsc clean · 2179 tests (139 files) · matrix 571 incl. meta (ratchet residue = ROUNDTRIP_SERIALIZATION only) · rebuild + e2e 17 green.
- **SPECIALIST USAGE ANALYSIS (18 in tree, all zero-state/zero-write — doctrine already lived)**: clusters → **Arbiter** (pure fact→verdict: permission_resolver, visibility, quota, diagnostics; the hand-built gather/rule split authorization_specialist→permission_resolver becomes the canonical companion shape; edge rule ≈ "depends on nothing") and **Projector** (model→derived view: surface_projector, spec_projection, landscape_diagram, diagram, state_hash; ≤1 read facade; also resolves the stateless-Index temptation) both JUSTIFIED as new stereotypes; Composer (3, coupled) + Codec (2, self-named) below the line → document as named Specialist idioms, promote on 3rd member; residue 8-9 genuine Specialists (auth_specialist archetype); no mistypes; watch share_snapshot_specialist (Orchestrator-creep). Post-retype the wildcard halves without vanishing. AWAITING Robbe's call → would be a spec-first standard track (schema enum, stereotype-deps, matrix, canvas, profiles, docs).
- [ ] Robbe: merge chain #60 → this PR; then vocabulary decision (Arbiter/Projector).
- [x] 2026-09-12: RECOVERED onto dev (branch fix/chaining-verdicts-confinement). PR #61 had merged into feat/rule-matrix-tests 19 s after #60 had already merged that branch, so neither commit reached dev until now.

## Specialist shapes as registered variants (DONE) — Robbe's call 2026-08-12, branch feat/specialist-variants
Robbe on the Arbiter/Projector proposal: not convinced for first-class ("Projector is just a form of transformer? or an orchestrator?"); verdict = registered Specialist VARIANTS as "the workaround inbetween". Design answers recorded: an Orchestrator owns workflow control flow + write licenses — typing verdict engines as Orchestrators-with-repo-access makes rulings time-dependent/unreplayable (the god-channel from the other side); a generic Transformer names the mathematical shape not a role = a second wildcard. Honest concession: first-class Arbiter's only edge over a variant is machine-enforced purity; if that gap ever bites, the cheaper instrument is variant-scoped forbid-edge assertions, not vocabulary growth. PROMOTION CRITERION codified: variant → stereotype when independently re-registered across projects/packs or when rules exceed guidance/assertion expressiveness.
- [x] .wai/variants/specialist-shapes.yaml: arbiter / projector / composer / codec (base Specialist, full discipline guidance — flows into agent briefs via resolveVariantGuidance).
- [x] 14 components tagged via sdd_update_spec (arbiter ×4: permission_resolver, visibility, quota, diagnostics; projector ×5: surface_projector, spec_projection, landscape_diagram, diagram, state_hash; codec ×2: openapi_codec, pack_codec; composer ×3: pack_scaffold, instructions, skills_resource). All verified on disk (stale-server warnings benign — variant is old schema, carry-forward seam merged). Residue: 4 genuine Specialists (auth, authorization=gather-half, share_snapshot=watched, spec_validator=gather+judge).
- [x] docs/standards/architecture.md §8: "The four registered Specialist shapes (variants)" table + promotion criterion.
- [x] sdd_validate_tree 0 errors / 0 warnings (UNKNOWN_VARIANT/VARIANT_BASE_MISMATCH clean).
- NOTE: statehash re-dirtied by the 14 variant tags → wairon lock before/with the merge.

## Chained subsystems — stage 2a-0: project configuration as held state (ACTIVE) — decisions from Robbe 2026-09-13, branch feat/stage2a0-config-repository
Wairon's own tree read and wrote `.wai/project.yaml` directly from 10 components in 4 subsystems (76 code sites in 26 files) with no Store — its own "no persistence shortcuts" rule broken. It ships as its own behaviour-neutral PR before stage 2a; the parked 2a draft (6ad0f67 on feat/stage2a-identity-exports) builds on it. Design record: `docs/design/chained-subsystems/decisions.md` § Wairon modeling decisions.
- [x] L2 (approved 2026-09-13): `project_config_repository` (Repository) owning `project_config_store` (Store, read-through), `project_config_registry` (Registry), `project_config_index` (Index) and `project_config_fs_adapter` (Adapter)
- [x] L3 (approved 2026-09-13): store, registry, index and facade methods, sized to every current reader and writer (map in `docs/design/chained-subsystems/stage-2a0-config-map.md`); `core_portal` reads, `core_orchestrator` writes, plus the sdd_cli, sdd_host, sdd_skills and sdd_validator adapters. Decided 2026-09-13:
  - the Store round-trips keys the schema does not know — no write erases data;
  - the specs folder (`paths.specsDir`) is resolved once at binding through `project_config_index.specsDir()` and handed to the spec repository, which never reads configuration itself.
- [x] L3 refinement (approved 2026-09-13):
  - the registry's pack writes mirror today's code (config map § Registry write semantics): `registerPackRef`/`deregisterPackRef` for path strings; `upsertPackSelection`/`removePackSelection` for selections, returning booleans, with upsert moving a re-selected pack last; `markSelectionsBundled` and `pinGlobalPacksAsSelections` as single writes; every pack write records `useGlobalPacks`; `setGlobalPacksEnabled` dropped;
  - `icli_core_adapter.projectConfigExists` for the "is this a project" guards;
  - `icli_runner.runExecutionShow` / `runExecutionSetTier` — the execution-tier CLI had no spec.
- [x] Spec text corrected: `tree_archive_manifest` (the project name comes from L0); `spec_file_store` (its specs folder is fixed at binding).
- [x] Migrate every spec reader and writer:
  - [x] `core_orchestrator` (provision, chained scaffold, externalize)
  - [x] consumers agent: `extension_orchestrator`, `agent_resolver`, `cli_packs_adapter`, `cli_runner`, `cli_core_adapter`, `cli_validator_adapter`, `cli_mcp_adapter`, `skills_exporter`, `instructions_specialist`, `spec_validator` — on the refined registry; `pack use`/`unuse`/`bundle` given calls-only narratives so their writes are in the call graph; sdd_cli, sdd_core, sdd_skills, sdd_validator at 0 errors (remaining warnings name code not yet migrated)
  - [x] sdd_host: `pack_registry` (reduced to pack storage), `pack_orchestrator`, `project_policy_orchestrator`, `operations_orchestrator` — the whole tree validates with 0 errors.
    - [x] `ProfileApplication.adoptedPackRef`, so orchestrators never compose the storage path
    - [x] exposure-policy administration split into `exposure_policy_orchestrator` (approved 2026-09-13 — `operations_orchestrator` had crossed GOD_COMPONENT with the config edge; now 9 dependencies)
    - [x] `readProjectReferences` left `pack_registry`: `project_config` gains `declaredPackNames()` / `declaredProfileIds()`, and the health report builds the reference in a local step (decided 2026-09-13)
  - stay as they are: `tree_archive_adapter.hasSpecTree` (a layout check of the import destination; its Adapter depends only on sdk_portal) and internalize deleting a child's `.wai` (a project removal); `web_project_orchestrator` and `web_portal` only forward
- [x] Code (2026-09-13, through sdd-delegate owner briefs):
  - [x] wave 1, sdd_core owner: the Repository, the core surface, sdd_core's callers (49cc30e, af7d4d2, 90fda87, 132d23c), then readField (e3a5b97) and provisionProject creating before the L0 (e61a3fb)
  - [x] wave 2, integrated: CLI (1263b28…850043b), skills/validator/MCP readers (d76d0e1…13b0ae8), hosted (8730806 health-references fix, 0447b2d…d3d6622); full suite 2469 passing
  - [x] follow-ups: MCP `sdd_validate_tree` keeps its error on a missing configuration, and an anchored `loadProjectConfig` forward (56de98a, 0029e80); `wairon init` keeps an existing configuration (spec 264c963, code fd9306a + its summary line, decided 2026-09-13)
  - [x] wave 3: loader.ts `loadProjectConfig`/`saveProjectConfig` and the transitional write deleted (b28e36a); the public entry keeps a throwing `loadProjectConfig` and drops `saveProjectConfig` (edc3b06; both decided 2026-09-13); CHANGELOG (0606e8f)
  - [x] gates: typecheck, build, 162 test files / 2474 tests, e2e 21, `validate --ci`, whole tree 0 errors / 0 warnings
  - [ ] PR to dev; the maintainer runs `wairon lock` after merge

## Wairon friction fixes (before stage 2a) — decided by Robbe 2026-09-13
Most of the friction log (F1–F28) is resolved, not deferred: the tool must be clear and truthful, with no unneeded silence or confusion. Every pull request is spec-first, and all land before stage 2a. Order, decided with the rule model's shape (B′) and extended 2026-09-15: schema PR → rule model PR → rule fixes PR → doctrine D1 → doctrine D2 → data-model PR → PR A → PR B → PR C; track D follows. Proposals, examples and every decision: `docs/design/chained-subsystems/friction-fixes-proposals.md` and `decisions.md`.

### Schema PR — method-level source files and findings (ACTIVE, branch feat/method-source-and-findings, stacked on PR #70)
- [x] L3 (approved 2026-09-13, with both refinements): `method_implementation` (types `implementation_spec.methods`) with a per-method `sourcePath` (B3, moved up from PR B); `narrative_step` (fields only); `finding_declaration` and `findings` on `method_signature`; the MCP write tools express both fields
- [x] L4/L5 (approved 2026-09-13; plus the pure type methods `implementation_spec.sourceFiles()` and `method_implementation.sourceFile()` every consumer shares): the consumers of a method's source file described — code model paths, structural, call, dependency, integration and hidden-state conformance, narrative detail, agent write fences, status, chained path re-expression — and `UNREALIZED_FINDING` (declared codes found as string literals in the method's source file). The rule behaviour stays prose in `spec_validator_impl` step 20 until the rule model PR: the one change made before the rules have specs
- [x] Wairon's own tree (after the code lands and the MCP server is rebuilt): a method names another file only when its own body lives there. A scan found 25 — `cli_runner`'s 20 command methods (`src/commands/*.ts`) and `core_orchestrator`'s 5 provision methods (`src/core/provision.ts`) — and whatever linking them surfaces is fixed. Not linked, with reasons: Portal and Adapter methods that forward by import or re-export (144; their realization is the forwarding); methods whose body sits in a collaborator's file (state hash, lock file, diagram); the shared `utils/fs.ts` and `utils/yaml.ts` plumbing (imported by 47 and 15 files — claiming it for one Store would be untrue); `approval.ts`, `config/loader.ts` and `diagram-export.ts`, whose code belongs to no modelled method (track D)
- [x] Fix what linking surfaced (approved 2026-09-14; `validate --ci` fails on warnings): `cli_core_adapter` gains `resolveAgentTopology`, `ensureProjectInitialized`, `listDirectChainedSubprojects`, `defaultPackSelections` (through `core_portal` to `core_orchestrator` / `extension_orchestrator`); `project_config.activeTargetTypes()`; `init` bootstraps L0 through core and stops writing its own architect agent file; `list`, `show` and `generate` resolve the topology through the adapter; `generate`'s cascade is narrated and its redundant cache invalidation removed; the MCP install action is named and bound by `symbol`
- [x] Code; tests proven by revert; gates (typecheck, 2564 tests, e2e 21, validate --ci); CHANGELOG; PR #71, stacked on #70 (rebase onto dev after #70 merges; the maintainer runs `wairon lock` after merge)

### Rule model PR — the 43 validator rules designed (ACTIVE, branch feat/validator-rule-model; A13, shape B′ chosen 2026-09-13)
- [x] L2 (approved 2026-09-14): eight `arbiter` Specialist families — integrity, narrative, intrinsic, doctrine, extension, wiring, conformance, heuristic — plus `narrative_graph_projector` (`projector`); `rule_registry` depends on `rule_store` and the eight. `spec_validator` retyped to Orchestrator (approved 2026-09-15: validation is a workflow)
- [x] L3 (approved 2026-09-15): 43 rule methods carrying their 163 finding codes; `narrative_graph_projector.walk`; the shared helpers placed as `rule_context` queries (with `roundTripIssues` gathered by the validator) and as type methods on `component_spec`, `method_implementation`, `method_signature`, `type_spec`, `interface_spec`, `surface_snapshot` and `surface_contract_entry`; `sdd_rule.scope`; `specScopedRules` on rule_index and rule_repository; `validateComponentCandidate` on spec_validator and validator_portal
- [x] L4/L5 (approved 2026-09-15): full narratives for all 43 rules, faithful to today's code, each method's `sourcePath` `src/core/rules/<family>/<rule>.ts` with `symbol: check`; the projector's walk; one registration step per rule in run order and `rule_store.clear`; `validateSddTree` gathers the round-trip and known-code inputs, then loops over the rule sequence; `validateComponentCandidate` in `src/core/rules/candidate.ts`. L3 additions found while narrating: `code_model.pathKey`/`factsFor`, `source_file_facts.resolveImport` and its analysis fields, `component_spec.isPattern` and its Portal, event and link fields, `subsystem_spec.isDraft`, `rule_context.namingConfigFor`, `implementation_spec.simPath`/`technologies`, `rule_ext.packSelections`/`selectionFailures`, `type_spec.invariants`, typed `profile_def.allowedEdges`. 27 of 44 narratives exceed the coming complexity defaults; they stay faithful here and are decomposed in the doctrine PR
- [x] Code (2026-09-15, waves 84e733d and 2f3e08d): one rule per file in family folders, helpers moved to their homes, `SDD_RULES` into the rule repository, the gather fix, the portal re-exporting `validateComponentCandidate` — no behaviour change, test assertions unchanged; the catalog test (5a3e0b4) compares the code registry with the spec catalog, proven by four spec mutations; gates: tsc, 171 files / 2702 tests, e2e 21, `validate --ci` green (nine draft warnings until the lock); PR to dev, the maintainer runs `wairon lock` after merge

### Rule fixes PR — what narrating the rules found (approved 2026-09-15; design notes section F)
Each reported behaviour is reproduced with a failing test first; a confirmed one changes its rule's narrative, then its code; an unconfirmed one is recorded with the reason. After the rule model PR, before the doctrine PR.
- [x] Narrative family: antipattern analysis reads one step graph closed by a completion step, so it agrees with the graph at parallel arm ends and at the end of the narrative; call cycles through parallel arms are detected; the lint-allow anchor order is locale-independent; cross-tree calls and dispatches resolved through a surface are held to `dependsOn`
- [x] Doctrine family: a non-pattern block is never recorded as an owner; RouterComponent's single Portal; the first owner named; the write shortcut judged on dispatch-table routes; the licensed-edge message names the crosser; the non-Portal endpoint ban reported on its interface
- [x] Wiring family: every in-scope dependency cycle reported in a scoped run; unused-detection honours a draft subsystem and reports UNUSED_METHOD on its interface; untyped seams judged through type references, prose signatures included (wairon's own `icli_runner` host options now typed); claim phrases, register edges and quoted text; invariant draft context; one finding per topic; no guarantee finding against a missing bound method. Kept, with its reason in the narrative: UNASSERTED_INVARIANT on an unimplemented write method
- [x] Conformance family: a missing file is not also an unwired sim; chained subprojects skipped; one report per dependency; method-level `conformance: off` honoured by hidden-state; UNREALIZED_FINDING names both anchors it accepts
- [x] Heuristic family: GOD_COMPONENT and EXCESSIVE_DEPENDENCIES read one effective cap; draft context consistent across naming, complexity, language and technology; an empty profile is no profile for severity overrides
- [x] Integrity family and docs: the unresolved-contract finding reads the shared draft recipe; one type-resolution path; the switch `on` field documented as optional, with the two loop continues and a closing step per nested loop
- [x] Code and gates (2026-09-15): each fix reproduced by a failing test and proven by revert; tsc, 178 files / 2794 tests, e2e 21, `validate --ci` green (nine draft warnings until the lock); rule-matrix ratchet unchanged; PR to dev after the rule model PR, the maintainer runs `wairon lock` after merge
- [x] Left for the doctrine PRs and queued there, each with its reason: the pattern-owning-pattern report and the Gateway Portal count in D1; in D2, the call-cycle miss in `doWhile` and `try` bodies (the step graph gives a try's catch edges to its header rather than its body steps, so the fix changes the graph every flow analysis reads) and draft checks as type methods

### Doctrine D1 — logic and process blocks (decided by Robbe 2026-09-15; proposals E6–E10; branch feat/doctrine-blocks)
- [x] L2 (approved 2026-09-15):
  - The 27 Specialists become Orchestrators (18 `pure`, 9 `read`, shape variants kept), and 14 are renamed for what they are responsible for: `architecture_diagrams`, `state_hash`, `authentication`, `authorization`, `diagnostics`, `landscape_graph`, `permission_rules`, `quota_rules`, `share_snapshots`, `visibility_rules`, `spec_doc_pages`, `pack_scaffold`, `server_instructions`, `skill_resources`.
  - `mcp_server` becomes an Orchestrator, a factory that supervises nothing.
  - `host_server` stays the root Supervisor, with a new `backup_schedule` Actor (the 60-second backup timer) and a new `instance_bootstrap` Orchestrator (boot seeding).
  - The gate identity moves to a pure `gate_identity` in sdd_validator: core compares a lock against the gate id its caller passes, and the state hash's allow goes.
  - Renames go through a new `sdd_rename_component` that rewrites every reference.
  - The realtime hub, also an Actor, stays with track D.
- [x] L3 (approved 2026-09-15; amended the same day when the Gateway pattern retired into a built-in variant):
  - `component_spec.dependencyClass`, plus the type methods `isLogic()`, `holdsState()` and `isRetired()`. The first two give each of the two logic-stereotype sets one home: hidden-state judges logic that holds no state.
  - Doctrine codes:
    - retired: `ARCHITECTURE_VIOLATION_SPECIALIST_DEP` and `GATEWAY_CONTAINMENT`;
    - new: `DEPENDENCY_CLASS_VIOLATION`, `ARCHITECTURE_VIOLATION_SUPERVISOR_DEP`, `ACTOR_REACHED_WITHOUT_SUPERVISOR`, `ARCHITECTURE_VIOLATION_QUERY_DEP`, and `UNOWNED_QUERY` (an error: a Query lives only inside a Repository);
    - pure logic is allowed under Stores, Registries, Adapters, Indexes and Views, and facade forwarding covers Repositories;
    - no Actor-specific rule.
  - Intrinsic rules: `logicDeclaration` (`DEPENDENCY_CLASS_ON_NON_ORCHESTRATOR`) and `retiredStereotypes` (`STEREOTYPE_RETIRED`, covering Specialist and Gateway).
  - Gate identity:
    - `gate_identity.compute`;
    - `computeGateStateId` on the validator portal, the spec validator and the CLI and host validator adapters;
    - core's `readLockState(current)` and `consumedContractInputs()`;
    - the state hash loses its gate method.
  - `renameComponent`, from `sdd_rename_component` down to the core orchestrator; `retireSpecialists(apply)` behind a modelled `runDoctor`.
  - `backup_schedule` (start, stop, sweep) and `instance_bootstrap` (seed).
- [x] L4/L5 (approved 2026-09-15; authored by four agents, reviewed and amended):
  - Validator rules: the class, Supervisor, Actor and Query checks; retired components and Portal or Observer targets judged once; containment waits only where a retired member changes a count; the two intrinsic rules registered after durability-declaration.
  - Gate identity: `gate_identity.compute` keeps today's doctrine identity under the marker `sha256+content+doctrine+inputs`; the host lock and policy flows and the CLI lock ask the validator; the state hash computes content only, types included.
  - Core, CLI and MCP:
    - `renameComponent`: four labelled refusals, every reference position rewritten;
    - `retireSpecialists`: fixed-point classification, with variants rebased through the variant adapter, which gains `listProjectVariants`, `rebaseProjectVariants` and its built-in layer;
    - `readLockState(current)` and `consumedContractInputs`;
    - `runDoctor`, at calls-only detail.
  - Hosted process: `backup_schedule` and `instance_bootstrap`, with `host_server` delegating to both.
  - Descriptions made untrue by D1 are rewritten.
  - `wairon doctor` imports core directly for work no spec models yet. A reasoned UNDECLARED_DEPENDENCY allow on `cli_runner_impl` acknowledges it, and track D routes those reads through modelled adapters.
- [x] Logic is an Orchestrator: a flowchart that may nest others, with one or more cohesive methods (a class, or a module of functions). A logic component declares `dependencyClass: pure | read`, a first-class field the validator enforces as it enforces a Store's `durability` (decided 2026-09-15, revising E7's variant wording); unset means a workflow. `pure` depends only on pure components; `read` adds read methods of Repositories, Indexes and Adapters, the write check judged once facade methods carry effect tags. Specialist retires, with a migration for existing trees; the four shape variants stay guidance-only, rebased onto Orchestrator
- [x] Process layer: an Actor owns one live thing, entity instances included, and its methods are full flowcharts; a Supervisor owns the set and may supervise Supervisors; a live Actor is reached by id through its Supervisor (`dependsOn` lists both) and is the only writer of its aggregate; the method owning a workflow owns its transaction and applies in-memory state only after commit; a Portal may message a Supervisor by id; dependency rules for Orchestrator, Supervisor and Actor; `hidden-state` stops treating Supervisor and Actor as stateless
- [x] Data access: a Store covers one aggregate; `Query` joins Store, Registry and Index as a Repository member for computed reads; cross-aggregate reads go through a read Orchestrator or a read-model Repository; the outbox is a sibling Repository (§7 and §10 aligned)
- [x] Timers: per-instance timers are Actor state; deadlines are aggregate fields read by an Index; a schedule aggregate only for timers that span aggregates
- [x] The standard gains the module realization in the language bindings and the live auction as its worked example
- [x] Decide how the lock's gate hash gets the validator's built-in rule identity (decided at L2 and shipped: the validator's `gate_identity` computes it, and core compares a lock with the identity its caller passes). `state_hash_specialist` (sdd_core) reads `SDD_RULES` from sdd_validator's rule repository — a cross-subsystem edge no spec declares, made visible when the rule model PR moved the list; acknowledged with a reasoned UNDECLARED_DEPENDENCY allow on `state_hash_specialist_impl` until decided. Options: sdd_core reads it through sdd_validator's published surface (a mutual subsystem dependency to acknowledge), or the lock workflow passes it in with the doctrine
- [x] `LOGIC_STEREOTYPES` is defined twice (narrative-detail and hidden-state); give the logic-stereotype test a home when the stereotypes change: `ComponentSpec.isLogic()` and `holdsState()`
- [x] From the rule fixes: a pattern owning a pattern also raised VISIBILITY_VIOLATION on the inner pattern's dependants; the inner pattern now gets no owner, so only PATTERN_OWNS_PATTERN reports. The Gateway Portal-count question closed when the Gateway pattern retired.
- [x] Variants built in (decided 2026-09-15):
  - A built-in layer under the global and project variant directories ships arbiter, projector, composer and codec, based on Orchestrator, and `gateway`, based on Portal. Wairon's own `.wai/variants/specialist-shapes.yaml` goes.
  - The Gateway pattern retires:
    - `GATEWAY_CONTAINMENT` goes;
    - facade forwarding covers Repositories only;
    - the cross-subsystem rule no longer accepts a Gateway as a front door;
    - `STEREOTYPE_RETIRED` reports a Gateway with manual steps, while `doctor --fix` retypes Specialists.
- [ ] Code (branch feat/doctrine-blocks):
  - [x] Waves 1–5: the model, MCP schema and built-in variants; the validator rules and gate identity; the rename tool, Specialist retirement and doctor; the hosted process; the standard, guides and UI
  - [x] Wave 6 code: wairon's own 27 Specialists retyped through `retireSpecialists` (18 pure, 9 read), guides regenerated, the mermaid diagram's Query class, and docs and comments following the renames
  - [x] Wave 6 leftovers: the demo seed's Specialist is a pure `pricing` Orchestrator, the web editor offers a retired type only as a component's current value, and `portal-fields`, `GOD_COMPONENT` and the comments stop teaching a Gateway
  - [x] Wave 6 tree:
    - the 14 renames through `sdd_rename_component` (42 specs moved) and their display names;
    - the prose that still taught Specialist or Gateway or named an old component: the rule families now read as pure arbiter logic, and "the permission resolver" reads as `permission_rules`;
    - `gate_identity` is pure;
    - `src/server/permission_resolver.ts` moved to `permission-rules.ts`, together with its spec;
    - the L0 requirement that listed a nonexistent Resolver type now points at the standard;
    - the rename tool's findings are friction F40–F46.
  - [ ] Gates, CHANGELOG, PR

### Doctrine D2 — complexity, naming and cohesion checks (decided by Robbe 2026-09-15; scoped 2026-09-17 on measured evidence; D2a on feat/doctrine-checks)
Measured on wairon own tree after D1 (1061 narratives): 777 linear, 211 simple, 40 moderate, 9 complex, 24 severe — 40 exceed the defaults (33 by cognitive score, 21 by step count). The two axes are independent: `rule_registry.registerBuiltinRules` is 48 steps scoring 1, `heuristic_rules.targetLanguage` scores 44 in 28 steps. Naming: 7 head-noun mismatches (exactly the Registry-to-Store/Repository promotions), 0 generic words, 66 stutter at the strictest definition (27 Adapter, 6 Portal), 6 one-method non-Adapter components. Cohesion: 5 Orchestrators with two or more groups of two or more methods.
- [x] Decisions (2026-09-17):
  - stutter exempts thin forwarders (Adapter, Portal), leaving 33 — a CLI adapter method mirrors a user-visible command name, so the repetition carries meaning;
  - the 24 severe narratives are split; the 9 complex and the steps-only cases carry a reasoned allow;
  - `cli_runner` and `core_orchestrator` are allowed as deliberate facades; `project_ops_orchestrator`, `web_admin_orchestrator` and `web_orchestrator` are split;
  - ships as D2a (checks, config, precedence, the 7 registry renames, allows) then D2b (the cleanup, removing each allow).
- [x] L2 approved (2026-09-17): no new components. Three methods on `heuristic_rules` — `narrativeComplexity`, `namingDiscipline`, `methodCohesion` — carrying nine codes; `cognitiveScore`/`complexityLevel` on `method_implementation` and `headNoun` on `component_spec` as pure type methods; four fields on `complexity_rule_config`
- [x] L3, types written: the score and band, the head noun, and the four config fields (`maxNarrativeSteps` defaults to 25, `narrativeStepsHardMax`, `cognitiveWarnAbove` default moderate, `maxCognitiveLevel`)
- [x] L3, rules: the three methods with their findings; `EXCESSIVE_NARRATIVE_STEPS` moved from `complexityAndMetadata` to `narrativeComplexity`
- [x] Config precedence DECIDED and shipped: the project wins, aligning values with severities. The three `*ConfigFor` methods and their specs say so, and the CHANGELOG carries the upgrade note
- [x] L4/L5 approved, then five code waves, all merged by cherry-pick. **D2a is PR #76**, gates green: 3166 tests, 21 e2e, validate --ci with only the 12 drafts
- [ ] Carried to D2b, from the rule fixes: ~~UNCONDITIONAL_CALL_CYCLE misses a call in a `doWhile` body or the first step of a `try` body~~ FIXED in wave C, with fire fixtures for both (no existing fixture covered either, so the finding set of the 561 was unchanged by it); draft checks on interfaces and subsystems as type methods
- [ ] D2b-0, the tool the cleanup needs: `sdd_rename_method` (branch feat/rename-method). A contract method has no rename — `sdd_update_spec` upserts by name — so 37 renames would mean re-sending every implementation narrative by hand (F16). It mirrors `sdd_rename_component` down the same chain, pins the symbol so code keeps binding, and reports the prose it leaves alone (F43). Specs approved and committed; code in progress.
  - NOTE for D2b-3: `renameComponent` and `renameMethod` both score exactly 9, the top of the moderate band. Five guards cost 5, and a nested branch costs 2, so one more refusal on either tips it to complex. Extracting the shared refusal block is the concrete first candidate when the component splits are designed.
- [x] D2b-1 MERGED (PR #79): 37 contract methods renamed through sdd_rename_method, mcp_server's display name, 25 naming allows removed. Five code symbols followed; the rest keep a `symbol:` pin, because the clean name is a bare verb exported from a module and three (`export`, `import`, `delete`) are reserved words. Two findings: a rename can break a second component bound to the same function by name (identity_portal, caught only by validate), and `mentions` skips `lint.allow[].reason`.
- [ ] D2b-2, the rule narratives (decided 2026-09-17: split aggressively, splits first then helpers):
  - **D2b-2a MERGED (#81): five rule methods became fourteen**, all scoring 4 to 19. contract-symmetry-and-narratives (99), narrative-flow (61), structural-conformance (36), dispatch-tables (40), architectural-profiles (30), type-references (26). Two things had to precede a cut: narrative-flow's soundness verdict moved onto method_implementation as stepConfigVerdict (splitting the flag away would have called sound steps dead), and contract-symmetry's duplicated surface-resolution block was unified first. Proven finding-for-finding across every rule-matrix fixture: 2069 findings, byte-identical. Wairon also judges its own rules at cognitiveWarnAbove: complex, with the reason recorded in project.yaml.
  - **D2b-2b (branch feat/doctrine-rule-helpers): the shared read model, then the doctrine family.**
    - Five memoized indexes now live on rule_context, where eight rule files each built their own: codeIndex, realizationIndex, importGraph, ownershipIndex and dependencyEdges, plus a memoized interfaceMethodsOf. dependencyConformance fell 28 to 14 on sharing alone.
    - Three deliberate refusals: technology-boundaries keeps its own owner map (last-claimant-wins over every claim, feeding a scope-widening step, so sharing would ADD findings); dependency-conformance keeps its facade-hop map for the same reason; each rule's filter on which files count stayed in the rule, because "only exact grade may accuse" is doctrine, not plumbing. The import graph turned out to be two graphs, parameterised per closed set.
    - stereotypeDependencies (95) and patternOwnership (52) became nine rules scoring 3 to 17, with the pack allowedEdges escape carried on the edge so a licensed edge cannot escape only part of the matrix. pattern-containment measured 10, so the per-kind split was not needed. The PORTAL_DEP short-circuit survives three ways, and the two CROSS_SUBSYSTEM_NON_ADAPTER arms stayed separate because only one carries surfaceResolved.
    - Registry 48 to 67 rules. Severe narratives 24 to 15, each covered by an allow naming what is left.
  - **D2b-2c, the fifteen still severe**: declarativeAssertions 61, targetLanguage 44, publicSurface 33, integrationConformance 29, narrativeDetail 27, portalCallAuth 26, narrativeAntipatterns 26, callConformance 25, namespaceHygiene 25, technologyBoundaries 23, narrative_graph_projector.walk 21, portalEndpoints 20, unusedDetection 20, invariantBacking 20; and host_request_orchestrator.handle 25, which is not a rule and belongs with D2b-3's component splits.
    - **Wave A done (9f850e9): the three heaviest became seven.** `declarativeAssertions` (61) split one rule per assertion kind — `forbid-edge`, `require-field` and `endpoint-shape` are each a word a pack author types, so each is a rule a user can find — at 14/14/10, over a new shared reading of one declared assertion (`extension/declared-assertion.ts`). `targetLanguage` (44) became `signature-language-builtins` (6) and `narrative-language-constructs` (6); the split alone left the signature half at 25, so each half now collects the specs it judges before judging them. `technologyBoundaries` (23) was reshaped rather than halved: `TECH_ON_LOGIC_COMPONENT` lifted out at 3, and the other two codes stayed together at 15 by flattening the token walk, their shared technology-home index and its deliberate last-claimant-wins owner map untouched. No new `rule_context` member earned a place — nothing there is read by more than one rule. Proven at 2069 findings, md5 identical before, after the code and after the specs. All four allows deleted, one of which had named the wrong narratives for some time. Registry 67 to 71 rules.
    - **Wave B done (base 9f850e9): four more severe rules come under the bar, one of them without a split.** `publicSurface` (33) became `public-surface-binding` (14), `public-surface-declared-type` (15) and `public-surface-bound-contract` (16); `namespaceHygiene` (25) became `reserved-id-segments` (3) and `namespace-shadowing` (3) over a new `rule_context.specIds()`, because its size was neither of its two questions but a private `checkId` closure that L5 cannot name (friction F54), so the same two checks were unrolled five times; `integrationConformance` (29) became `integration-sim-declaration` (7), `-file` (7), `-wiring` (14) and `-coverage` (16), each restating the precedence it used to inherit by falling through, with no shared "is this judgeable" helper; and `callConformance` (25) was NOT split — one code, one question — reduced to 15 instead by `rule_context.implementationMethods()`, the plumbing descent only. The exact-grade test stays written in the rule: D2b-2b refused to share that stance, and `narrative-detail` proves it was never shared anyway, since it downgrades one check rather than skipping. One proposal was refused: the bound-contract rule was to read `pi.component` as a string only and never resolve it, but dropping `public-surface-binding`'s resolution guard would make `intf.component !== pi.component` trivially true for every interface whenever the component id is a typo, so the guard is restated (+3, 16 not 13). The original short-circuited on an unresolvable backing before ever reaching the interface check, so restating it is behaviour preservation rather than taste — the brief was wrong, and no fixture covers the combination that would have shown it. Proven at 2069 findings, md5 identical before and after. All three allows deleted, one of which had named `dependencyConformance` since the read-model wave dropped it to 14. Registry 71 to 77 rules; nothing in `integrity_rules_impl` or `conformance_rules_impl` is severe or over 25 steps any more. Eight severe narratives are left in the tree: narrativeDetail 27, narrativeAntipatterns 26, portalCallAuth 26, narrative_graph_projector.walk 21, unusedDetection 20, invariantBacking 20, portalEndpoints 20, and host_request_orchestrator.handle 25, which belongs to D2b-3.
    - **Wave C done (base db0cb0d): the last six severe rules come under the bar.** `narrativeAntipatterns` (26) became `meaningless-branches` (11), `inescapable-cycles` (8) and `unconditional-call-cycles` (11) over a shared `narrative/completed-step-graph.ts` (the completion-closed graph, the graph-agnostic Tarjan SCC, and isUnavoidable) — two rules run the SCC, over graphs that share nothing, and the module says so. `narrativeDetail` (27) became `narrative-detail` (18, the two arms of one branch) and `detail-sufficiency` (16), whose two codes stay together because the measured finding SUPPRESSES the stereotype one. `portalEndpoints` (20) became `portal-endpoints` (12) and `non-portal-endpoints` (8); `portalCallAuth` (26) became `portal-call-auth` (12, the call site) and `auth-source-wiring` (16, the source) — two, not five, because the three source codes are one question's three failure modes and each extra rule would re-walk every narrative step to ask a third of it. `invariantBacking` (20) became `unique-invariant-ids` (6), `invariant-backing` (12) and `invariant-references` (3), with the ref grammar shared in `wiring/invariant-ref.ts`. `unusedDetection` (20) was split only three ways on purpose: the reachability walk already runs TWICE (internal seeds for `INVOKED_BY_REDUNDANT`, plus invokedBy seeds for the UNUSED_ codes), so one rule per code would walk the graph four times — `unused-types` (3) and `invoked-by-description` (8) lift out, the trio keeps the rule at 15. One proposal refused: `detail-sufficiency` was to take `rule_context.implementationMethods()`, but that member drops implementations whose component does not resolve (which UNNARRATED_COMPLEXITY still judges) and chained subprojects (which DETAIL_BELOW_STEREOTYPE, a spec-side verdict, must keep), so the rule flattens its own gather instead. No fixture covers either combination. Proven at 2069 findings, md5 identical before and after. All three family allows deleted — no rule family carries a NARRATIVE_COMPLEXITY allow any more. Registry 77 to 86 rules. Two severe narratives are left in the tree, neither a rule: `narrative_graph_projector.walk` 21 and `host_request_orchestrator.handle` 25. Two new unclaimed source files join F17's list: `narrative/completed-step-graph.ts` and `wiring/invariant-ref.ts`. The carried defect is fixed in the same branch: UNCONDITIONAL_CALL_CYCLE missed a call in a `doWhile` body and one in the first step of a `try` body.
  - **Unclaimed source files to decide:** read-model.ts, types.ts and doctrine/stereotype-terms.ts are named by no implementation (friction F17). isPureLogic and isReadLogic are natural component_spec type methods; only storeResolutionHint needs a home in the rules.
  - `host_request_orchestrator.handle` (25/59) is not a rule; it goes to D2b-3 with the component splits.
- [ ] **D2b-3, the component work.** Measured 2026-09-18: 51 allows, of which only 13 promised future work — the other 38 are reasoned and permanent (network boundaries, the separate @wairon/sdk package, deliberate facades, sanctioned standalone Stores), plus 11 UNUSED_TYPE that belong to PR A item A12.
  - **Wave 1 done (ce17f60, 117176b): the cohesion allows, 51 to 48.** `INCOHESIVE_METHODS` now exempts a pure forwarder — every narrated method exactly one `call` or `dispatch` step, every other step a `return`. Two looser criteria were refused with evidence: a step-count ceiling admits a method with no call at all, and the one I suspected was truer ("exactly one call step and no flow steps") exempts `skill_resources`, whose `local` steps enumerate skill templates and resolve ids. The criterion shipped is `facade-forwarding`'s own §7 definition relaxed by the `return` terminator — not a new yardstick.
  - It covers exactly one firing Orchestrator, `project_ops_orchestrator` (38 narrated methods, 0 non-forwarding), which was ALREADY the facade shape, so nothing moved there. `cli_runner` (27 of 32 non-forwarding) and `core_orchestrator` (13 of 45) do not qualify and keep their allows, whose "deliberate facade" reasons were corrected to rest on being the one entry point of the terminal and of the library.
  - `web_orchestrator` became `web_session_orchestrator` (10 methods to 8, 10 deps to 9). The views half was refused: `web_graph_orchestrator` already existed and its own description says it was split out of this component, so `getGraph`/`getProjectCanvas` were a vestigial hop. Deleted, with `web_portal` reaching the graph orchestrator directly (6 to 7 deps); both call steps resolve by N:1 identity forwarding, so no code changed.
  - `web_admin_orchestrator` (23 methods, 9 deps, 89 steps) lost its five logic-holding methods to a new `web_instance_admin_orchestrator` and became a pure forwarder (18 methods, 2 deps, 19 steps), so the exemption covers it and its allow was deleted rather than reworded. Both natural homes were blocked by `maxComponentDependencies: 10` (`landscape_orchestrator` would have needed 12, `identity_orchestrator` 11) — reported, not absorbed. The five moved verbatim into the file they already lived in, so not a line of code moved. `removeUnit` keeps a step allow in its new home, reworded to say the disposition IS the workflow: 4 allows deleted, 1 moved.
  - Proof: the fixture dump moved 2086 to 2096 with every line accounted for — one message string changed (the rule's new clause) and ten added by the new control fixture, all background MISSING_SOURCE_PATH / UNUSED_COMPONENT noise that family already produces. Zero removed. 3316 unit tests; wairon's own tree 0 findings before and after.
  - **Side finding, not acted on:** `src/server/webadmin.ts` exports `setSecret` and `web.ts` routes `adminSetSecret` to it, but neither appears anywhere in the spec tree. Had it been specified, the new component would have split cleanly into an organization component and a two-method secret component, and no naming compromise would have been needed.
  - **Wave 2, the last 9 allows:** 7 EXCESSIVE_NARRATIVE_STEPS (`cli_runner.runPack` 26, `core_orchestrator.renameComponent` 30, `host_request_orchestrator.handle` 59, `identity_orchestrator.completeSsoLogin` 26, `project_policy_orchestrator.initializeProjectWithProfile` 29 and `reconcileProjectPolicy` 34, `rule_registry.registerBuiltinRules` 89, `spec_validator.validateSddTree` 26) and the 2 remaining NARRATIVE_COMPLEXITY (`host_request_orchestrator.handle` 25, `narrative_graph_projector.walk` 21).
    - **Wave 2a done (base 0ea05ef): the six step allows outside `host_request_orchestrator`, 4 deleted and 2 reworded.** Every one of the seven methods scored 9 or less, so this was length. Five are answered by naming a phase the code already had, and four of the six new contract methods bind a function that was already in the source: `identity_orchestrator.completeSsoLogin` 26→20 was inlining `resolveEnabledProvider` and `appendAuditBestEffort`, which its own contract already declares and which `mintSelfToken`/`revokeSelfToken`/`startSsoLogin` were written against (spec-only, no code changed); `createGovernedProject` (22, `symbol: performInit`) takes the body `initializeProjectWithProfile` 29→6 and `executeApprovedInit` 25→2 had each narrated in full — and the two copies had already drifted in 3 of 20 step descriptions, one about who `selectedBy` is stamped from; `reconcileProjectPolicy` 34→24 hands its profile half to a new `repairGoverningProfile` (9) plus a `appendAuditBestEffort` binding this file's own `tryAppendAudit`; `renameComponent` 30→17 hands the move to `moveRenamedSpecs` (15); `validateSddTree` 26→25 names `resolveThroughParent` (4). Two code extractions only (`repairGoverningProfile` in policy.ts, `moveRenamedSpecs` in provision.ts), both behaviour-preserving; the rest is spec catching up with code. Proof: every moved block diffed field by field against HEAD and identical after relocating jump targets — 20, 8, 14 and 1 steps. The one exception is `validateSddTree`'s chained-parent step, whose single paragraph blended the callee's walk and verdict with the caller's gate and merge; each clause moved to the side that performs it, with none dropped.
      - **Two allows kept, reworded, because the count is a list not a job.** `registerBuiltinRules` (89): the function is three statements and steps 1/88/89 say so; the other 86 are one `register` seed per built-in rule — the catalog `tests/core/rule-catalog.test.ts` pins in order to `SDD_RULES`. Splitting per family would split no code and would reorder the run sequence: the eight families occur in THIRTEEN non-contiguous blocks of `SDD_RULES`, integrity alone in three (positions 1–7, 59–61, 86). `runPack` (26): a flat twelve-arm if-chain, two steps per `pack` subcommand; all twelve arms call one module and every subcommand's action calls `runPack` directly, so sub-dispatchers would be invented. The same shape sits at every size in the runner — runPacks 8, runAgent 9, runSurface 12, runRemote 14.
      - **The phased design's own candidate was measured and declined here.** The shared refusal block of `renameComponent`/`renameMethod` is real DRY, but it is worth four steps and would have left `renameComponent` at 26; what carries the length is the spec-moving tail. It belongs to the DRY pass, not to a step-count wave.
      - **Two new value objects** for the extracted returns: `ParentResolution` (spec_validator) and `ProfileRepair` (sdd_host). No dependency cap was touched — a method calling its own component adds no edge, which is what kept `project_policy_orchestrator` at 9. 3316 unit / 21 e2e; tree 0 findings before and after.
      - **Wave 2b done (base 98178bf): the last two severe narratives, and the last two NARRATIVE_COMPLEXITY allows.** `host_request_orchestrator.handle` (59/25 to 21/5) was inlining two exported functions of `src/server/request.ts` that `ihost_request_orchestrator` never declared: `dispatchProjectLifecycleTool` (steps 13-46, the sixteen-arm hosted tool table, 38 steps / 18) and `auditToolCall` (steps 54-58, 6 steps / 1). Spec-only, one new value object (`McpToolResponse`), dependencies untouched at 9. Its `EXCESSIVE_NARRATIVE_STEPS` allow is reworded and moves to the tool table: splitting the table by owning orchestrator would invent three sub-dispatchers where the file has one switch, and would have to invent a `default` arm for each, where today the single default IS `sdd_host_get_approval_status` - so two of the three would be unreachable code deciding what an impossible tool name does. `narrative_graph_projector.walk` (25/21 to 20/15) hands the per-step edge derivation to `stepEdges` (8/1), a real extraction: the file held one exported function and `methodKey`. The `narrative_step` type method was considered and REFUSED - the derivation needs the whole tree's components to route a `dispatch` step, takes the walk's `followRegisterEdges` policy, and IS the register-handoff doctrine that needs an L5 narrative a type method cannot carry; the reasoning is written into the component description. The gate phase (steps 1-12) was NOT made a fourth method: no function exists for it and `handle` is already 21/5. Proof: 56 of 59 and 18 of 25 steps byte-identical after relocating jump targets; the 3 that are not are the seam (one branch blending the callee's classification with the caller's dispatch, two jumps the method boundary replaced), partitioned clause by clause with none dropped, checked programmatically. `stepEdges` agrees with the pre-extraction inner loop on all 384 step shapes. 3316 unit / 21 e2e; `validateAsComplete` 0 findings. NOTHING IN THE TREE IS SEVERE ANY MORE. Four `EXCESSIVE_NARRATIVE_STEPS` allows remain, all reasoned and permanent: `runPack` 26, `removeUnit` 37, `dispatchProjectLifecycleTool` 38, `registerBuiltinRules` 89. Awaiting the human lock - editing a draft-status component surfaces its DRAFT_COMPONENT_WARNING until then (--ci waived).
- [x] D2b, what still carries an allow after those:
  - 33 stutter renames across 17 interfaces (METHOD_REPEATS_COMPONENT);
  - 7 one-method components folded or renamed, `mcp_server` also dropping "Manager" from its name;
  - the 24 severe narratives split, the doctrine's own rule families first, and the 21 above 25 steps;
  - `project_ops_orchestrator`, `web_admin_orchestrator` and `web_orchestrator` split along the groups the finding names; `cli_runner` and `core_orchestrator` keep their facade allows;
  - the 7 registry renames are DONE, in D2a;
  - **DONE: the 12 draft components are `complete`** (36 specs: each component with its interface and its implementation - `conformance_rules`, `doctrine_rules`, `extension_rules`, `heuristic_rules`, `integrity_rules`, `intrinsic_rules`, `narrative_rules`, `wiring_rules`, `gate_identity` and `narrative_graph_projector` in `sdd_validator`; `backup_schedule` and `instance_bootstrap` in `sdd_host`). Nothing else in the tree is draft or design any more, so `--ci` waives nothing: a completeness regression in those rule families is now an error, not a downgraded warning. It cost nothing - `validate` and `validate --ci` were at 0 findings before and after, no allow was added, and `status` (with `updatedAt`) was the only field that moved on any of the 36. 3316 unit / 21 e2e.

**D2b is COMPLETE.** Its one remaining loose thread is the second clause of the carried rule-fix item above - the draft check is a `subsystem_spec.isDraft()` type method, but there is no equivalent on the interface side (and no `DRAFT_INTERFACE_WARNING` at all), so that clause is not delivered and is not an allow; it belongs wherever the interface-side draft question is next taken up.

### Data-model PR — entities as table schemas (decided by Robbe 2026-09-15; proposal E10)
- [ ] One type per entity with persistence metadata (`database`, `table`, a per-field `column`, `transient`); relations stated on fields with derived foreign keys — other aggregates by id, value objects embedded, owned collections as child tables, many-to-many join tables derived; `linkedEntity`, `references` and `key: foreign` retired
- [ ] Each table-backed entity belongs to exactly one Store, bound to its database
- [ ] Read models derived explicitly (`derivedFrom`, a source per field) instead of `extends`; polymorphism through a discriminator plus one owned value object per variant
- [ ] `wairon produce schema --database <id> --target postgres|prisma|efcore`; migrations stay with the ORM
- [ ] Checks: unknown database or table, a table without a primary key, an unresolved relation, cross-aggregate navigation, a table owned by zero or two Stores, read-model drift, spec-to-ORM-schema drift

### PR A — truthful authoring tools (PARKED at 9cd939c on feat/truthful-authoring-tools; rebases onto the rule model PR)
- [x] L1/L2 (approved 2026-09-13): new subsystem `sdd_authoring` (authoring_portal, authoring_orchestrator, authoring_core_adapter, authoring_validator_adapter); `mcp_authoring_adapter` in sdd_mcp
- [ ] L3 (authored 2026-09-13, awaiting approval): `writeSpec`/`updateSpec`/`deleteSpec` returning a structured `SpecChangeReport`; types `spec_delta` (`applyTo`), `spec_delta_application`, `spec_change`, `spec_change_report`, `candidate_verdict`, `candidate_options`, `doctor_options`; `validateComponentCandidate` on validator_portal and spec_validator; core, spec loader, registry and index type/delete methods; mcp_portal `sdd_update_spec`, `sdd_get_spec` (methods filter), `sdd_delete_spec`, `sdd_add_type`, the `sdd_set_*` tools and `status` on the create tools; `cli_runner.runDoctor`. On rebase, re-add `narrative_step.foreignFields(): string[]` (removed from the schema PR; its per-type field table is A3's)
- [ ] L4/L5 narratives
- [ ] Code:
  - A1 a write that changes nothing writes nothing and says so; a key with no effect at its depth refuses the write
  - A2 `[]` and `unset` at every level
  - A3 a type change rebuilds the step, keeping description and label
  - A4 `FOREIGN_STEP_FIELD` (warning) with `doctor --fix`, and the 7 stale steps in wairon's tree fixed
  - A5 delta order documented; a label retargets an existing jump
  - A6 step delete guards
  - A7 arrays inside elements merge by identity
  - A8 `T | null` param types documented; the migration checked against unions
  - A9 `status` on the create tools; no silent demotion
  - A10 `sdd_get_spec` methods filter
  - A11 nested tool input strict
  - A12 `UNUSED_TYPE` counts type method signatures — a spec change to `wiring_rules.unusedDetection` first
  - A4 likewise starts as a spec change to `narrative_rules.narrativeStepConfig`
  - structured JSON results

### PR B — conformance that never goes quiet (NEXT)
- [ ] B1 call steps realized only by calls resolving to the target's file (`CALL_ORIGIN_UNRESOLVED` when unresolvable)
- [ ] B2 `METHOD_BODY_NOT_FOUND` (B3 moved to the schema PR)
- [ ] B4 `UNDECLARED_COLOCATED_CALL` (unit-level); B5 intent `calls`, reach-everything fallback removed
- [ ] B6 `sourcePath`/`symbol` on types; B7 site-precise lint allows, existing allows rewritten
- [ ] B8 opt-in source roots with `UNCLAIMED_SOURCE_FILE`

### PR C — delegation (NEXT)
- [ ] C1 `sdd_validate_tree` on a worktree root; C2 tests to revisit on a contract change; C3 typecheck tests in CI
- [ ] C4 brief conventions (commits, worktrees, fences); C5 `sdd_move_methods`
- [ ] C6 every unmodelled MCP tool and validator contract modelled

### Track D — spec coverage (after PR C; does not block stage 2a) — decided by Robbe 2026-09-13
148 of 234 source files are named by no spec, none left out for a recorded reason (audit in `decisions.md`). B8's `UNCLAIMED_SOURCE_FILE` makes the list visible and stops it growing; B3 and B6 link the files already designed; each undesigned area is then designed spec-first, one at a time.
- [ ] Agent-file exporters (Claude, Gemini, custom) behind `generate`
- [ ] Realtime hub and WebSocket transport
- [ ] Permission-model migration, version stamp, demo seed
- [ ] Domains (detection, scan, add/remove) and the aliases command
- [ ] AI guide files and project context
- [ ] `wairon doctor` through modelled adapters: its direct reads of legacy spec filenames, subproject config backfill, context sync, version stamps, guide injection, skill freshness, approver text and pack diagnosis, so the reasoned allow on `cli_runner_impl` can go
- [ ] Execution profiles and budgets (with C6)
- [ ] User config and update channels, download, logger, errors, HTTP helpers
- [ ] The React app (`web/src`), with the generated canvas engine claimed under `conformance: off` — or a decision, recorded in the standard with its reason, that UI is outside SDD

## Doctrine and authoring-UX track (NEXT, after chained-subsystems stage 2) — decisions from Robbe 2026-09-13
- [x] **Authoring friction left over from 2a-0:** moved into the friction-fix programme (PR A–C) above.
- [x] **Specialist is the last resort:** superseded on 2026-09-15 by the doctrine PR in the friction programme above. Specialist retires: logic is an Orchestrator whose variant declares its dependency class, and wairon's 28 Specialists migrate.
- [ ] **Repository-first held state:**
  - adding a Repository scaffolds its Store (durability required), and the result names both;
  - members are added with `ownedBy: <repository>`;
  - a Store outside a Repository must declare `standalone: { reason }` when created, or the write is refused — this replaces `UNOWNED_STORE` + `lint.allow`;
  - the sdd-architect recipe becomes Repository-first;
  - migrate existing standalone Stores to the declaration.
- [ ] **Index → Index:** a derived-Index edge carries a written rationale (to verify with this track).
- [ ] **Later:** conformance for type methods (optional `sourcePath`/`symbol` on type specs).

schemaVersion: 1.0.0
name: Waffle-AIron
extensions:
  # wairon's own doctrine is wairon's own — nothing from a developer's global
  # packs folder (~/.wairon/packs) may govern this repo. Without this, whatever a
  # contributor happens to have installed machine-wide silently injects profiles,
  # rules, and skills here: the gate, `wairon skills list`, and the MCP
  # instructions/resources then differ per developer, and CI (which has no global
  # packs) disagrees with every local run.
  useGlobalPacks: false
targets:
  - type: claude
    outputDir: .claude/agents
    enabled: true
  - type: gemini
    outputDir: .gemini/agents
    enabled: true
  - type: agy
    outputDir: .gemini/agents
    enabled: true
  - type: cursor
    outputDir: .cursor/rules
    enabled: true
  - type: copilot
    outputDir: .github/prompts
    enabled: true
  - type: codex
    outputDir: .codex/agents
    enabled: true
rules:
  noOverlappingOwnership: true
  requireOwnedPaths: true
  complexity:
    # The hosted control-plane hub orchestrators (admin, identity, landscape)
    # legitimately coordinate up to ~10 collaborators after Phase 6 folded in
    # scoped-administration deps. A reviewed, visible cap of 10 — rather than
    # per-spec lint.allow silences — governs both the GOD_COMPONENT and
    # EXCESSIVE_DEPENDENCIES rules. Genuinely bloated components still warn.
    maxComponentDependencies: 10
    # This project IS a linter: sdd_validator's rules are dispatch ladders over
    # a closed vocabulary, where the branch count is the subject matter, not
    # tangled control flow. Judged against the shipped default (warn above
    # moderate) the rule family would warn forever, so wairon holds its own
    # tree to the next band up. The default is unchanged for everyone else,
    # and it is not the problem: of this tree's 1061 narratives, 777 are
    # linear and only 33 exceed the shipped default. A narrative that reaches
    # severe still warns here.
    cognitiveWarnAbove: complex
  # wairon's code realizes several spec-level roles inside shared modules
  # (e.g. portal+orchestrator+adapters folded into src/mcp/server.ts), so
  # per-component implementer agents would claim overlapping paths. Subsystem
  # owners own their code until the code-side split lands.
  generateComponentImplementers: false
  metaAgentTags:
    - meta
    - guardian
    - architect
    - adapter
    - portal
    - orchestrator
  enforceReproducibility: true
  # Where wairon's own source lives, and what of it no spec claims yet.
  # Declaring a source root is what opts a project into UNCLAIMED_SOURCE_FILE:
  # a project that declares none walks nothing and the check stays silent.
  conformance:
    sourceRoots:
      - src
      - web/src
    exclude:
      # The vendored Cytoscape bundle shipped with the canvas template. Not
      # ours to claim, and never will be, so it does not belong in a list
      # that only shrinks.
      - src/templates/canvas
    # Where this project keeps its tests. Declaring them is what lets a gated
    # write name the tests it just invalidated (F25) - a spec pass that changes
    # a contract and says nothing is how a brief comes to promise green tests it
    # has already broken. Separate from sourceRoots on purpose: tests are not
    # code the specs claim, and listing them there would make every test file an
    # UNCLAIMED_SOURCE_FILE.
    testRoots:
      - tests
    # The debt register, frozen. Every file here is source wairon ships and
    # no spec names — F17, written down where a reviewer sees the whole list
    # instead of discovering it one file at a time. It can only SHRINK: claim
    # a file with a sourcePath and delete its line, and an entry that is
    # already claimed, a proven re-export barrel, or gone is reported
    # (STALE_UNCLAIMED_ENTRY). A file that is neither claimed nor listed is
    # reported the day it appears, which is the whole point.
    unclaimed:
      # Constants — the supported aliases, the version, the default target list.
      # 16 importers and nothing to call.
      - src/config/defaults.ts
      - src/core/demo-seed.ts
      - src/core/narrative-labels.ts
      # Stereotype predicates shared by three doctrine rules that are themselves
      # modelled. Rule-family vocabulary.
      - src/core/rules/doctrine/stereotype-terms.ts
      # Assertion selectors shared by three extension rules that are themselves
      # modelled. Rule-family vocabulary.
      - src/core/rules/extension/declared-assertion.ts
      # The rule barrel and the context every rule reads. 97 importers, and every rule
      # it assembles IS modelled — 93 of the 100 rule files carry a spec. Modelling the
      # assembly point as well would put a component between each rule and its own
      # registration. See read-model.ts, which is its substrate and unclaimed for the
      # same reason.
      - src/core/rules/index.ts
      # Graph helpers shared by three narrative cycle rules that are themselves
      # modelled. Rule-family vocabulary.
      - src/core/rules/narrative/completed-step-graph.ts
      # The analysis substrate rules/index.ts builds the context from: the code index,
      # the import graph, the realization and ownership indexes. Exactly one importer,
      # and that importer is the barrel above. Claiming this without claiming the
      # barrel would leave all seven builders with no modelled caller.
      - src/core/rules/read-model.ts
      # Invariant reference parsing shared by two wiring rules that are themselves
      # modelled. Rule-family vocabulary.
      - src/core/rules/wiring/invariant-ref.ts
      # The package entry. It re-exports the core Portal and names the documented
      # embedding API that no contract covers — validateSddTree, loadExtensions and
      # their neighbours, which docs/extending-wairon.md documents and
      # examples/wrapper/wrapper.js calls. A library surface, not a component.
      - src/index.ts
      # sdd_host's HTTP error shapes, 39 importers inside that subsystem. Vocabulary
      # for replies, not a component that answers anything.
      - src/server/errors.ts
      # Request and response plumbing shared by the hosted handlers — reading a body,
      # writing a status. The handlers are modelled; this is how they speak HTTP.
      - src/server/httpio.ts
      - src/server/migration.ts
      - src/server/realtime.ts
      - src/server/swagger.ts
      - src/server/websocket.ts
      # Deterministic serialization for hashing. Pure, and the gate identity depends
      # on it being exactly one implementation rather than a component someone could
      # reasonably swap.
      - src/utils/canonical-json.ts
      # A file download helper for `wairon update`. Two importers, wraps one fetch.
      - src/utils/download.ts
      # The error types every subsystem throws and catches. 39 importers, no state,
      # no decisions: modelling it would give all 39 a declared edge saying only
      # "this one throws errors". Same shape as fs.ts below.
      - src/utils/errors.ts
      # Filename slugging. Pure.
      - src/utils/filenames.ts
      # An interactive prompt widget. One importer, and it decides nothing about this
      # system — it asks a person a question.
      - src/utils/filteredCheckbox.ts
      # The shared yaml and fs helpers. The spec file store used to claim both
      # as its own sourcePaths, which was false: it is its own module and these
      # are utilities many modules use. Carried here rather than left on a spec
      # that does not hold them.
      - src/utils/fs.ts
      # Split out of config/loader.ts so that file could be one job. Kept
      # unclaimed ON PURPOSE: nineteen modules resolve paths through it, it
      # holds no state and decides nothing, and modelling it as a component
      # was tried and measured - nineteen components then needed a declared
      # edge saying only "this one resolves paths". Same shape as fs.ts above.
      - src/config/paths.ts
      # Terminal output, 26 importers. A component here would be on the dependency
      # list of nearly every CLI narrative and would tell a reader nothing.
      - src/utils/logger.ts
      # Semver comparison. Pure arithmetic over strings.
      - src/utils/version.ts
      # Parse and serialize, 16 importers. Deliberately NOT the spec file store: it
      # holds the syntax, the store holds the decisions about files.
      - src/utils/yaml.ts
      # The React SPA under web/. It was outside `sourceRoots` entirely until now,
      # so it was not tracked debt — it was invisible, and could grow without any
      # gate saying so. Listing it does not excuse it: these enter the ratchet like
      # everything else and the list only shrinks. web/src/canvas is GENERATED by
      # scripts/gen-canvas-engine.mjs and will be claimed by its owner under
      # `conformance: off` when the diagram components are designed; it sits here
      # until then rather than being excluded, because generated code wairon itself
      # produces is ours to claim, not somebody else’s to ignore.
      - web/src/App.tsx
      - web/src/HeaderMenu.tsx
      - web/src/RealtimeStatus.tsx
      - web/src/ThemeControls.tsx
      - web/src/api.ts
      - web/src/canvas/engine.ts
      - web/src/canvas/exporters.ts
      - web/src/canvas/skeleton.ts
      - web/src/canvas/styles.ts
      - web/src/components/GitCredentialCard.tsx
      - web/src/components/InfoTip.tsx
      - web/src/components/ThemeMenu.tsx
      - web/src/components/UnitSelect.tsx
      - web/src/main.tsx
      - web/src/realtime.tsx
      - web/src/session.tsx
      - web/src/settings.tsx
      - web/src/theme/canvasBridge.ts
      - web/src/theme/colorUtils.ts
      - web/src/theme/themes.ts
      - web/src/types.ts
      - web/src/ui.tsx
      - web/src/views/Approvals.tsx
      - web/src/views/Audit.tsx
      - web/src/views/CanvasRouter.tsx
      - web/src/views/CanvasView.tsx
      - web/src/views/Environment.tsx
      - web/src/views/Health.tsx
      - web/src/views/Home.tsx
      - web/src/views/Instance.tsx
      - web/src/views/Login.tsx
      - web/src/views/Permissions.tsx
      - web/src/views/ProjectOps.tsx
      - web/src/views/Projects.tsx
      - web/src/views/Providers.tsx
      - web/src/views/Roles.tsx
      - web/src/views/Sharing.tsx
      - web/src/views/SpecsEditor.tsx
      - web/src/views/ThemeBuilder.tsx
      - web/src/views/Tokens.tsx
      - web/src/views/Units.tsx
      - web/src/views/Users.tsx
    # The conformance debt register: the code-vs-spec findings this tree carries
    # as DECLARED debt, grouped by the reason that explains them. `unclaimed`'s
    # one-way shape, for findings about code a spec does name.
    #
    # It is not a second lint.allow. An allow says "this finding is wrong here,
    # by design", and means to live forever; an entry here says "this finding is
    # RIGHT, and it is not paid yet". Only a CARRYABLE code may appear (anything
    # else is an error), an entry carries a finding only when it lists EVERY
    # unit that finding reports (so an aggregating finding cannot grow behind
    # it), and an entry that stops applying is STALE_CARRIED_FINDING — so the
    # register can only shrink, and `wairon validate` prints what is left.
    #
    # `kind` is what makes it a register rather than a suppression list:
    # drift is spec-vs-code disagreement an author fixes, undecided is a
    # modelling decision nobody has taken, unreadable is a shape the analysis
    # cannot follow and there is nothing in the tree to fix for it.
    carried:
      # 45 finding(s), 60 unit(s).
      - kind: drift
        why: >-
          A spec names a source file that only USES the function, and the code that holds it lives
          somewhere else. Both checks prove it rather than guess: the call resolves, and it
          resolves outside the target's own file; the method name is declared, and the declaration
          is an import or a destructured binding with no body under it. Two shapes. One, the thin
          "core adapter" components (mcp_core_adapter, validator_core_adapter,
          surfaces_core_adapter, cli_core_adapter, authoring_core_adapter and their neighbours)
          are realized by the file that CONSUMES the core API; that file imports the functions and
          neither writes nor republishes them, so every step routed through such an adapter lands
          in src/core/specs.ts or src/core/extensions.ts instead. Two, a portal's methods are
          claimed by one file and written in another: admin_portal is realized by
          src/server/http.ts while createProject and its siblings live in src/server/admin.ts.
          Paying it means pointing each spec at the module that holds the code, or narrating the
          target the code actually reaches. Either way an author's edit, not a limit of the
          reader.
        revisit: >-
          Challenge the first shape hardest: "the thin core adapter components are realized by the
          file that CONSUMES the core API". If that is true the fix is to point each adapter spec
          at the module that holds the code, and every finding here is real drift. If those
          adapters instead MODEL the consumption itself - a component whose whole job is to reach
          the core API from this file - then the specs are right, the narratives name the wrong
          target, and part of this group is a different finding than it claims. Nobody has
          measured which, and 46 findings ride on the answer.
        findings:
          - code: CALL_STEP_UNREALIZED
            spec: authoring_orchestrator_impl
            at: 'addComponent'
            covers:
              - '2:authoring_validator_adapter.validateComponentCandidate'
              - '5:authoring_core_adapter.saveComponentSpec'
          - code: CALL_STEP_UNREALIZED
            spec: authoring_orchestrator_impl
            at: 'updateSpecGated'
            covers:
              - '3:authoring_core_adapter.updateSpec'
              - '5:authoring_validator_adapter.findTestsReferencing'
          - code: CALL_STEP_UNREALIZED
            spec: cli_host_adapter_impl
            at: 'createProject'
            covers:
              - '1:admin_portal.createProject'
          - code: CALL_STEP_UNREALIZED
            spec: cli_host_adapter_impl
            at: 'destroyProject'
            covers:
              - '1:admin_portal.destroyProject'
          - code: CALL_STEP_UNREALIZED
            spec: cli_host_adapter_impl
            at: 'listKeys'
            covers:
              - '1:admin_portal.listKeys'
          - code: CALL_STEP_UNREALIZED
            spec: cli_host_adapter_impl
            at: 'listProjects'
            covers:
              - '1:admin_portal.listProjects'
          - code: CALL_STEP_UNREALIZED
            spec: cli_host_adapter_impl
            at: 'lockProject'
            covers:
              - '1:admin_portal.lockProject'
          - code: CALL_STEP_UNREALIZED
            spec: cli_host_adapter_impl
            at: 'mintKey'
            covers:
              - '1:admin_portal.mintKey'
          - code: CALL_STEP_UNREALIZED
            spec: cli_host_adapter_impl
            at: 'revokeKey'
            covers:
              - '1:admin_portal.revokeKey'
          - code: CALL_STEP_UNREALIZED
            spec: cli_runner_impl
            at: 'runDoctor'
            covers:
              - '5:cli_skills_adapter.exportSddSkills'
              - '9:cli_mcp_adapter.installMcpServer'
          - code: CALL_STEP_UNREALIZED
            spec: cli_runner_impl
            at: 'runSubsystemExternalize'
            covers:
              - '4:cli_core_adapter.externalizeSubsystem'
          - code: CALL_STEP_UNREALIZED
            spec: cli_runner_impl
            at: 'runSubsystemInternalize'
            covers:
              - '2:cli_core_adapter.internalizeSubsystem'
          - code: CALL_STEP_UNREALIZED
            spec: cli_runner_impl
            at: 'runSubsystemMove'
            covers:
              - '4:cli_core_adapter.moveSubsystemProject'
          - code: CALL_STEP_UNREALIZED
            spec: cli_runner_impl
            at: 'runSurface'
            covers:
              - '10:cli_surfaces_client_adapter.pinFamilySurfaces'
              - '2:cli_surfaces_client_adapter.exportSurface'
              - '4:cli_surfaces_client_adapter.importSurface'
              - '6:cli_surfaces_client_adapter.listSnapshots'
              - '8:cli_surfaces_client_adapter.listExternalInterfaces'
          - code: CALL_STEP_UNREALIZED
            spec: cli_validator_adapter_impl
            at: 'runValidation'
            covers:
              - '3:cli_validator_adapter.validateRegistry'
              - '4:cli_validator_adapter.validateProjectConfig'
              - '5:cli_validator_adapter.validateSddTree'
          - code: CALL_STEP_UNREALIZED
            spec: mcp_server_impl
            at: 'create'
            covers:
              - '9:mcp_core_adapter.resolveChainingParent'
          - code: CALL_STEP_UNREALIZED
            spec: spec_validator_impl
            at: 'computeGateStateId'
            covers:
              - '2:validator_core_adapter.loadProjectExtensions'
          - code: CALL_STEP_UNREALIZED
            spec: spec_validator_impl
            at: 'listRules'
            covers:
              - '1:validator_core_adapter.loadProjectExtensions'
          - code: CALL_STEP_UNREALIZED
            spec: spec_validator_impl
            at: 'validateAsComplete'
            covers:
              - '11:validator_core_adapter.dryRunSerializeSpecs'
              - '2:validator_core_adapter.loadSystemSpec'
              - '3:validator_core_adapter.loadSubsystemSpecs'
              - '4:validator_core_adapter.loadComponentSpecs'
              - '5:validator_core_adapter.loadInterfaceSpecs'
              - '6:validator_core_adapter.loadImplementationSpecs'
              - '7:validator_core_adapter.loadTypeSpecs'
              - '8:validator_surfaces_adapter.loadSurfaceSnapshots'
              - '9:validator_surfaces_adapter.loadMountSurfaceSnapshots'
          - code: CALL_STEP_UNREALIZED
            spec: spec_validator_impl
            at: 'validateSddTree'
            covers:
              - '13:validator_core_adapter.loadProjectExtensions'
              - '14:validator_core_adapter.dryRunSerializeSpecs'
              - '2:validator_core_adapter.loadSystemSpec'
              - '3:validator_core_adapter.loadSubsystemSpecs'
              - '4:validator_core_adapter.loadComponentSpecs'
              - '5:validator_core_adapter.loadInterfaceSpecs'
              - '6:validator_core_adapter.loadImplementationSpecs'
              - '7:validator_core_adapter.loadTypeSpecs'
              - '8:validator_surfaces_adapter.loadSurfaceSnapshots'
              - '9:validator_surfaces_adapter.loadMountSurfaceSnapshots'
          - code: CALL_STEP_UNREALIZED
            spec: surface_orchestrator_impl
            at: 'listExternalInterfaces'
            covers:
              - '2:surfaces_core_adapter.resolveChainingParent'
          - code: CALL_STEP_UNREALIZED
            spec: surface_orchestrator_impl
            at: 'listMountSnapshots'
            covers:
              - '2:surfaces_core_adapter.resolveSubprojectForNamespace'
          - code: CALL_STEP_UNREALIZED
            spec: surface_orchestrator_impl
            at: 'pinFamilySurfaces'
            covers:
              - '1:surfaces_core_adapter.resolveChainingParent'
              - '5:surfaces_core_adapter.loadSubsystemSpecs'
          - code: CALL_STEP_UNREALIZED
            spec: surface_projector_impl
            at: 'projectOwnSurface'
            covers:
              - '1:surfaces_core_adapter.loadSystemSpec'
              - '2:surfaces_core_adapter.loadSubsystemSpecs'
              - '3:surfaces_core_adapter.loadComponentSpecs'
              - '4:surfaces_core_adapter.loadInterfaceSpecs'
              - '5:surfaces_core_adapter.loadTypeSpecs'
          - code: CALL_STEP_UNREALIZED
            spec: surface_projector_impl
            at: 'projectSubsystemSurface'
            covers:
              - '1:surfaces_core_adapter.loadSystemSpec'
              - '2:surfaces_core_adapter.loadSubsystemSpecs'
              - '3:surfaces_core_adapter.loadComponentSpecs'
              - '4:surfaces_core_adapter.loadInterfaceSpecs'
              - '5:surfaces_core_adapter.loadTypeSpecs'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'createChainedSubsystem'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'externalizeSubsystem'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'internalizeSubsystem'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'loadComponentSpec'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'loadImplementationSpec'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'loadTypeSpec'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'loadInterfaceSpec'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'loadSubsystemSpec'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'loadSubsystemSpecs'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'loadSystemSpec'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'moveSubsystemProject'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'saveImplementationSpec'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'saveInterfaceSpec'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'saveSubsystemSpec'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'saveTypeSpec'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_core_adapter_impl
            at: 'saveSystemSpec'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_orchestrator_impl
            at: 'externalizeSubsystem'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_orchestrator_impl
            at: 'internalizeSubsystem'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_orchestrator_impl
            at: 'moveSubsystemProject'
          - code: METHOD_BODY_NOT_FOUND
            spec: mcp_validator_adapter_impl
            at: 'validateSddTree'
      # 107 finding(s), 144 unit(s).
      - kind: drift
        why: >-
          A narrative does not declare a call the code makes across a component boundary that
          happens to live inside one file. The components and the boundary are right; only the
          narrative is silent, and because nothing is imported no file-level check can see the
          hop. Paying it means adding the call step (or moving the code so the boundary is a real
          module edge), one method at a time.
        findings:
          - code: UNDECLARED_COLOCATED_CALL
            spec: authoring_portal_impl
            at: 'addComponent'
            covers:
              - 'authoring_core_adapter.loadProjectConfig'
          - code: UNDECLARED_COLOCATED_CALL
            spec: authoring_portal_impl
            at: 'updateSpecGated'
            covers:
              - 'authoring_core_adapter.loadProjectConfig'
          - code: UNDECLARED_COLOCATED_CALL
            spec: authoring_portal_impl
            at: 'moveMethods'
            covers:
              - 'authoring_core_adapter.loadProjectConfig'
          - code: UNDECLARED_COLOCATED_CALL
            spec: cli_runner_impl
            at: 'runRemote'
            covers:
              - 'remote_orchestrator.listStoredInstances'
              - 'remote_orchestrator.resolveTarget'
          - code: UNDECLARED_COLOCATED_CALL
            spec: cli_runner_impl
            at: 'runSubsystemAdd'
            covers:
              - 'cli_core_adapter.loadSystemSpec'
          - code: UNDECLARED_COLOCATED_CALL
            spec: core_orchestrator_impl
            at: 'computeStateIdAt'
            covers:
              - 'spec_loader.loadSystemSpec'
          - code: UNDECLARED_COLOCATED_CALL
            spec: core_orchestrator_impl
            at: 'saveComponentSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: core_orchestrator_impl
            at: 'saveImplementationSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: core_orchestrator_impl
            at: 'saveInterfaceSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: core_orchestrator_impl
            at: 'saveSubsystemSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: core_orchestrator_impl
            at: 'saveSystemSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: core_orchestrator_impl
            at: 'saveTypeSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: core_orchestrator_impl
            at: 'deleteSubsystemSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: core_orchestrator_impl
            at: 'deleteComponentSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: core_orchestrator_impl
            at: 'deleteInterfaceSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: core_orchestrator_impl
            at: 'deleteImplementationSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: core_orchestrator_impl
            at: 'deleteTypeSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: credential_repository_impl
            at: 'create'
            covers:
              - 'credential_store.read'
              - 'credential_store.write'
          - code: UNDECLARED_COLOCATED_CALL
            spec: credential_repository_impl
            at: 'findByToken'
            covers:
              - 'credential_store.read'
          - code: UNDECLARED_COLOCATED_CALL
            spec: credential_repository_impl
            at: 'list'
            covers:
              - 'credential_store.read'
          - code: UNDECLARED_COLOCATED_CALL
            spec: credential_repository_impl
            at: 'listByOwner'
            covers:
              - 'credential_store.read'
          - code: UNDECLARED_COLOCATED_CALL
            spec: credential_repository_impl
            at: 'revoke'
            covers:
              - 'credential_store.read'
              - 'credential_store.write'
          - code: UNDECLARED_COLOCATED_CALL
            spec: credential_repository_impl
            at: 'revokeAllForOwner'
            covers:
              - 'credential_store.read'
              - 'credential_store.write'
          - code: UNDECLARED_COLOCATED_CALL
            spec: extension_orchestrator_impl
            at: 'diagnoseProjectPacks'
            covers:
              - 'pack_source_adapter.discoverPacks'
              - 'pack_source_adapter.globalPacksDir'
          - code: UNDECLARED_COLOCATED_CALL
            spec: git_backing_orchestrator_impl
            at: 'bindScope'
            covers:
              - 'git_backing_repository.listBindings'
          - code: UNDECLARED_COLOCATED_CALL
            spec: git_backing_orchestrator_impl
            at: 'runPeriodicBackingSync'
            covers:
              - 'git_backing_repository.touchSync'
          - code: UNDECLARED_COLOCATED_CALL
            spec: identity_portal_impl
            at: 'completeSsoLogin'
            covers:
              - 'identity_orchestrator.appendAuditBestEffort'
              - 'identity_orchestrator.resolveEnabledProvider'
          - code: UNDECLARED_COLOCATED_CALL
            spec: identity_portal_impl
            at: 'mintToken'
            covers:
              - 'identity_orchestrator.appendAuditBestEffort'
          - code: UNDECLARED_COLOCATED_CALL
            spec: identity_portal_impl
            at: 'pruneAuditEvents'
            covers:
              - 'identity_orchestrator.appendAuditBestEffort'
          - code: UNDECLARED_COLOCATED_CALL
            spec: identity_portal_impl
            at: 'removeIdentityProvider'
            covers:
              - 'identity_orchestrator.appendAuditBestEffort'
          - code: UNDECLARED_COLOCATED_CALL
            spec: identity_portal_impl
            at: 'revokeToken'
            covers:
              - 'identity_orchestrator.appendAuditBestEffort'
          - code: UNDECLARED_COLOCATED_CALL
            spec: identity_portal_impl
            at: 'setUserStatus'
            covers:
              - 'identity_orchestrator.appendAuditBestEffort'
          - code: UNDECLARED_COLOCATED_CALL
            spec: identity_portal_impl
            at: 'startSsoLogin'
            covers:
              - 'identity_orchestrator.appendAuditBestEffort'
              - 'identity_orchestrator.resolveEnabledProvider'
          - code: UNDECLARED_COLOCATED_CALL
            spec: identity_portal_impl
            at: 'upsertIdentityProvider'
            covers:
              - 'identity_orchestrator.appendAuditBestEffort'
          - code: UNDECLARED_COLOCATED_CALL
            spec: identity_portal_impl
            at: 'upsertUser'
            covers:
              - 'identity_orchestrator.appendAuditBestEffort'
          - code: UNDECLARED_COLOCATED_CALL
            spec: landscape_portal_impl
            at: 'generateLandscape'
            covers:
              - 'landscape_graph.build'
          - code: UNDECLARED_COLOCATED_CALL
            spec: operations_portal_impl
            at: 'evaluateQuota'
            covers:
              - 'diagnostics.collectUsage'
              - 'quota_rules.evaluateUsage'
          - code: UNDECLARED_COLOCATED_CALL
            spec: operations_portal_impl
            at: 'getHealthReport'
            covers:
              - 'diagnostics.buildHealthReport'
              - 'diagnostics.collectUsage'
              - 'diagnostics.runChecks'
          - code: UNDECLARED_COLOCATED_CALL
            spec: operations_portal_impl
            at: 'getUsage'
            covers:
              - 'diagnostics.collectUsage'
          - code: UNDECLARED_COLOCATED_CALL
            spec: pack_orchestrator_impl
            at: 'listGlobalPacks'
            covers:
              - 'pack_store.storeListGlobalPacks'
          - code: UNDECLARED_COLOCATED_CALL
            spec: permission_repository_impl
            at: 'getAssignment'
            covers:
              - 'permission_store.load'
          - code: UNDECLARED_COLOCATED_CALL
            spec: permission_repository_impl
            at: 'listAssignments'
            covers:
              - 'permission_store.load'
          - code: UNDECLARED_COLOCATED_CALL
            spec: permission_repository_impl
            at: 'remapScope'
            covers:
              - 'permission_store.load'
              - 'permission_store.replaceAll'
          - code: UNDECLARED_COLOCATED_CALL
            spec: permission_repository_impl
            at: 'removeAssignment'
            covers:
              - 'permission_store.load'
              - 'permission_store.replaceAll'
          - code: UNDECLARED_COLOCATED_CALL
            spec: permission_repository_impl
            at: 'removeAssignmentsForScopes'
            covers:
              - 'permission_store.load'
              - 'permission_store.replaceAll'
          - code: UNDECLARED_COLOCATED_CALL
            spec: permission_repository_impl
            at: 'setAssignment'
            covers:
              - 'permission_store.load'
              - 'permission_store.replaceAll'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_index_impl
            at: 'resolveBinding'
            covers:
              - 'project_store.read'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_index_impl
            at: 'resolveRoot'
            covers:
              - 'project_repository.resolveBinding'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_policy_orchestrator_impl
            at: 'createGovernedProject'
            covers:
              - 'policy_store.load'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_policy_orchestrator_impl
            at: 'evaluateInitRequest'
            covers:
              - 'policy_store.load'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_policy_orchestrator_impl
            at: 'evaluateProjectPolicy'
            covers:
              - 'policy_store.load'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_policy_orchestrator_impl
            at: 'getPackPolicy'
            covers:
              - 'policy_store.load'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_policy_orchestrator_impl
            at: 'reconcileProjectPolicy'
            covers:
              - 'policy_store.load'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_policy_orchestrator_impl
            at: 'setPackPolicy'
            covers:
              - 'policy_store.replaceActive'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_policy_portal_impl
            at: 'evaluateProjectPolicy'
            covers:
              - 'policy_store.load'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_policy_portal_impl
            at: 'getPackPolicy'
            covers:
              - 'policy_store.load'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_policy_portal_impl
            at: 'initializeProjectWithProfile'
            covers:
              - 'project_policy_orchestrator.createGovernedProject'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_policy_portal_impl
            at: 'reconcileProjectPolicy'
            covers:
              - 'policy_store.load'
              - 'project_policy_orchestrator.appendAuditBestEffort'
              - 'project_policy_orchestrator.repairGoverningProfile'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_policy_portal_impl
            at: 'setPackPolicy'
            covers:
              - 'policy_store.replaceActive'
              - 'project_policy_orchestrator.appendAuditBestEffort'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_repository_impl
            at: 'create'
            covers:
              - 'project_store.read'
              - 'project_store.write'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_repository_impl
            at: 'list'
            covers:
              - 'project_store.read'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_repository_impl
            at: 'remove'
            covers:
              - 'project_store.read'
              - 'project_store.write'
          - code: UNDECLARED_COLOCATED_CALL
            spec: project_repository_impl
            at: 'resolveBinding'
            covers:
              - 'project_store.read'
          - code: UNDECLARED_COLOCATED_CALL
            spec: role_repository_impl
            at: 'create'
            covers:
              - 'role_store.load'
              - 'role_store.replaceAll'
          - code: UNDECLARED_COLOCATED_CALL
            spec: role_repository_impl
            at: 'delete'
            covers:
              - 'role_store.load'
              - 'role_store.replaceAll'
          - code: UNDECLARED_COLOCATED_CALL
            spec: role_repository_impl
            at: 'list'
            covers:
              - 'role_store.load'
          - code: UNDECLARED_COLOCATED_CALL
            spec: role_repository_impl
            at: 'update'
            covers:
              - 'role_store.load'
              - 'role_store.replaceAll'
          - code: UNDECLARED_COLOCATED_CALL
            spec: rule_repository_impl
            at: 'knownIssueCodes'
            covers:
              - 'rule_store.list'
          - code: UNDECLARED_COLOCATED_CALL
            spec: rule_repository_impl
            at: 'registerBuiltinRules'
            covers:
              - 'rule_store.add'
              - 'rule_store.clear'
          - code: UNDECLARED_COLOCATED_CALL
            spec: rule_repository_impl
            at: 'registerPackRules'
            covers:
              - 'rule_store.add'
          - code: UNDECLARED_COLOCATED_CALL
            spec: secret_repository_impl
            at: 'list'
            covers:
              - 'secret_store.read'
          - code: UNDECLARED_COLOCATED_CALL
            spec: secret_repository_impl
            at: 'write'
            covers:
              - 'secret_store.read'
          - code: UNDECLARED_COLOCATED_CALL
            spec: skills_orchestrator_impl
            at: 'export'
            covers:
              - 'skills_core_adapter.loadProjectConfig'
              - 'skills_core_adapter.loadProjectExtensions'
          - code: UNDECLARED_COLOCATED_CALL
            spec: skills_portal_impl
            at: 'exportSddSkills'
            covers:
              - 'skills_core_adapter.loadProjectConfig'
              - 'skills_core_adapter.loadProjectExtensions'
          - code: UNDECLARED_COLOCATED_CALL
            spec: skills_portal_impl
            at: 'listResources'
            covers:
              - 'skill_resources.listSkillResources'
          - code: UNDECLARED_COLOCATED_CALL
            spec: skills_portal_impl
            at: 'readResource'
            covers:
              - 'skill_resources.listSkillResources'
              - 'skill_resources.readSkillResource'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_index_impl
            at: 'listChainedRoots'
            covers:
              - 'spec_loader.inspectChainedRoots'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_loader_impl
            at: 'saveComponentSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_loader_impl
            at: 'saveImplementationSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_loader_impl
            at: 'saveInterfaceSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_loader_impl
            at: 'saveSubsystemSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_loader_impl
            at: 'saveSystemSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_loader_impl
            at: 'saveTypeSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_loader_impl
            at: 'deleteSubsystemSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_loader_impl
            at: 'deleteComponentSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_loader_impl
            at: 'deleteInterfaceSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_loader_impl
            at: 'deleteImplementationSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_loader_impl
            at: 'deleteTypeSpec'
            covers:
              - 'spec_registry.invalidateCache'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_validator_impl
            at: 'resolveThroughParent'
            covers:
              - 'validator_portal.validateSddTree'
          - code: UNDECLARED_COLOCATED_CALL
            spec: spec_validator_impl
            at: 'validateAsComplete'
            covers:
              - 'validator_portal.validateSddTree'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_orchestrator_impl
            at: 'getSnapshot'
            covers:
              - 'surface_store.list'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_orchestrator_impl
            at: 'import'
            covers:
              - 'surface_store.put'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_orchestrator_impl
            at: 'listExternalInterfaces'
            covers:
              - 'surface_projector.projectSubsystemSurface'
              - 'surface_store.list'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_orchestrator_impl
            at: 'listMountSnapshots'
            covers:
              - 'surface_store.list'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_orchestrator_impl
            at: 'pinFamilySurfaces'
            covers:
              - 'surface_store.list'
              - 'surface_store.put'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_portal_impl
            at: 'exportSurface'
            covers:
              - 'surface_projector.projectOwnSurface'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_portal_impl
            at: 'getSnapshot'
            covers:
              - 'surface_store.list'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_portal_impl
            at: 'importSurface'
            covers:
              - 'surface_store.put'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_portal_impl
            at: 'listExternalInterfaces'
            covers:
              - 'surface_projector.projectChildSurface'
              - 'surface_projector.projectSubsystemSurface'
              - 'surface_store.list'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_portal_impl
            at: 'listMountSnapshots'
            covers:
              - 'surface_store.list'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_portal_impl
            at: 'pinFamilySurfaces'
            covers:
              - 'surface_projector.projectChildSurface'
              - 'surface_projector.projectSubsystemSurface'
              - 'surface_store.list'
              - 'surface_store.put'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_registry_impl
            at: 'hydrateFromFiles'
            covers:
              - 'surface_store.list'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_repository_impl
            at: 'getSnapshot'
            covers:
              - 'surface_store.list'
          - code: UNDECLARED_COLOCATED_CALL
            spec: surface_repository_impl
            at: 'hydrate'
            covers:
              - 'surface_store.list'
          - code: UNDECLARED_COLOCATED_CALL
            spec: user_repository_impl
            at: 'remapUnitReferences'
            covers:
              - 'user_store.load'
              - 'user_store.replaceAll'
          - code: UNDECLARED_COLOCATED_CALL
            spec: validator_portal_impl
            at: 'computeGateStateId'
            covers:
              - 'validator_core_adapter.computeStateId'
              - 'validator_core_adapter.consumedContractInputs'
              - 'validator_core_adapter.loadProjectConfig'
          - code: UNDECLARED_COLOCATED_CALL
            spec: validator_portal_impl
            at: 'validateSddTree'
            covers:
              - 'spec_validator.resolveThroughParent'
              - 'validator_core_adapter.loadProjectConfig'
      # 6 finding(s), 56 unit(s).
      - kind: undecided
        why: >-
          src/mcp/server.ts is one file modelled as five components (mcp_portal, mcp_server,
          mcp_orchestrator, mcp_core_adapter, mcp_skills_adapter), so every hop between them is a
          boundary crossing that looks local. createMcpServer alone crosses 23 of them. Narrating
          23 cross-component calls inside one function would be an unreadable narrative, and
          splitting a 2000-line server registration into five modules is a real change with real
          risk: which of the two is right is a decision nobody has taken. It is recorded here as a
          question, not as work somebody forgot.
        findings:
          - code: UNDECLARED_COLOCATED_CALL
            spec: mcp_orchestrator_impl
            at: 'listResources'
            covers:
              - 'mcp_core_adapter.composeAgentBrief'
              - 'mcp_skills_adapter.readResource'
          - code: UNDECLARED_COLOCATED_CALL
            spec: mcp_orchestrator_impl
            at: 'readResource'
            covers:
              - 'mcp_skills_adapter.listResources'
          - code: UNDECLARED_COLOCATED_CALL
            spec: mcp_portal_impl
            at: 'createMcpServer'
            covers:
              - 'mcp_core_adapter.composeAgentBrief'
              - 'mcp_core_adapter.createChainedSubsystem'
              - 'mcp_core_adapter.loadImplementationSpec'
              - 'mcp_core_adapter.loadProjectConfig'
              - 'mcp_core_adapter.loadSubsystemSpecs'
              - 'mcp_core_adapter.loadSystemSpec'
              - 'mcp_core_adapter.loadTypeSpec'
              - 'mcp_core_adapter.saveImplementationSpec'
              - 'mcp_core_adapter.saveInterfaceSpec'
              - 'mcp_core_adapter.loadInterfaceSpec'
              - 'mcp_core_adapter.loadComponentSpec'
              - 'mcp_core_adapter.saveSubsystemSpec'
              - 'mcp_core_adapter.saveSystemSpec'
              - 'mcp_core_adapter.saveTypeSpec'
              - 'mcp_orchestrator.externalizeSubsystem'
              - 'mcp_orchestrator.internalizeSubsystem'
              - 'mcp_orchestrator.moveSubsystemProject'
              - 'mcp_orchestrator.renameComponent'
              - 'mcp_orchestrator.renameMethod'
              - 'mcp_skills_adapter.buildServerInstructions'
              - 'mcp_skills_adapter.listResources'
              - 'mcp_skills_adapter.readResource'
              - 'mcp_surfaces_adapter.listExternalInterfaces'
              - 'mcp_validator_adapter.validateSddTree'
          - code: UNDECLARED_COLOCATED_CALL
            spec: mcp_portal_impl
            at: 'listResources'
            covers:
              - 'mcp_core_adapter.composeAgentBrief'
              - 'mcp_skills_adapter.listResources'
              - 'mcp_skills_adapter.readResource'
          - code: UNDECLARED_COLOCATED_CALL
            spec: mcp_portal_impl
            at: 'readResource'
            covers:
              - 'mcp_core_adapter.composeAgentBrief'
              - 'mcp_skills_adapter.listResources'
              - 'mcp_skills_adapter.readResource'
          - code: UNDECLARED_COLOCATED_CALL
            spec: mcp_server_impl
            at: 'create'
            covers:
              - 'mcp_core_adapter.composeAgentBrief'
              - 'mcp_core_adapter.createChainedSubsystem'
              - 'mcp_core_adapter.loadImplementationSpec'
              - 'mcp_core_adapter.loadProjectConfig'
              - 'mcp_core_adapter.loadSubsystemSpecs'
              - 'mcp_core_adapter.loadTypeSpec'
              - 'mcp_core_adapter.saveImplementationSpec'
              - 'mcp_core_adapter.saveInterfaceSpec'
              - 'mcp_core_adapter.loadInterfaceSpec'
              - 'mcp_core_adapter.loadComponentSpec'
              - 'mcp_core_adapter.saveSubsystemSpec'
              - 'mcp_core_adapter.saveSystemSpec'
              - 'mcp_core_adapter.saveTypeSpec'
              - 'mcp_orchestrator.externalizeSubsystem'
              - 'mcp_orchestrator.internalizeSubsystem'
              - 'mcp_orchestrator.moveSubsystemProject'
              - 'mcp_orchestrator.renameComponent'
              - 'mcp_orchestrator.renameMethod'
              - 'mcp_portal.advertiseHostedTools'
              - 'mcp_portal.readResource'
              - 'mcp_skills_adapter.listResources'
              - 'mcp_skills_adapter.readResource'
              - 'mcp_surfaces_adapter.listExternalInterfaces'
      # 12 finding(s).
      - kind: unreadable
        why: >-
          The contract is realized by a FORWARDING BINDING, and the check reads function-like
          bodies only. src/server/adapters.ts declares its adapters as objects of forwards
          (hostGit = { publish: gitPortal.publish, ... }), and src/server/http.ts realizes
          ihost_server.init as `export { bootstrapInstance as init }`. In every one of these the
          realization is real and deliberate: the name is bound, and the body is one hop away
          through a property or an alias the model does not follow. This is the same blind spot
          the pure re-export barrel exemption closes for Level 1, one level down, and there is
          nothing in the tree to fix for it.
        findings:
          - code: METHOD_BODY_NOT_FOUND
            spec: host_git_adapter_impl
            at: 'configureSync'
          - code: METHOD_BODY_NOT_FOUND
            spec: host_git_adapter_impl
            at: 'disable'
          - code: METHOD_BODY_NOT_FOUND
            spec: host_git_adapter_impl
            at: 'enable'
          - code: METHOD_BODY_NOT_FOUND
            spec: host_git_adapter_impl
            at: 'publish'
          - code: METHOD_BODY_NOT_FOUND
            spec: host_git_adapter_impl
            at: 'status'
          - code: METHOD_BODY_NOT_FOUND
            spec: host_git_adapter_impl
            at: 'sync'
          - code: METHOD_BODY_NOT_FOUND
            spec: host_producer_adapter_impl
            at: 'configure'
          - code: METHOD_BODY_NOT_FOUND
            spec: host_producer_adapter_impl
            at: 'list'
          - code: METHOD_BODY_NOT_FOUND
            spec: host_producer_adapter_impl
            at: 'produce'
          - code: METHOD_BODY_NOT_FOUND
            spec: host_producer_adapter_impl
            at: 'remove'
          - code: METHOD_BODY_NOT_FOUND
            spec: host_server_impl
            at: 'init'
          - code: METHOD_BODY_NOT_FOUND
            spec: host_surfaces_adapter_impl
            at: 'exportBoundSurface'
      # 7 finding(s), 11 unit(s).
      - kind: unreadable
        why: >-
          The step's target IS called by name, from a site written in a shape a pure model cannot
          resolve to a file: a bare call on a name bound to a package specifier (scaffoldPack,
          extractPack out of the SDK), a receiver holding a value the module assembled (sdkPortal,
          hostCore), and a receiver this model records no name for at all. The code is deliberate
          about this: CALL_ORIGIN_UNRESOLVED exists to report what was NOT checked, so the step is
          neither proven realized nor accused of being missing, and no working call is asked to be
          rewritten to suit the reader. Nothing here is an author defect; it is the reader's
          reach, written down.
        findings:
          - code: CALL_ORIGIN_UNRESOLVED
            spec: cli_packs_adapter_impl
            at: 'initPack'
            covers:
              - '1:sdk_portal.scaffoldPack'
          - code: CALL_ORIGIN_UNRESOLVED
            spec: cli_packs_adapter_impl
            at: 'installPack'
            covers:
              - '2:sdk_portal.extractPack'
          - code: CALL_ORIGIN_UNRESOLVED
            spec: cli_packs_adapter_impl
            at: 'syncPacks'
            covers:
              - '12:sdk_portal.extractPack'
          - code: CALL_ORIGIN_UNRESOLVED
            spec: core_orchestrator_impl
            at: 'updateSpec'
            covers:
              - '16:spec_loader.saveComponentSpec'
              - '1:spec_loader.loadComponentSpec'
          - code: CALL_ORIGIN_UNRESOLVED
            spec: core_orchestrator_impl
            at: 'moveMethods'
            covers:
              - '1:spec_loader.loadComponentSpec'
              - '5:spec_loader.loadInterfaceSpecs'
              - '7:spec_loader.loadImplementationSpecs'
              - '15:spec_loader.saveImplementationSpec'
          - code: CALL_ORIGIN_UNRESOLVED
            spec: host_sdk_adapter_impl
            at: 'extractArchive'
            covers:
              - '1:sdk_portal.extractPack'
          - code: CALL_ORIGIN_UNRESOLVED
            spec: host_validator_adapter_impl
            at: 'validateAsComplete'
            covers:
              - '1:host_core_adapter.loadProjectConfig'
      # 1 finding(s), 1 unit(s).
      - kind: unreadable
        why: >-
          The forward check does not follow a TARGET through the file that republishes it.
          core_portal is realized by src/core/index.ts, which re-exports src/core/extensions.ts,
          and the call is proven to land in extensions.ts. Landing-side widening already follows a
          call through a barrel to where the function lives; the same relation read from the
          target side is not read yet. Measured on this tree it is exactly 1 of the 60 call-step
          units — the other 59 are real drift — so it is recorded as the narrow reader gap it is
          and not used to excuse them.
        findings:
          - code: CALL_STEP_UNREALIZED
            spec: cli_patterns_adapter_impl
            at: 'listPatterns'
            covers:
              - '1:core_portal.loadProjectExtensions'
      # 5 finding(s), 9 unit(s).
      - kind: drift
        why: >-
          A narrative names one loader or store call per data kind, and the realized method does
          not make it. The data arrives already loaded from the collaborator one hop out
          (renderDiagram and buildGraphModel both reach spec data through buildCanvasModel in
          src/core/canvas.ts), or it is derived from a kind the method does load (projectGraph
          reads each component's own subsystem field and never calls loadSubsystemSpecs), or the
          call belongs to the facade rather than the member (surface_registry.hydrateFromFiles
          parses and returns snapshots; the repository seats them). The finding is right: the step
          names a call this method does not make. Paying it means narrating the call the code
          actually makes - an author's edit, not a limit of the reader. It is carried here rather
          than allowed on the spec because unpaid narrative debt is exactly what it is: every
          allow these five replace said so in its own words ("reconcile", "overclaimed"), while an
          allow claims a finding is wrong by design and is meant to outlive everybody.
        findings:
          - code: CALL_STEP_UNREALIZED
            spec: architecture_diagrams_impl
            at: 'render'
            covers:
              - '6:spec_loader.loadTypeSpecs'
          - code: CALL_STEP_UNREALIZED
            spec: architecture_diagrams_impl
            at: 'buildGraphModel'
            covers:
              - '1:spec_loader.loadSystemSpec'
              - '2:spec_loader.loadSubsystemSpecs'
              - '3:spec_loader.loadComponentSpecs'
              - '4:spec_loader.loadInterfaceSpecs'
              - '5:spec_loader.loadTypeSpecs'
          - code: CALL_STEP_UNREALIZED
            spec: spec_doc_pages_impl
            at: 'project'
            covers:
              - '5:producer_core_adapter.loadImplementationSpecs'
          - code: CALL_STEP_UNREALIZED
            spec: spec_doc_pages_impl
            at: 'projectGraph'
            covers:
              - '1:producer_core_adapter.loadSubsystemSpecs'
          - code: CALL_STEP_UNREALIZED
            spec: surface_registry_impl
            at: 'hydrateFromFiles'
            covers:
              - '3:surface_store.put'
      # 2 finding(s), 2 unit(s).
      - kind: unreadable
        why: >-
          The MCP server's two skill-resource forwarders DO call the portal's methods at runtime.
          src/mcp/server.ts imports them RENAMED - "listResources as coreListSkillResources" and
          "readResource as coreReadSkillResource", from src/core/skills.ts, which realizes
          skills_portal - and forwards each one to one. The callee name at the call site is
          therefore the local alias, while the forward check looks for the target method's own
          name, so the call is reported as absent. Nothing here is overclaimed and nothing is
          wired wrong; what is missing is either a per-method symbol on the spec or an import
          rename the reader follows.
        revisit: >-
          Prove which of the two it is before anybody acts on it: the aliased binding the reader
          cannot follow (then this is unreadable and the fix belongs in the reader), or a step
          pointed at the wrong component (skills_portal and skill_resources are both realized by
          src/core/skills.ts - then it is drift and the fix is one narrative edit). The
          classification these two entries replace - "skill resources are served from the embedded
          exported skill set inside the MCP server, not by calling the skills portal at runtime" -
          was measured FALSE in this wave: the forwarders do call core at runtime. A reason that
          confident and that wrong is the reason to distrust this one until somebody checks it.
        findings:
          - code: CALL_STEP_UNREALIZED
            spec: mcp_skills_adapter_impl
            at: 'listResources'
            covers:
              - '1:skills_portal.listResources'
          - code: CALL_STEP_UNREALIZED
            spec: mcp_skills_adapter_impl
            at: 'readResource'
            covers:
              - '1:skills_portal.readResource'
      # 34 finding(s), 73 unit(s).
      - kind: undecided
        why: >-
          The first measured yield of export-conformance, after the 90 realization handles that
          `exportedVia` now declares. What is left is ragged rather than uniform, which is what real
          debt looks like and what the 90 were not: helpers a sibling component reaches for (ten path
          and loader-issue accessors on src/core/specs.ts, seven project-identity helpers on
          src/server/projects.ts), factories and classes that ARE a realization handle in another
          shape (ClaudeExporter, hostCore, projectConfigRepository) and could be declared once each
          one's owning method is identified, and shared constants nobody modelled (SUBPROJECT_SEPARATOR,
          BUILTIN_ROLES, COMPATIBLE_DEFAULT_EXPOSURE). Undecided rather than drift because each name
          has three legal answers - declare it as a contract method, declare it as a handle, or stop
          exporting it - and which one is right depends on what the name is FOR, which nobody has read
          file by file yet. None of it is a limit of the reader: every finding names an import that
          resolves, at exact grade, from a file realizing none of the accused components.
        findings:
          - code: UNDECLARED_EXPORT
            spec: cli_skills_adapter_impl
            at: 'src/commands/skills.ts'
            covers:
              - 'runSkillsInstall'
              - 'runSkillsList'
          - code: UNDECLARED_EXPORT
            spec: project_config_fs_adapter_impl
            at: 'src/config/project-config.ts'
            covers:
              - 'projectConfigRepository'
              - 'projectConfigRepositoryAt'
          - code: UNDECLARED_EXPORT
            spec: user_config_store_impl
            at: 'src/config/userconfig.ts'
            covers:
              - 'UPDATE_CHANNELS'
          - code: UNDECLARED_EXPORT
            spec: architecture_diagrams_impl
            at: 'src/core/diagram.ts'
            covers:
              - 'buildCanvasDataModel'
          - code: UNDECLARED_EXPORT
            spec: extension_orchestrator_impl
            at: 'src/core/extensions.ts'
            covers:
              - 'DeclarativePackSchema'
              - 'LoadedPackSkill'
              - 'PackScope'
              - 'loadExtensionPacks'
              - 'packDirEntry'
          - code: UNDECLARED_EXPORT
            spec: core_portal_impl
            at: 'src/core/index.ts'
            covers:
              - 'declaredPackNames'
              - 'declaredProfileIds'
              - 'diffSize'
          - code: UNDECLARED_EXPORT
            spec: openapi_codec_impl
            at: 'src/core/openapi.ts'
            covers:
              - 'isOpenApiDocument'
          - code: UNDECLARED_EXPORT
            spec: core_orchestrator_impl
            at: 'src/core/provision.ts'
            covers:
              - 'backfillChainedSubprojectConfigs'
          - code: UNDECLARED_EXPORT
            spec: spec_validator_impl
            at: 'src/core/rules/candidate.ts'
            covers:
              - 'formatCandidateRefusal'
          - code: UNDECLARED_EXPORT
            spec: skill_resources_impl
            at: 'src/core/skills.ts'
            covers:
              - 'activeTargetTypes'
              - 'checkSkillFreshness'
              - 'listSkillNames'
          - code: UNDECLARED_EXPORT
            spec: core_orchestrator_impl
            at: 'src/core/specs.ts'
            covers:
              - 'assertContainedProjectPath'
              - 'clearLoaderIssues'
              - 'findLegacySpecFiles'
              - 'getComponentPath'
              - 'getImplementationPath'
              - 'getInterfacePath'
              - 'getLoaderIssues'
              - 'getSubsystemPath'
              - 'scanAllSpecs'
              - 'snapshotSpecFiles'
          - code: UNDECLARED_EXPORT
            spec: state_hash_impl
            at: 'src/core/statehash.ts'
            covers:
              - 'stateIdEquals'
          - code: UNDECLARED_EXPORT
            spec: surface_fs_adapter_impl
            at: 'src/core/surfaces.ts'
            covers:
              - 'loadMountSurfaceSnapshots'
          - code: UNDECLARED_EXPORT
            spec: template_source_adapter_impl
            at: 'src/core/templates.ts'
            covers:
              - 'renderTemplateInstructions'
          - code: UNDECLARED_EXPORT
            spec: claude_exporter_impl
            at: 'src/exporters/claude.ts'
            covers:
              - 'ClaudeExporter'
          - code: UNDECLARED_EXPORT
            spec: custom_exporter_impl
            at: 'src/exporters/custom.ts'
            covers:
              - 'CustomExporter'
          - code: UNDECLARED_EXPORT
            spec: gemini_exporter_impl
            at: 'src/exporters/gemini.ts'
            covers:
              - 'GeminiExporter'
          - code: UNDECLARED_EXPORT
            spec: host_core_adapter_impl
            at: 'src/server/adapters.ts'
            covers:
              - 'hostCore'
              - 'hostGit'
              - 'hostProducer'
              - 'hostSdk'
              - 'hostSurfaces'
          - code: UNDECLARED_EXPORT
            spec: admin_orchestrator_impl
            at: 'src/server/admin.ts'
            covers:
              - 'AdminAuthError'
              - 'LockValidationError'
              - 'hostedApprover'
          - code: UNDECLARED_EXPORT
            spec: audit_index_impl
            at: 'src/server/audit.ts'
            covers:
              - 'DEFAULT_AUDIT_POLICY'
          - code: UNDECLARED_EXPORT
            spec: authentication_impl
            at: 'src/server/auth.ts'
            covers:
              - 'localDevSubject'
          - code: UNDECLARED_EXPORT
            spec: authorization_impl
            at: 'src/server/authorization.ts'
            covers:
              - 'actionableProjectIds'
              - 'actionableUnitIds'
              - 'isInstanceAdmin'
          - code: UNDECLARED_EXPORT
            spec: credential_index_impl
            at: 'src/server/credentials.ts'
            covers:
              - 'hashToken'
          - code: UNDECLARED_EXPORT
            spec: identity_orchestrator_impl
            at: 'src/server/identity.ts'
            covers:
              - 'ANONYMOUS_SSO_ACTOR'
              - 'ForbiddenError'
              - 'buildSsoAuditEvent'
              - 'isInAdminGroup'
          - code: UNDECLARED_EXPORT
            spec: identity_provider_adapter_impl
            at: 'src/server/idp.ts'
            covers:
              - 'assertAllowedRedirectUri'
          - code: UNDECLARED_EXPORT
            spec: organization_index_impl
            at: 'src/server/organization.ts'
            covers:
              - 'validateUnitHierarchy'
          - code: UNDECLARED_EXPORT
            spec: policy_index_impl
            at: 'src/server/policy.ts'
            covers:
              - 'COMPATIBLE_DEFAULT_EXPOSURE'
          - code: UNDECLARED_EXPORT
            spec: project_index_impl
            at: 'src/server/projects.ts'
            covers:
              - 'SUBPROJECT_SEPARATOR'
              - 'assertMintableNarrowingEntry'
              - 'existingProjectRoot'
              - 'isValidProjectId'
              - 'parseQualifiedSelector'
              - 'registerLocalDevProject'
              - 'resolveSubprojectMounts'
          - code: UNDECLARED_EXPORT
            spec: role_index_impl
            at: 'src/server/roles.ts'
            covers:
              - 'BUILTIN_ROLES'
              - 'isBuiltinRoleId'
          - code: UNDECLARED_EXPORT
            spec: visibility_rules_impl
            at: 'src/server/visibility.ts'
            covers:
              - 'audienceCovers'
          - code: UNDECLARED_EXPORT
            spec: web_graph_orchestrator_impl
            at: 'src/server/web.ts'
            covers:
              - 'sessionCookieValue'
              - 'setSessionCookie'
          - code: UNDECLARED_EXPORT
            spec: web_session_index_impl
            at: 'src/server/websessions.ts'
            covers:
              - 'WEB_SESSION_PREFIX'
          - code: UNDECLARED_EXPORT
            spec: ai_tool_guide_impl
            at: 'src/utils/ai-guide.ts'
            covers:
              - 'GLOBAL_GUIDE_BODY'
          - code: UNDECLARED_EXPORT
            spec: secret_index_impl
            at: 'src/utils/secrets.ts'
            covers:
              - 'resolveGitToken'
              - 'resolveSecret'
      # 45 finding(s), 85 unit(s).
      - kind: undecided
        why: >-
          The first measured yield of type-shape, after the 32 envelope fields the spec types now
          declare. Three shapes, and they are ragged rather than uniform. One, a spec names data the
          code does not carry (hosted_project_record describes nine fields its record has never had).
          Two, the code carries data no spec describes, so the ERD draws a record smaller than the one
          that exists. Three, and split almost two to one, the two sides disagree about whether a field
          may be absent: 19 are a spec calling a defaulted field optional - true of what an AUTHOR may
          omit, false of the value the type finally holds, which is what a type spec describes - and 9
          run the other way, where the spec promises a field is always there and the code lets it be
          missing. Those 9 are the ones to read first: a promise the data does not keep is the shape
          that misleads, and the other two shapes only omit.
          Undecided rather than drift because for each one the spec may be wrong or the code may be,
          and which it is cannot be read off the divergence - only off what the data is FOR. Nothing
          here is a limit of the reader: every finding is exact grade, and the shape it compares was
          either listed by the declaration or reached by one hop to the value it is built from.
        findings:
          - code: UNDECLARED_TYPE_FIELD
            spec: agent_brief
            at: 'AgentBrief'
            covers:
              - 'profile'
              - 'budget'
          - code: UNDECLARED_TYPE_FIELD
            spec: agent_record
            at: 'AgentRecord'
            covers:
              - 'description'
              - 'bundleOrigin'
              - 'readPaths'
              - 'writePaths'
              - 'dependencies'
              - 'targets'
          - code: UNDECLARED_TYPE_FIELD
            spec: canvas_model
            at: 'CanvasModel'
            covers:
              - 'types'
              - 'typeEdges'
              - 'dataEdges'
              - 'issues'
          - code: UNDECLARED_TYPE_FIELD
            spec: complexity_rule_config
            at: 'ComplexityRuleConfig'
            covers:
              - 'maxUnnarratedComplexity'
          - code: TYPE_FIELD_OPTIONALITY
            spec: component_spec
            at: 'ComponentSpec'
            covers:
              - 'status'
          - code: UNREALIZED_TYPE_FIELD
            spec: component_spec
            at: 'ComponentSpec'
            covers:
              - 'remoteInterfaces'
          - code: UNDECLARED_TYPE_FIELD
            spec: diagram_options
            at: 'DiagramOptions'
            covers:
              - 'format'
          - code: TYPE_FIELD_OPTIONALITY
            spec: domain
            at: 'Domain'
            covers:
              - 'ownedPaths'
          - code: UNDECLARED_TYPE_FIELD
            spec: git_config
            at: 'GitConfig'
            covers:
              - 'credentialRef'
              - 'lastSyncAt'
          - code: UNDECLARED_TYPE_FIELD
            spec: host_config
            at: 'HostConfig'
            covers:
              - 'adminHost'
          - code: UNREALIZED_TYPE_FIELD
            spec: host_config
            at: 'HostConfig'
            covers:
              - 'authFile'
              - 'auditPolicy'
          - code: UNDECLARED_TYPE_FIELD
            spec: host_exposure_policy
            at: 'HostExposurePolicy'
            covers:
              - 'webUiEnabled'
          - code: UNDECLARED_TYPE_FIELD
            spec: hosted_project_record
            at: 'HostedProjectRecord'
            covers:
              - 'unitId'
          - code: UNREALIZED_TYPE_FIELD
            spec: hosted_project_record
            at: 'HostedProjectRecord'
            covers:
              - 'displayName'
              - 'description'
              - 'createdBySubject'
              - 'ownerSubject'
              - 'tags'
              - 'environment'
              - 'ownerUnitId'
              - 'placementIds'
              - 'mcpServerDisplayName'
          - code: TYPE_FIELD_OPTIONALITY
            spec: implementation_spec
            at: 'ImplementationSpec'
            covers:
              - 'status'
          - code: TYPE_FIELD_OPTIONALITY
            spec: interface_spec
            at: 'InterfaceSpec'
            covers:
              - 'status'
          - code: UNREALIZED_TYPE_FIELD
            spec: lock_record
            at: 'LockRecord'
            covers:
              - 'lockedBySubject'
              - 'lockedByTokenId'
          - code: TYPE_FIELD_OPTIONALITY
            spec: lock_status
            at: 'LockStatus'
            covers:
              - 'record'
          - code: TYPE_FIELD_OPTIONALITY
            spec: method_implementation
            at: 'MethodImplementation'
            covers:
              - 'narrative'
          - code: UNDECLARED_TYPE_FIELD
            spec: method_implementation
            at: 'MethodImplementation'
            covers:
              - 'exportedVia'
          - code: TYPE_FIELD_OPTIONALITY
            spec: method_signature
            at: 'MethodSignature'
            covers:
              - 'signature'
              - 'returns'
          - code: UNDECLARED_TYPE_FIELD
            spec: method_signature
            at: 'MethodSignature'
            covers:
              - 'description'
          - code: TYPE_FIELD_OPTIONALITY
            spec: organization_unit_record
            at: 'OrganizationUnitRecord'
            covers:
              - 'createdBy'
          - code: UNREALIZED_TYPE_FIELD
            spec: pack_instruction_block
            at: 'PackInstructionBlock'
            covers:
              - 'pack'
          - code: UNDECLARED_TYPE_FIELD
            spec: pack_selection_failure
            at: 'PackSelectionFailure'
            covers:
              - 'severity'
          - code: UNREALIZED_TYPE_FIELD
            spec: pack_skill
            at: 'PackSkill'
            covers:
              - 'pack'
              - 'version'
          - code: UNDECLARED_TYPE_FIELD
            spec: principal
            at: 'Principal'
            covers:
              - 'authenticated'
          - code: TYPE_FIELD_OPTIONALITY
            spec: project_config
            at: 'ProjectConfig'
            covers:
              - 'projectType'
          - code: UNDECLARED_TYPE_FIELD
            spec: project_config
            at: 'ProjectConfig'
            covers:
              - 'description'
              - 'execution'
              - 'paths'
              - 'globalTemplatesDir'
              - 'aiGuide'
          - code: UNDECLARED_TYPE_FIELD
            spec: project_config_view
            at: 'ProjectConfigView'
            covers:
              - 'lockStale'
          - code: TYPE_FIELD_OPTIONALITY
            spec: project_placement
            at: 'ProjectPlacement'
            covers:
              - 'createdBy'
          - code: TYPE_FIELD_OPTIONALITY
            spec: project_relation_record
            at: 'ProjectRelationRecord'
            covers:
              - 'sourceAdapter'
              - 'createdBy'
          - code: UNREALIZED_TYPE_FIELD
            spec: remote_command_options
            at: 'RemoteCommandOptions'
            covers:
              - 'yes'
          - code: UNDECLARED_TYPE_FIELD
            spec: remote_transfer_outcome
            at: 'RemoteTransferOutcome'
            covers:
              - 'skipped'
          - code: TYPE_FIELD_OPTIONALITY
            spec: rules_config
            at: 'RulesConfig'
            covers:
              - 'sddRuleSeverity'
          - code: UNDECLARED_TYPE_FIELD
            spec: rules_config
            at: 'RulesConfig'
            covers:
              - 'generateComponentImplementers'
              - 'materializeAgentFiles'
              - 'designDepth'
          - code: TYPE_FIELD_OPTIONALITY
            spec: subsystem_spec
            at: 'SubsystemSpec'
            covers:
              - 'status'
          - code: UNDECLARED_TYPE_FIELD
            spec: subsystem_spec
            at: 'SubsystemSpec'
            covers:
              - 'profile'
              - 'designDepth'
          - code: UNDECLARED_TYPE_FIELD
            spec: surface_export_result
            at: 'SurfaceExportResult'
            covers:
              - 'renderedSet'
              - 'writtenPaths'
          - code: TYPE_FIELD_OPTIONALITY
            spec: system_public_interface
            at: 'SystemPublicInterface'
            covers:
              - 'id'
              - 'name'
              - 'subsystem'
              - 'component'
              - 'type'
              - 'details'
              - 'audience'
              - 'authPolicy'
          - code: TYPE_FIELD_OPTIONALITY
            spec: system_spec
            at: 'SystemSpec'
            covers:
              - 'databases'
          - code: TYPE_FIELD_OPTIONALITY
            spec: template
            at: 'Template'
            covers:
              - 'defaultTags'
              - 'requiresOwnedPaths'
              - 'version'
          - code: TYPE_FIELD_OPTIONALITY
            spec: token_mint_request
            at: 'TokenMintRequest'
            covers:
              - 'projects'
          - code: UNDECLARED_TYPE_FIELD
            spec: validation_issue
            at: 'ValidationIssue'
            covers:
              - 'surfaceResolved'
          - code: UNDECLARED_TYPE_FIELD
            spec: web_session
            at: 'WebSession'
            covers:
              - 'projects'
      # 214 finding(s), 244 unit(s).
      - kind: undecided
        why: >-
          The first measured yield of param-conformance on the signatures the contracts describe in
          their own terms, after the 40 implementations that declare their injected wiring. Four
          shapes, and each name has two legal answers that only a reader of the code can choose
          between. A contract declares an argument the function never takes, so the promise goes
          nowhere and every brief built from it passes one. A function takes an argument no contract
          names and no injection accounts for - enableGit takes a `pat`, executeApprovedLock an
          `approver` - which is an input a caller must supply that the design never mentions, and the
          credential case is the reason this rule exists at all. The two sides name the same argument
          differently, agreeing on position and type: `config` where the code says `projectConfig`,
          `credential` where it says `token`. And they disagree about whether an argument may be left
          out, which tells a caller a value is guaranteed when it is not, or the reverse. Undecided
          rather than drift because for each one the contract may be behind the code or the code may
          have grown past its contract, and the divergence does not say which. Nothing here is a limit
          of the reader: every finding is exact grade, aligned from the front, and where a name has
          several bodies only what ALL of them agree on is reported.
        findings:
          - code: UNDECLARED_PARAM
            spec: admin_orchestrator_impl
            at: 'executeApprovedCreate'
            covers:
              - 'placedBy'
          - code: PARAM_OPTIONALITY
            spec: admin_orchestrator_impl
            at: 'executeApprovedLock'
            covers:
              - 'subproject'
          - code: UNDECLARED_PARAM
            spec: admin_orchestrator_impl
            at: 'executeApprovedLock'
            covers:
              - 'subproject'
          - code: PARAM_NAME_MISMATCH
            spec: agent_file_generator_impl
            at: 'generateAll'
            covers:
              - 'config'
          - code: PARAM_NAME_MISMATCH
            spec: agent_file_generator_impl
            at: 'resolveExpectedOutputPaths'
            covers:
              - 'config'
          - code: UNDECLARED_PARAM
            spec: agent_file_generator_impl
            at: 'resolveExpectedOutputPaths'
            covers:
              - 'projectRoot'
          - code: PARAM_NAME_MISMATCH
            spec: ai_tool_guide_impl
            at: 'globalPath'
            covers:
              - 'target'
          - code: PARAM_NAME_MISMATCH
            spec: ai_tool_guide_impl
            at: 'localPath'
            covers:
              - 'target'
          - code: PARAM_NAME_MISMATCH
            spec: ai_tool_guide_impl
            at: 'refreshLocal'
            covers:
              - 'targets'
          - code: PARAM_NAME_MISMATCH
            spec: ai_tool_guide_impl
            at: 'writeDelegator'
            covers:
              - 'target'
          - code: UNDECLARED_PARAM
            spec: approval_comparison_impl
            at: 'captureApprovedSpecs'
            covers:
              - 'scope'
          - code: PARAM_OPTIONALITY
            spec: approval_comparison_impl
            at: 'currentChildPins'
            covers:
              - 'root'
              - 'mounts'
          - code: PARAM_OPTIONALITY
            spec: approval_comparison_impl
            at: 'movedChildren'
            covers:
              - 'root'
              - 'mounts'
          - code: UNREALIZED_PARAM
            spec: approval_index_impl
            at: 'list'
            covers:
              - 'requestedBy'
          - code: UNREALIZED_PARAM
            spec: authoring_core_adapter_impl
            at: 'moveMethods'
            covers:
              - 'dryRun'
          - code: PARAM_OPTIONALITY
            spec: cli_core_adapter_impl
            at: 'getStatusReport'
            covers:
              - 'decor'
          - code: UNDECLARED_PARAM
            spec: cli_lock_adapter_impl
            at: 'lockTree'
            covers:
              - 'gate'
          - code: PARAM_OPTIONALITY
            spec: cli_mcp_adapter_impl
            at: 'detectHostedMcpSource'
            covers:
              - 'cwd'
          - code: PARAM_OPTIONALITY
            spec: cli_mcp_adapter_impl
            at: 'installMcpServer'
            covers:
              - 'options'
          - code: PARAM_OPTIONALITY
            spec: cli_packs_adapter_impl
            at: 'buildPack'
            covers:
              - 'options'
          - code: PARAM_OPTIONALITY
            spec: cli_packs_adapter_impl
            at: 'bundlePack'
            covers:
              - 'options'
          - code: PARAM_OPTIONALITY
            spec: cli_packs_adapter_impl
            at: 'initPack'
            covers:
              - 'options'
          - code: PARAM_OPTIONALITY
            spec: cli_packs_adapter_impl
            at: 'usePack'
            covers:
              - 'options'
          - code: PARAM_OPTIONALITY
            spec: cli_runner_impl
            at: 'localStatus'
            covers:
              - 'options'
          - code: PARAM_NAME_MISMATCH
            spec: cli_runner_impl
            at: 'runDiagram'
            covers:
              - 'options'
          - code: PARAM_OPTIONALITY
            spec: cli_runner_impl
            at: 'runDiagram'
            covers:
              - 'options'
          - code: PARAM_NAME_MISMATCH
            spec: cli_runner_impl
            at: 'runExecutionSetTier'
            covers:
              - 'tier'
          - code: PARAM_NAME_MISMATCH
            spec: cli_runner_impl
            at: 'runLockCheck'
            covers:
              - 'options'
          - code: PARAM_OPTIONALITY
            spec: cli_runner_impl
            at: 'runLogin'
            covers:
              - 'options'
          - code: PARAM_NAME_MISMATCH
            spec: cli_runner_impl
            at: 'runLogout'
            covers:
              - 'options'
          - code: PARAM_OPTIONALITY
            spec: cli_runner_impl
            at: 'runLogout'
            covers:
              - 'options'
          - code: UNDECLARED_PARAM
            spec: cli_runner_impl
            at: 'runPack'
            covers:
              - 'arg'
              - 'opts'
          - code: UNDECLARED_PARAM
            spec: cli_runner_impl
            at: 'runPacks'
            covers:
              - 'arg'
              - 'opts'
          - code: PARAM_OPTIONALITY
            spec: cli_runner_impl
            at: 'runRemote'
            covers:
              - 'options'
          - code: PARAM_OPTIONALITY
            spec: core_orchestrator_impl
            at: 'moveRenamedSpecs'
            covers:
              - 'movingInterface'
              - 'movingImplementation'
          - code: PARAM_OPTIONALITY
            spec: core_orchestrator_impl
            at: 'specPathsInScope'
            covers:
              - 'scopeSubsystem'
          - code: PARAM_OPTIONALITY
            spec: core_orchestrator_impl
            at: 'updateSpec'
            covers:
              - 'dryRun'
          - code: UNDECLARED_PARAM
            spec: core_orchestrator_impl
            at: 'updateSpec'
            covers:
              - 'delta'
              - 'hooks'
              - 'dryRun'
          - code: UNDECLARED_PARAM
            spec: diagnostics_impl
            at: 'runChecks'
            covers:
              - 'relations'
              - 'relationSnapshots'
          - code: PARAM_NAME_MISMATCH
            spec: domain_detector_impl
            at: 'detectCandidates'
            covers:
              - 'trackedPaths'
              - 'trackedIds'
          - code: UNDECLARED_PARAM
            spec: exposure_policy_orchestrator_impl
            at: 'getExposurePolicy'
            covers:
              - 'credential'
          - code: UNDECLARED_PARAM
            spec: exposure_policy_orchestrator_impl
            at: 'setExposurePolicy'
            covers:
              - 'exposure'
          - code: UNDECLARED_PARAM
            spec: git_backing_adapter_impl
            at: 'cloneOrOpen'
            covers:
              - 'credentialRef'
          - code: UNDECLARED_PARAM
            spec: host_request_orchestrator_impl
            at: 'auditToolCall'
            covers:
              - 'outcome'
              - 'subproject'
          - code: UNDECLARED_PARAM
            spec: host_request_orchestrator_impl
            at: 'dispatchProjectLifecycleTool'
            covers:
              - 'body'
              - 'subproject'
          - code: UNDECLARED_PARAM
            spec: host_request_orchestrator_impl
            at: 'handle'
            covers:
              - 'req'
              - 'res'
              - 'body'
              - 'credential'
          - code: UNREALIZED_PARAM
            spec: host_validator_adapter_impl
            at: 'validateAsComplete'
            covers:
              - 'options'
              - 'projectType'
          - code: PARAM_NAME_MISMATCH
            spec: instance_bootstrap_impl
            at: 'seed'
            covers:
              - 'config'
          - code: PARAM_OPTIONALITY
            spec: instance_identity_repository_impl
            at: 'ensureSeeded'
            covers:
              - 'now'
          - code: PARAM_OPTIONALITY
            spec: landscape_orchestrator_impl
            at: 'generate'
            covers:
              - 'scope'
          - code: UNDECLARED_PARAM
            spec: landscape_orchestrator_impl
            at: 'generate'
            covers:
              - 'scope'
          - code: PARAM_OPTIONALITY
            spec: landscape_orchestrator_impl
            at: 'listRelations'
            covers:
              - 'projectId'
          - code: UNDECLARED_PARAM
            spec: landscape_orchestrator_impl
            at: 'listRelations'
            covers:
              - 'projectId'
          - code: UNDECLARED_PARAM
            spec: landscape_orchestrator_impl
            at: 'placeProject'
            covers:
              - 'placement'
          - code: UNDECLARED_PARAM
            spec: landscape_orchestrator_impl
            at: 'refreshPublicSurface'
            covers:
              - 'projectId'
          - code: UNDECLARED_PARAM
            spec: landscape_orchestrator_impl
            at: 'removeRelation'
            covers:
              - 'id'
          - code: UNDECLARED_PARAM
            spec: landscape_orchestrator_impl
            at: 'upsertRelation'
            covers:
              - 'relation'
          - code: UNDECLARED_PARAM
            spec: landscape_orchestrator_impl
            at: 'upsertUnit'
            covers:
              - 'unit'
          - code: PARAM_NAME_MISMATCH
            spec: landscape_portal_impl
            at: 'exportProjectSurface'
            covers:
              - 'format'
              - 'maxAudience'
              - 'portalId'
          - code: PARAM_OPTIONALITY
            spec: landscape_portal_impl
            at: 'exportProjectSurface'
            covers:
              - 'portalId'
          - code: UNDECLARED_PARAM
            spec: landscape_portal_impl
            at: 'exportProjectSurface'
            covers:
              - 'portalId'
          - code: PARAM_OPTIONALITY
            spec: landscape_portal_impl
            at: 'generateLandscape'
            covers:
              - 'scope'
          - code: UNDECLARED_PARAM
            spec: landscape_portal_impl
            at: 'generateLandscape'
            covers:
              - 'scope'
          - code: PARAM_OPTIONALITY
            spec: landscape_portal_impl
            at: 'listRelations'
            covers:
              - 'projectId'
          - code: UNDECLARED_PARAM
            spec: landscape_portal_impl
            at: 'listRelations'
            covers:
              - 'projectId'
          - code: UNDECLARED_PARAM
            spec: landscape_portal_impl
            at: 'listVisibleSurfaces'
            covers:
              - 'currentProjectId'
          - code: UNDECLARED_PARAM
            spec: landscape_portal_impl
            at: 'placeProject'
            covers:
              - 'placement'
          - code: UNDECLARED_PARAM
            spec: landscape_portal_impl
            at: 'refreshPublicSurface'
            covers:
              - 'projectId'
          - code: UNDECLARED_PARAM
            spec: landscape_portal_impl
            at: 'removeRelation'
            covers:
              - 'id'
          - code: UNDECLARED_PARAM
            spec: landscape_portal_impl
            at: 'upsertRelation'
            covers:
              - 'relation'
          - code: UNDECLARED_PARAM
            spec: landscape_portal_impl
            at: 'upsertUnit'
            covers:
              - 'unit'
          - code: UNDECLARED_PARAM
            spec: mcp_orchestrator_impl
            at: 'readResource'
            covers:
              - 'listChanged'
          - code: PARAM_NAME_MISMATCH
            spec: mcp_orchestrator_impl
            at: 'renameComponent'
            covers:
              - 'id'
          - code: PARAM_NAME_MISMATCH
            spec: mcp_orchestrator_impl
            at: 'renameMethod'
            covers:
              - 'id'
              - 'method'
          - code: UNDECLARED_PARAM
            spec: mcp_portal_impl
            at: 'readResource'
            covers:
              - 'listChanged'
          - code: PARAM_OPTIONALITY
            spec: operations_orchestrator_impl
            at: 'evaluateQuota'
            covers:
              - 'scope'
          - code: UNDECLARED_PARAM
            spec: operations_orchestrator_impl
            at: 'evaluateQuota'
            covers:
              - 'scope'
          - code: PARAM_OPTIONALITY
            spec: operations_orchestrator_impl
            at: 'getHealthReport'
            covers:
              - 'scope'
          - code: UNDECLARED_PARAM
            spec: operations_orchestrator_impl
            at: 'getHealthReport'
            covers:
              - 'scope'
          - code: PARAM_OPTIONALITY
            spec: operations_orchestrator_impl
            at: 'getUsage'
            covers:
              - 'scope'
          - code: UNDECLARED_PARAM
            spec: operations_orchestrator_impl
            at: 'getUsage'
            covers:
              - 'scope'
          - code: PARAM_OPTIONALITY
            spec: operations_portal_impl
            at: 'evaluateQuota'
            covers:
              - 'scope'
          - code: UNDECLARED_PARAM
            spec: operations_portal_impl
            at: 'evaluateQuota'
            covers:
              - 'scope'
          - code: PARAM_OPTIONALITY
            spec: operations_portal_impl
            at: 'getHealthReport'
            covers:
              - 'scope'
          - code: UNDECLARED_PARAM
            spec: operations_portal_impl
            at: 'getHealthReport'
            covers:
              - 'scope'
          - code: PARAM_OPTIONALITY
            spec: operations_portal_impl
            at: 'getUsage'
            covers:
              - 'scope'
          - code: UNDECLARED_PARAM
            spec: operations_portal_impl
            at: 'getUsage'
            covers:
              - 'scope'
          - code: PARAM_NAME_MISMATCH
            spec: pack_archive_adapter_impl
            at: 'inflateEntry'
            covers:
              - 'path'
          - code: PARAM_NAME_MISMATCH
            spec: pack_orchestrator_impl
            at: 'adoptProjectPack'
            covers:
              - 'name'
          - code: UNDECLARED_PARAM
            spec: pack_orchestrator_impl
            at: 'adoptProjectPack'
            covers:
              - 'name'
          - code: PARAM_OPTIONALITY
            spec: pack_orchestrator_impl
            at: 'installGlobalPackArchive'
            covers:
              - 'name'
          - code: UNDECLARED_PARAM
            spec: pack_orchestrator_impl
            at: 'installGlobalPackArchive'
            covers:
              - 'name'
          - code: PARAM_NAME_MISMATCH
            spec: pack_orchestrator_impl
            at: 'installGlobalPack'
            covers:
              - 'content'
          - code: UNDECLARED_PARAM
            spec: pack_orchestrator_impl
            at: 'installGlobalPack'
            covers:
              - 'content'
          - code: PARAM_OPTIONALITY
            spec: pack_orchestrator_impl
            at: 'installProjectPackArchive'
            covers:
              - 'name'
          - code: UNDECLARED_PARAM
            spec: pack_orchestrator_impl
            at: 'installProjectPackArchive'
            covers:
              - 'name'
          - code: PARAM_NAME_MISMATCH
            spec: pack_orchestrator_impl
            at: 'installProjectPack'
            covers:
              - 'name'
              - 'content'
          - code: UNDECLARED_PARAM
            spec: pack_orchestrator_impl
            at: 'installProjectPack'
            covers:
              - 'content'
          - code: UNDECLARED_PARAM
            spec: pack_orchestrator_impl
            at: 'listAdoptableProjectPacks'
            covers:
              - 'project'
          - code: UNDECLARED_PARAM
            spec: pack_orchestrator_impl
            at: 'listAvailableProfiles'
            covers:
              - 'credential'
          - code: UNDECLARED_PARAM
            spec: pack_orchestrator_impl
            at: 'listGlobalPacks'
            covers:
              - 'credential'
          - code: UNDECLARED_PARAM
            spec: pack_orchestrator_impl
            at: 'listProjectPacks'
            covers:
              - 'project'
          - code: UNDECLARED_PARAM
            spec: pack_orchestrator_impl
            at: 'listProjectProfiles'
            covers:
              - 'project'
          - code: UNDECLARED_PARAM
            spec: pack_orchestrator_impl
            at: 'removeGlobalPack'
            covers:
              - 'name'
          - code: PARAM_NAME_MISMATCH
            spec: pack_orchestrator_impl
            at: 'removeProjectPack'
            covers:
              - 'name'
          - code: UNDECLARED_PARAM
            spec: pack_orchestrator_impl
            at: 'removeProjectPack'
            covers:
              - 'name'
          - code: UNDECLARED_PARAM
            spec: pack_source_adapter_impl
            at: 'readManifest'
            covers:
              - 'projectRoot'
          - code: PARAM_OPTIONALITY
            spec: pack_store_impl
            at: 'installGlobalPackArchive'
            covers:
              - 'name'
          - code: UNDECLARED_PARAM
            spec: pack_store_impl
            at: 'installGlobalPackArchive'
            covers:
              - 'name'
          - code: PARAM_NAME_MISMATCH
            spec: pack_store_impl
            at: 'installGlobalPack'
            covers:
              - 'content'
          - code: UNDECLARED_PARAM
            spec: pack_store_impl
            at: 'installGlobalPack'
            covers:
              - 'content'
          - code: PARAM_NAME_MISMATCH
            spec: pack_store_impl
            at: 'installProjectPackArchive'
            covers:
              - 'name'
          - code: PARAM_OPTIONALITY
            spec: pack_store_impl
            at: 'installProjectPackArchive'
            covers:
              - 'name'
          - code: UNDECLARED_PARAM
            spec: pack_store_impl
            at: 'installProjectPackArchive'
            covers:
              - 'archive'
              - 'name'
          - code: PARAM_NAME_MISMATCH
            spec: pack_store_impl
            at: 'installProjectPack'
            covers:
              - 'content'
          - code: UNDECLARED_PARAM
            spec: pack_store_impl
            at: 'installProjectPack'
            covers:
              - 'name'
              - 'content'
          - code: PARAM_OPTIONALITY
            spec: pack_store_impl
            at: 'listProjectPacks'
            covers:
              - 'config'
          - code: UNDECLARED_PARAM
            spec: pack_store_impl
            at: 'listProjectPacks'
            covers:
              - 'project'
          - code: UNDECLARED_PARAM
            spec: pack_store_impl
            at: 'removeGlobalPack'
            covers:
              - 'name'
          - code: PARAM_NAME_MISMATCH
            spec: pack_store_impl
            at: 'removeProjectPack'
            covers:
              - 'ref'
          - code: PARAM_OPTIONALITY
            spec: pack_store_impl
            at: 'removeProjectPack'
            covers:
              - 'ref'
          - code: UNDECLARED_PARAM
            spec: pack_store_impl
            at: 'removeProjectPack'
            covers:
              - 'name'
          - code: PARAM_NAME_MISMATCH
            spec: permission_admin_orchestrator_impl
            at: 'bindRole'
            covers:
              - 'roleId'
              - 'scopeKind'
          - code: PARAM_OPTIONALITY
            spec: permission_admin_orchestrator_impl
            at: 'bindRole'
            covers:
              - 'scopeKind'
          - code: UNDECLARED_PARAM
            spec: permission_admin_orchestrator_impl
            at: 'bindRole'
            covers:
              - 'scopeId'
          - code: UNDECLARED_PARAM
            spec: permission_admin_orchestrator_impl
            at: 'createRole'
            covers:
              - 'role'
          - code: UNDECLARED_PARAM
            spec: permission_admin_orchestrator_impl
            at: 'deleteRole'
            covers:
              - 'roleId'
          - code: PARAM_NAME_MISMATCH
            spec: permission_admin_orchestrator_impl
            at: 'listAssignments'
            covers:
              - 'subjectKind'
          - code: PARAM_OPTIONALITY
            spec: permission_admin_orchestrator_impl
            at: 'listAssignments'
            covers:
              - 'scopeKind'
          - code: UNDECLARED_PARAM
            spec: permission_admin_orchestrator_impl
            at: 'listAssignments'
            covers:
              - 'subjectId'
          - code: UNDECLARED_PARAM
            spec: permission_admin_orchestrator_impl
            at: 'listRoles'
            covers:
              - 'credential'
          - code: UNDECLARED_PARAM
            spec: permission_admin_orchestrator_impl
            at: 'removeAssignment'
            covers:
              - 'assignmentId'
          - code: UNDECLARED_PARAM
            spec: permission_admin_orchestrator_impl
            at: 'setAssignment'
            covers:
              - 'assignment'
          - code: PARAM_NAME_MISMATCH
            spec: permission_admin_orchestrator_impl
            at: 'unbindRole'
            covers:
              - 'roleId'
              - 'scopeKind'
          - code: PARAM_OPTIONALITY
            spec: permission_admin_orchestrator_impl
            at: 'unbindRole'
            covers:
              - 'scopeKind'
          - code: UNDECLARED_PARAM
            spec: permission_admin_orchestrator_impl
            at: 'unbindRole'
            covers:
              - 'scopeId'
          - code: UNDECLARED_PARAM
            spec: permission_admin_orchestrator_impl
            at: 'updateRole'
            covers:
              - 'role'
          - code: UNDECLARED_PARAM
            spec: project_lifecycle_orchestrator_impl
            at: 'awaitApproval'
            covers:
              - 'timeoutSeconds'
          - code: UNDECLARED_PARAM
            spec: project_lifecycle_orchestrator_impl
            at: 'decideRequest'
            covers:
              - 'decision'
          - code: UNDECLARED_PARAM
            spec: project_lifecycle_orchestrator_impl
            at: 'executeApprovedRequest'
            covers:
              - 'requestId'
          - code: UNDECLARED_PARAM
            spec: project_lifecycle_orchestrator_impl
            at: 'getApprovalStatus'
            covers:
              - 'requestId'
          - code: UNDECLARED_PARAM
            spec: project_lifecycle_orchestrator_impl
            at: 'initializeProject'
            covers:
              - 'request'
          - code: PARAM_OPTIONALITY
            spec: project_lifecycle_orchestrator_impl
            at: 'listPendingRequests'
            covers:
              - 'projectId'
          - code: UNDECLARED_PARAM
            spec: project_lifecycle_orchestrator_impl
            at: 'listPendingRequests'
            covers:
              - 'projectId'
          - code: PARAM_NAME_MISMATCH
            spec: project_lifecycle_orchestrator_impl
            at: 'lockProject'
            covers:
              - 'subproject'
          - code: PARAM_OPTIONALITY
            spec: project_lifecycle_orchestrator_impl
            at: 'lockProject'
            covers:
              - 'subproject'
          - code: UNDECLARED_PARAM
            spec: project_lifecycle_orchestrator_impl
            at: 'lockProject'
            covers:
              - 'subproject'
          - code: PARAM_NAME_MISMATCH
            spec: project_ops_orchestrator_impl
            at: 'setExposurePolicy'
            covers:
              - 'policy'
          - code: PARAM_NAME_MISMATCH
            spec: project_ops_orchestrator_impl
            at: 'setPackPolicy'
            covers:
              - 'policy'
          - code: UNDECLARED_PARAM
            spec: project_policy_portal_impl
            at: 'evaluateProjectPolicy'
            covers:
              - 'projectId'
          - code: UNDECLARED_PARAM
            spec: project_policy_portal_impl
            at: 'getPackPolicy'
            covers:
              - 'credential'
          - code: UNDECLARED_PARAM
            spec: project_policy_portal_impl
            at: 'initializeProjectWithProfile'
            covers:
              - 'request'
          - code: UNDECLARED_PARAM
            spec: project_policy_portal_impl
            at: 'reconcileProjectPolicy'
            covers:
              - 'projectId'
          - code: UNDECLARED_PARAM
            spec: project_policy_portal_impl
            at: 'setPackPolicy'
            covers:
              - 'policy'
          - code: PARAM_NAME_MISMATCH
            spec: project_relation_store_impl
            at: 'replaceAll'
            covers:
              - 'records'
          - code: PARAM_OPTIONALITY
            spec: project_status_impl
            at: 'report'
            covers:
              - 'options'
              - 'decor'
          - code: PARAM_OPTIONALITY
            spec: remote_orchestrator_impl
            at: 'pullTree'
            covers:
              - 'options'
          - code: PARAM_OPTIONALITY
            spec: remote_orchestrator_impl
            at: 'pushTree'
            covers:
              - 'options'
          - code: PARAM_OPTIONALITY
            spec: remote_orchestrator_impl
            at: 'resolveTarget'
            covers:
              - 'overrides'
          - code: UNDECLARED_PARAM
            spec: share_admin_orchestrator_impl
            at: 'createShareLink'
            covers:
              - 'input'
          - code: UNDECLARED_PARAM
            spec: share_admin_orchestrator_impl
            at: 'getShareAccessLog'
            covers:
              - 'limit'
          - code: UNDECLARED_PARAM
            spec: share_admin_orchestrator_impl
            at: 'listShareLinks'
            covers:
              - 'projectId'
          - code: UNDECLARED_PARAM
            spec: share_admin_orchestrator_impl
            at: 'refreshSnapshot'
            covers:
              - 'linkId'
          - code: UNDECLARED_PARAM
            spec: share_admin_orchestrator_impl
            at: 'removeShareLink'
            covers:
              - 'linkId'
          - code: UNDECLARED_PARAM
            spec: share_admin_orchestrator_impl
            at: 'updateShareLink'
            covers:
              - 'changes'
          - code: PARAM_NAME_MISMATCH
            spec: share_portal_impl
            at: 'downloadSharedArtifact'
            covers:
              - 'kind'
          - code: PARAM_OPTIONALITY
            spec: share_portal_impl
            at: 'downloadSharedArtifact'
            covers:
              - 'portalId'
          - code: UNDECLARED_PARAM
            spec: share_portal_impl
            at: 'downloadSharedArtifact'
            covers:
              - 'res'
          - code: UNDECLARED_PARAM
            spec: share_portal_impl
            at: 'getSharedModel'
            covers:
              - 'req'
              - 'res'
          - code: PARAM_OPTIONALITY
            spec: share_portal_impl
            at: 'getSharedOpenApi'
            covers:
              - 'portalId'
          - code: UNDECLARED_PARAM
            spec: share_portal_impl
            at: 'getSharedOpenApi'
            covers:
              - 'res'
          - code: UNDECLARED_PARAM
            spec: share_portal_impl
            at: 'getSharedView'
            covers:
              - 'req'
              - 'res'
          - code: PARAM_NAME_MISMATCH
            spec: share_snapshots_impl
            at: 'capture'
            covers:
              - 'sessionId'
              - 'view'
          - code: UNDECLARED_PARAM
            spec: share_snapshots_impl
            at: 'capture'
            covers:
              - 'artifacts'
          - code: PARAM_OPTIONALITY
            spec: source_analysis_adapter_impl
            at: 'findTestsReferencing'
            covers:
              - 'testRoots'
          - code: PARAM_NAME_MISMATCH
            spec: spec_file_store_impl
            at: 'remove'
            covers:
              - 'specPath'
          - code: PARAM_OPTIONALITY
            spec: spec_validator_impl
            at: 'findTestsReferencing'
            covers:
              - 'testRoots'
          - code: PARAM_OPTIONALITY
            spec: spec_validator_impl
            at: 'resolveThroughParent'
            covers:
              - 'childRules'
          - code: UNREALIZED_PARAM
            spec: spec_validator_impl
            at: 'validateAsComplete'
            covers:
              - 'projectType'
          - code: PARAM_NAME_MISMATCH
            spec: surface_exchange_orchestrator_impl
            at: 'exportProjectSurface'
            covers:
              - 'format'
              - 'maxAudience'
              - 'portalId'
          - code: PARAM_OPTIONALITY
            spec: surface_exchange_orchestrator_impl
            at: 'exportProjectSurface'
            covers:
              - 'portalId'
          - code: UNDECLARED_PARAM
            spec: surface_exchange_orchestrator_impl
            at: 'exportProjectSurface'
            covers:
              - 'portalId'
          - code: PARAM_NAME_MISMATCH
            spec: surface_exchange_orchestrator_impl
            at: 'getProjectSurfaceForMcp'
            covers:
              - 'targetProjectId'
          - code: UNDECLARED_PARAM
            spec: surface_exchange_orchestrator_impl
            at: 'getProjectSurfaceForMcp'
            covers:
              - 'targetProjectId'
          - code: PARAM_NAME_MISMATCH
            spec: surface_exchange_orchestrator_impl
            at: 'listReachableProjectInterfacesForMcp'
            covers:
              - 'targetProjectId'
          - code: UNDECLARED_PARAM
            spec: surface_exchange_orchestrator_impl
            at: 'listReachableProjectInterfacesForMcp'
            covers:
              - 'targetProjectId'
          - code: UNDECLARED_PARAM
            spec: surface_exchange_orchestrator_impl
            at: 'listReachableProjectsForMcp'
            covers:
              - 'currentProjectId'
          - code: UNDECLARED_PARAM
            spec: surface_exchange_orchestrator_impl
            at: 'listVisibleSurfaces'
            covers:
              - 'currentProjectId'
          - code: UNDECLARED_PARAM
            spec: surface_fs_adapter_impl
            at: 'deleteSnapshot'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_fs_adapter_impl
            at: 'writeSnapshot'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_index_impl
            at: 'byProject'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_orchestrator_impl
            at: 'getSnapshot'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_orchestrator_impl
            at: 'removeSnapshot'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_portal_impl
            at: 'exportSurface'
            covers:
              - 'portalId'
          - code: UNDECLARED_PARAM
            spec: surface_portal_impl
            at: 'getSnapshot'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_portal_impl
            at: 'removeSnapshot'
            covers:
              - 'rootDir'
          - code: UNREALIZED_PARAM
            spec: surface_projector_impl
            at: 'projectChildSurface'
            covers:
              - 'childSubsystemId'
          - code: UNDECLARED_PARAM
            spec: surface_registry_impl
            at: 'deleteSnapshot'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_registry_impl
            at: 'upsertSnapshot'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_repository_impl
            at: 'getSnapshot'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_repository_impl
            at: 'removeSnapshot'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_repository_impl
            at: 'saveSnapshot'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_store_impl
            at: 'get'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_store_impl
            at: 'put'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_store_impl
            at: 'remove'
            covers:
              - 'rootDir'
          - code: UNDECLARED_PARAM
            spec: surface_transfer_adapter_impl
            at: 'readDocument'
            covers:
              - 'origin'
          - code: PARAM_NAME_MISMATCH
            spec: surface_transfer_adapter_impl
            at: 'writeSnapshotTo'
            covers:
              - 'targetPath'
              - 'rendered'
          - code: UNDECLARED_PARAM
            spec: surface_transfer_adapter_impl
            at: 'writeSnapshotTo'
            covers:
              - 'portalId'
          - code: PARAM_NAME_MISMATCH
            spec: template_source_adapter_impl
            at: 'loadTemplate'
            covers:
              - 'templateName'
          - code: UNDECLARED_PARAM
            spec: template_source_adapter_impl
            at: 'loadTemplate'
            covers:
              - 'globalOverride'
          - code: PARAM_NAME_MISMATCH
            spec: user_config_store_impl
            at: 'setDisabledAliases'
            covers:
              - 'names'
          - code: PARAM_OPTIONALITY
            spec: validator_portal_impl
            at: 'findTestsReferencing'
            covers:
              - 'testRoots'
          - code: UNREALIZED_PARAM
            spec: validator_portal_impl
            at: 'validateAsComplete'
            covers:
              - 'projectType'
          - code: PARAM_OPTIONALITY
            spec: web_graph_orchestrator_impl
            at: 'get'
            covers:
              - 'projectId'
createdAt: '2026-06-10T20:23:10.714Z'
updatedAt: '2026-06-10T20:23:10.714Z'

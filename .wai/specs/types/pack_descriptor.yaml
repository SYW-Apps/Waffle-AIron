kind: value-object
id: pack_descriptor
name: PackDescriptor
description: >-
  The result of probing one extension pack: its declared name, where it lives, and how many
  profiles/languages/rules it contributes — or the load error if it failed to load. Used by the
  hosted pack-management surface to list and confirm installs without exposing pack internals.
fields:
  - name: name
    type: string
    description: The pack's declared name (or its ref basename if unnamed)
    optional: false
  - name: scope
    type: string
    description: '"global" (server-wide) | "project" (a hosted project''s committed packs)'
    optional: false
  - name: ref
    type: string
    description: The resolved pack ref (global file path, or the project-relative .wai/packs/... registration)
    optional: false
  - name: profiles
    type: number
    description: Count of architectural profiles the pack contributes
    optional: false
  - name: languages
    type: number
    description: Count of language/platform tables the pack contributes
    optional: false
  - name: rules
    type: number
    description: Count of programmatic rules the pack contributes (always 0 for declarative packs)
    optional: false
  - name: error
    type: string
    description: The load error if the pack failed to load, else absent
    optional: true
  - name: tier
    type: string
    description: >-
      For a global pack, which tier it was discovered in: "image" (immutable image-layer directory,
      WAIRON_IMAGE_PACKS_DIR) or "instance" (mutable instance directory, WAIRON_PACKS_DIR). Absent
      for project-scoped packs.
    optional: true
  - name: shadowed
    type: boolean
    description: >-
      True for an image-tier pack that is shadowed by a same-named instance pack (the instance copy
      is the effective one). Reported as drift; absent/false otherwise.
    optional: true
  - name: profileIds
    type: string[]
    description: >-
      The ids of the architectural profiles this pack contributes (the same profiles the `profiles`
      count summarizes) — lets a UI offer a pack's profiles as structured choices instead of a bare
      count. Absent on a pack that failed to load.
    optional: true
  - name: languageIds
    type: string[]
    description: The ids of the language/platform tables this pack contributes. Absent on a load failure.
    optional: true
  - name: ruleIds
    type: string[]
    description: >-
      The ids/codes of the programmatic rules this pack contributes (empty for a declarative pack).
      Absent on a load failure.
    optional: true
methods: []
createdAt: '2026-07-04T18:42:23.978Z'
updatedAt: '2026-07-22T19:11:38.733Z'

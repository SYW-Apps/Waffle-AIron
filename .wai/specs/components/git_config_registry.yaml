id: git_config_registry
name: Git Config Registry
description: >-
  I/O manager for a project's git-backing config (.wai/git.json): remote URL, default branch,
  working branch, and enabled flag. Modeled as a single file-backed Registry, mirroring the
  spec_loader convention.
subsystem: sdd_git
componentType: Store
owns: []
dependsOn: []
lint:
  allow:
    - code: UNOWNED_STORE
      reason: >-
        Option F registry-taxonomy ruling (2026-07-19): deliberate standalone Store — per-project
        git-backing configuration is genuinely simple held state (one config file per scoped repo),
        consumed only by the git orchestrator. durability: read-through to be declared once the
        level3 branch merges (the enum value does not exist in this branch's schema).
status: complete
createdAt: '2026-07-04T16:31:20.264Z'
updatedAt: '2026-07-19T08:57:27.901Z'

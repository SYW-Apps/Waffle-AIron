id: iauthoring_orchestrator
name: Authoring Orchestrator Interface
description: >-
  The gated write workflows behind the authoring portal. A write that violates an intrinsic rule
  throws with the rule codes and nothing reaches disk; a write that changes nothing writes nothing
  and reports that it did not.
component: authoring_orchestrator
methods:
  - name: addComponent
    description: >-
      Author a NEW L2 component: resolve the project's own rule severities, judge the candidate
      against the intrinsic rules, refuse it on an error before anything touches disk, and otherwise
      persist it. Returns the candidate's warnings as notices, followed by the store's placement
      notices.
    signature: 'addComponent(candidate: ComponentSpec): string[]'
    returns: string[]
    params:
      - name: candidate
        type: ComponentSpec
        description: The component as the caller authored it
  - name: updateSpecGated
    description: >-
      Patch an existing spec of any level through the same judgement, by injecting it into the
      store's delta update as a write hook. The hook judges the MERGED spec, because a delta on its
      own cannot tell you the resulting componentType, and it is the resulting componentType that
      decides whether a field belongs. Answers with the change report: exactly what changed, or that
      nothing did and nothing was written.
    signature: 'updateSpecGated(kind: string, id: string, delta: SpecDelta): SpecChangeReport'
    returns: SpecChangeReport
    params:
      - name: kind
        type: string
        description: system, subsystem, component, interface, implementation or type
      - name: id
        type: string
        description: The stored spec's id
      - name: delta
        type: SpecDelta
        description: The partial change to merge, shaped like the spec, carrying the delta's own verbs
status: complete
createdAt: '2026-09-18T21:12:15.156Z'
updatedAt: '2026-09-18T21:41:40.295Z'

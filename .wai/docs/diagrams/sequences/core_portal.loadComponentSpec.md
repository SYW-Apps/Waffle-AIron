# Core Portal.loadComponentSpec — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "Core Portal.loadComponentSpec — narrative sequence"
---
sequenceDiagram
  autonumber
  participant core_portal as Core Portal «Portal»
  Note over core_portal: loadComponentSpec()
  participant core_orchestrator as Core Orchestrator «Orchestrator»
  core_portal->>+core_orchestrator: loadComponentSpec()
  participant spec_loader as Spec Loader «Registry»
  core_orchestrator->>+spec_loader: loadComponentSpec()
  Note over spec_loader: Read the specified component spec file and validate against Component…
  spec_loader-->>-core_orchestrator: return
  core_orchestrator-->>-core_portal: return
```

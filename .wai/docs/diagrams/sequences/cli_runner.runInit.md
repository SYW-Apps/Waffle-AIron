# CLI Runner.runInit — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "CLI Runner.runInit — narrative sequence"
---
sequenceDiagram
  autonumber
  participant cli_runner as CLI Runner «Orchestrator»
  Note over cli_runner: runInit()
  Note over cli_runner: Verify if workspace is already initialized
  Note over cli_runner: Bootstrap standard .wai directory structure
  participant cli_skills_adapter as CLI Skills Client Adapter «Adapter»
  cli_runner->>+cli_skills_adapter: exportSddSkills()
  participant skills_portal as Skills Portal «Portal»
  cli_skills_adapter->>+skills_portal: exportSddSkills()
  participant skills_orchestrator as Skills Orchestrator «Orchestrator»
  skills_portal->>skills_orchestrator: exportSddSkills()
  skills_portal-->>-cli_skills_adapter: return
  cli_skills_adapter-->>-cli_runner: return
  Note over cli_runner: Generate default L0/L1/L2 spec files if empty
```

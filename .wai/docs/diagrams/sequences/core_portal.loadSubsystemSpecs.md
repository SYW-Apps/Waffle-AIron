# Core Portal.loadSubsystemSpecs — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "Core Portal.loadSubsystemSpecs — narrative sequence"
---
sequenceDiagram
  autonumber
  participant core_portal as Core Portal «Portal»
  Note over core_portal: loadSubsystemSpecs()
  participant core_orchestrator as Core Orchestrator «Orchestrator»
  core_portal->>+core_orchestrator: loadSubsystemSpecs()
  participant spec_loader as Spec Loader «Registry»
  core_orchestrator->>+spec_loader: loadSubsystemSpecs()
  Note over spec_loader: List all YAML files in the subsystems specification directory
  Note over spec_loader: Read and parse each file against SubsystemSpecSchema, collecting Zod …
  spec_loader-->>-core_orchestrator: return
  core_orchestrator-->>-core_portal: return
```

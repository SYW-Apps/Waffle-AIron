# MCP Portal.sdd_add_subsystem — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "MCP Portal.sdd_add_subsystem — narrative sequence"
---
sequenceDiagram
  autonumber
  participant mcp_portal as MCP Portal «Portal»
  Note over mcp_portal: sdd_add_subsystem()
  participant mcp_orchestrator as MCP Orchestrator «Orchestrator»
  mcp_portal->>+mcp_orchestrator: addSubsystem()
  participant mcp_core_adapter as MCP Core Client Adapter «Adapter»
  mcp_orchestrator->>+mcp_core_adapter: loadSystemSpec()
  participant core_portal as Core Portal «Portal»
  mcp_core_adapter->>core_portal: loadSystemSpec()
  mcp_core_adapter-->>-mcp_orchestrator: return
  mcp_orchestrator->>+mcp_core_adapter: saveSubsystemSpec()
  mcp_core_adapter->>core_portal: saveSubsystemSpec()
  mcp_core_adapter-->>-mcp_orchestrator: return
  mcp_orchestrator-->>-mcp_portal: return
```

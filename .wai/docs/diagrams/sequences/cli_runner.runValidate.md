# CLI Runner.runValidate — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "CLI Runner.runValidate — narrative sequence"
---
sequenceDiagram
  autonumber
  participant cli_runner as CLI Runner «Orchestrator»
  Note over cli_runner: runValidate()
  Note over cli_runner: Load project configuration file
  participant cli_validator_adapter as CLI Validator Client Adapter «Adapter»
  cli_runner->>+cli_validator_adapter: validateRegistry()
  participant validator_portal as Validator Portal «Portal»
  cli_validator_adapter->>+validator_portal: validateRegistry()
  participant spec_validator as Spec Validator «Specialist»
  validator_portal->>spec_validator: validateRegistry()
  validator_portal-->>-cli_validator_adapter: return
  cli_validator_adapter-->>-cli_runner: return
  cli_runner->>+cli_validator_adapter: validateProjectConfig()
  cli_validator_adapter->>+validator_portal: validateProjectConfig()
  validator_portal->>spec_validator: validateProjectConfig()
  validator_portal-->>-cli_validator_adapter: return
  cli_validator_adapter-->>-cli_runner: return
  cli_runner->>+cli_validator_adapter: validateSddTree()
  cli_validator_adapter->>+validator_portal: validateSddTree()
  validator_portal->>spec_validator: validateSddTree()
  validator_portal-->>-cli_validator_adapter: return
  cli_validator_adapter-->>-cli_runner: return
  Note over cli_runner: Print validation errors/warnings and exit process if invalid
```

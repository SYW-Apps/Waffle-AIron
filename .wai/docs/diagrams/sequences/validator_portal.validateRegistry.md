# Validator Portal.validateRegistry — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "Validator Portal.validateRegistry — narrative sequence"
---
sequenceDiagram
  autonumber
  participant validator_portal as Validator Portal «Portal»
  Note over validator_portal: validateRegistry()
  participant spec_validator as Spec Validator «Specialist»
  validator_portal->>+spec_validator: validateRegistry()
  Note over spec_validator: Validate agent ID uniqueness
  Note over spec_validator: Validate per-agent properties like owned paths
  Note over spec_validator: Check for overlapping workspace file ownerships
  spec_validator-->>-validator_portal: return
```

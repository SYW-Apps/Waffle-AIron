# Validator Portal.validateProjectConfig — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "Validator Portal.validateProjectConfig — narrative sequence"
---
sequenceDiagram
  autonumber
  participant validator_portal as Validator Portal «Portal»
  Note over validator_portal: validateProjectConfig()
  participant spec_validator as Spec Validator «Specialist»
  validator_portal->>+spec_validator: validateProjectConfig()
  Note over spec_validator: Ensure at least one output target is configured in project.yaml
  Note over spec_validator: Verify that configured targets are enabled
  spec_validator-->>-validator_portal: return
```

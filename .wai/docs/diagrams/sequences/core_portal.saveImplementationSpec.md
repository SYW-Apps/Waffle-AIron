# Core Portal.saveImplementationSpec — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "Core Portal.saveImplementationSpec — narrative sequence"
---
sequenceDiagram
  autonumber
  participant core_portal as Core Portal «Portal»
  Note over core_portal: saveImplementationSpec()
  participant core_orchestrator as Core Orchestrator «Orchestrator»
  core_portal->>+core_orchestrator: saveImplementationSpec()
  participant spec_loader as Spec Loader «Registry»
  core_orchestrator->>+spec_loader: saveImplementationSpec()
  Note over spec_loader: Update the updatedAt timestamp and write the implementation spec to i…
  spec_loader-->>-core_orchestrator: return
  core_orchestrator-->>-core_portal: return
```

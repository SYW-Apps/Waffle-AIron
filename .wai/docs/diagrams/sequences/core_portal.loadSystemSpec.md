# Core Portal.loadSystemSpec — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "Core Portal.loadSystemSpec — narrative sequence"
---
sequenceDiagram
  autonumber
  participant core_portal as Core Portal «Portal»
  Note over core_portal: loadSystemSpec()
  participant core_orchestrator as Core Orchestrator «Orchestrator»
  core_portal->>+core_orchestrator: loadSystemSpec()
  participant spec_loader as Spec Loader «Registry»
  core_orchestrator->>+spec_loader: loadSystemSpec()
  Note over spec_loader: Determine the system spec path using AI_PATHS.specsSystem()
  Note over spec_loader: Read system.yaml if it exists and parse it using SystemSpecSchema
  spec_loader-->>-core_orchestrator: return
  core_orchestrator-->>-core_portal: return
```

# Core Portal.loadImplementationSpecs — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "Core Portal.loadImplementationSpecs — narrative sequence"
---
sequenceDiagram
  autonumber
  participant core_portal as Core Portal «Portal»
  Note over core_portal: loadImplementationSpecs()
  participant core_orchestrator as Core Orchestrator «Orchestrator»
  core_portal->>+core_orchestrator: loadImplementationSpecs()
  participant spec_loader as Spec Loader «Registry»
  core_orchestrator->>+spec_loader: loadImplementationSpecs()
  Note over spec_loader: List all YAML files in the implementations specification directory
  Note over spec_loader: Read and parse each file against ImplementationSpecSchema, collecting…
  spec_loader-->>-core_orchestrator: return
  core_orchestrator-->>-core_portal: return
```

# CLI Runner.runShow — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "CLI Runner.runShow — narrative sequence"
---
sequenceDiagram
  autonumber
  participant cli_runner as CLI Runner «Orchestrator»
  Note over cli_runner: runShow()
  participant cli_core_adapter as CLI Core Client Adapter «Adapter»
  cli_runner->>+cli_core_adapter: loadSystemSpec()
  participant core_portal as Core Portal «Portal»
  cli_core_adapter->>+core_portal: loadSystemSpec()
  participant core_orchestrator as Core Orchestrator «Orchestrator»
  core_portal->>core_orchestrator: loadSystemSpec()
  core_portal-->>-cli_core_adapter: return
  cli_core_adapter-->>-cli_runner: return
  Note over cli_runner: Format and display details for specified dynamic agent
```

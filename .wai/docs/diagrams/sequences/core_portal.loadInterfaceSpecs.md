# Core Portal.loadInterfaceSpecs — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "Core Portal.loadInterfaceSpecs — narrative sequence"
---
sequenceDiagram
  autonumber
  participant core_portal as Core Portal «Portal»
  Note over core_portal: loadInterfaceSpecs()
  participant core_orchestrator as Core Orchestrator «Orchestrator»
  core_portal->>+core_orchestrator: loadInterfaceSpecs()
  participant spec_loader as Spec Loader «Registry»
  core_orchestrator->>+spec_loader: loadInterfaceSpecs()
  Note over spec_loader: List all YAML files in the interfaces specification directory
  Note over spec_loader: Read and parse each file against InterfaceSpecSchema, collecting Zod …
  spec_loader-->>-core_orchestrator: return
  core_orchestrator-->>-core_portal: return
```

# Core Portal.resolveAgentTopology — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "Core Portal.resolveAgentTopology — narrative sequence"
---
sequenceDiagram
  autonumber
  participant core_portal as Core Portal «Portal»
  Note over core_portal: resolveAgentTopology()
  participant agent_resolver as Agent Topology Resolver «Orchestrator»
  core_portal->>+agent_resolver: resolveAgentTopology()
  participant spec_loader as Spec Loader «Registry»
  agent_resolver->>+spec_loader: loadSystemSpec()
  Note over spec_loader: Determine the system spec path using AI_PATHS.specsSystem()
  Note over spec_loader: Read system.yaml if it exists and parse it using SystemSpecSchema
  spec_loader-->>-agent_resolver: return
  agent_resolver->>+spec_loader: loadSubsystemSpecs()
  Note over spec_loader: List all YAML files in the subsystems specification directory
  Note over spec_loader: Read and parse each file against SubsystemSpecSchema, collecting Zod …
  spec_loader-->>-agent_resolver: return
  agent_resolver->>+spec_loader: loadComponentSpecs()
  Note over spec_loader: List all YAML files in the components specification directory
  Note over spec_loader: Read and parse each file against ComponentSpecSchema, collecting Zod …
  spec_loader-->>-agent_resolver: return
  agent_resolver->>+spec_loader: loadInterfaceSpecs()
  Note over spec_loader: List all YAML files in the interfaces specification directory
  Note over spec_loader: Read and parse each file against InterfaceSpecSchema, collecting Zod …
  spec_loader-->>-agent_resolver: return
  agent_resolver->>+spec_loader: loadImplementationSpecs()
  Note over spec_loader: List all YAML files in the implementations specification directory
  Note over spec_loader: Read and parse each file against ImplementationSpecSchema, collecting…
  spec_loader-->>-agent_resolver: return
  Note over agent_resolver: Combine loaded specs to build a dynamic list of AgentRecord objects r…
  agent_resolver-->>-core_portal: return
```

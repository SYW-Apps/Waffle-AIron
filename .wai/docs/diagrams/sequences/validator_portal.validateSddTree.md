# Validator Portal.validateSddTree — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "Validator Portal.validateSddTree — narrative sequence"
---
sequenceDiagram
  autonumber
  participant validator_portal as Validator Portal «Portal»
  Note over validator_portal: validateSddTree()
  participant spec_validator as Spec Validator «Specialist»
  validator_portal->>+spec_validator: validateSddTree()
  Note over spec_validator: Clear loader issues
  participant validator_core_adapter as Validator Core Client Adapter «Adapter»
  spec_validator->>+validator_core_adapter: loadSystemSpec()
  participant core_portal as Core Portal «Portal»
  validator_core_adapter->>core_portal: loadSystemSpec()
  validator_core_adapter-->>-spec_validator: return
  spec_validator->>+validator_core_adapter: loadSubsystemSpecs()
  validator_core_adapter->>core_portal: loadSubsystemSpecs()
  validator_core_adapter-->>-spec_validator: return
  spec_validator->>+validator_core_adapter: loadComponentSpecs()
  validator_core_adapter->>core_portal: loadComponentSpecs()
  validator_core_adapter-->>-spec_validator: return
  spec_validator->>+validator_core_adapter: loadInterfaceSpecs()
  validator_core_adapter->>core_portal: loadInterfaceSpecs()
  validator_core_adapter-->>-spec_validator: return
  spec_validator->>+validator_core_adapter: loadImplementationSpecs()
  validator_core_adapter->>core_portal: loadImplementationSpecs()
  validator_core_adapter-->>-spec_validator: return
  Note over spec_validator: Integrate Zod schema loader errors
  Note over spec_validator: Validate spec hierarchy and entity references
  Note over spec_validator: Validate method implementation signatures against contracts
  Note over spec_validator: Validate narrative call steps target declared dependencies
  Note over spec_validator: Enforce architectural component type interaction boundaries
  Note over spec_validator: Run DFS circular dependency cycle detection
  spec_validator-->>-validator_portal: return
```

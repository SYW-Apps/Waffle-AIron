# CLI Runner.runUpdate — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "CLI Runner.runUpdate — narrative sequence"
---
sequenceDiagram
  autonumber
  participant cli_runner as CLI Runner «Orchestrator»
  Note over cli_runner: runUpdate()
  Note over cli_runner: Check remote github release page for new releases
  Note over cli_runner: If new release is found and not in check-only mode, pull and apply up…
```

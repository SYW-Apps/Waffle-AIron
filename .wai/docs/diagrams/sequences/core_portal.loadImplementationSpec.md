# Core Portal.loadImplementationSpec — narrative sequence

> Generated by `wairon diagram` from `.wai/specs/` — do not edit; regenerate instead.

```mermaid
---
title: "Core Portal.loadImplementationSpec — narrative sequence"
---
sequenceDiagram
  autonumber
  participant core_portal as Core Portal «Portal»
  Note over core_portal: loadImplementationSpec()
  participant core_orchestrator as Core Orchestrator «Orchestrator»
  core_portal->>+core_orchestrator: loadImplementationSpec()
  participant spec_loader as Spec Loader «Registry»
  core_orchestrator->>+spec_loader: loadImplementationSpec()
  Note over spec_loader: Read the specified implementation spec file and validate against Impl…
  spec_loader-->>-core_orchestrator: return
  core_orchestrator-->>-core_portal: return
```
